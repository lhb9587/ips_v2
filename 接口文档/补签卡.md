# 补签卡接口文档

## 一、接口清单

### 1. 员工端（我要补签）

| 序号 | 接口名 | 路径 | 是否完成 |
| ---- | ------ | ---- | -------- |
| 1 | 【我要补签】补签申请初始化 | `POST /api/supplement/request/self/init` | 已完成 |
| 2 | 【我要补签】保存补签草稿（支持保存并提交） | `POST /api/supplement/request/self/save` | 已完成 |
| 3 | 【我要补签】我的补签列表分页 | `POST /api/supplement/request/self/page` | 已完成 |
| 4 | 【我要补签】补签详情 | `POST /api/supplement/request/admin/detail` | 已完成 |
| 5 | 【我要补签】删除补签草稿 | `POST /api/supplement/request/self/delete` | 已完成 |
| 6 | 【我要补签】废弃补签单 | `POST /api/supplement/request/self/abandon` | 已完成 |

### 2. 后台（补签卡管理）

| 序号 | 接口名 | 路径 | 是否完成 |
| ---- | ------ | ---- | -------- |
| 1 | 补签卡列表分页 | `POST /api/supplement/request/admin/page` | 已完成 |
| 2 | 补签卡详情查询（统一详情入口） | `POST /api/supplement/request/admin/detail` | 已完成 |
| 3 | 补签卡提交生效 | `POST /api/supplement/request/admin/direct-pass` | 已完成 |
| 4 | 补签卡反审批 | `POST /api/supplement/request/admin/reverse-approve` | 已完成 |
| 5 | 补签卡废弃 | `POST /api/supplement/request/admin/abandon` | 已完成 |
| 6 | 补签卡导出 | `POST /api/supplement/request/admin/export` | 已完成 |
| 7 | 缺卡检查分页 | `POST /api/supplement/request/admin/missing/page` | 已完成 |
| 8 | 缺卡检查批量生成补签卡 | `POST /api/supplement/request/admin/missing/generate` | 已完成 |

---

## 二、补签申请初始化

`POST /api/supplement/request/self/init`

### 权限

`@ScopeAuthRequired`

### 请求参数

表单传参，不使用 `@RequestBody`。

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| talentCode | String | 否 | 指定员工编码；不传时默认当前登录员工 |
| sourceDateTime | String | 否 | 从异常入口带入的默认补签时间，格式 `yyyy-MM-dd HH:mm` |
| sourceType | String | 否 | 来源标识，如 `calendar`、`punch_record`、`missing_check` |

### 返回

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| requestNoPreview | String | 补签单号预览（不落库） |
| employee | Object | 员工信息 |
| employee.userId | Long | 用户 ID |
| employee.talentCode | String | 员工编码 |
| employee.talentName | String | 员工姓名 |
| employee.deptCode | String | 部门编码 |
| employee.deptName | String | 部门名称 |
| employee.positionId | String | 岗位 ID |
| employee.positionName | String | 岗位名称 |
| employee.empStatus | String | 用工状态 |
| supplementTypes | List | 补签卡类型（固定返回 `REPAIR_CARD/补卡`） |
| supplementTypes[].typeCode | String | 类型编码 |
| supplementTypes[].typeName | String | 类型名称 |
| supplementTypes[].sortNo | Integer | 排序 |
| supplementReasons | List | 补签卡原因（从 `attendance_supplement_reason` 查询） |
| supplementReasons[].reasonCode | String | 原因编码 |
| supplementReasons[].reasonName | String | 原因名称 |
| supplementReasons[].allowAdvance | Boolean | 是否允许提前补签 |
| supplementReasons[].sortNo | Integer | 排序 |
| defaultDetail | Object | 默认补签明细 |
| defaultDetail.attendanceDateTime | String | 默认补签时间（从 `sourceDateTime` 带入） |
| defaultDetail.remark | String | 默认备注 |
| canApply | Boolean | 是否允许申请 |
| disabledReason | String | 不允许原因（非在职/无考勤档案） |

### 业务规则

- 不传 `talentCode`：查当前登录员工；传则查指定员工并校验权限
- 权限规则：本人/管理员→通过；HR/考勤员/部门负责人→deptCode 在权限范围内→通过；其他→拒绝
- 补签类型固定返回 `REPAIR_CARD/补卡`，不查数据库
- 单号预览 `BQKD-yyyyMMdd-XXXX`，仅预览不落库
- `canApply`：非在职→false；无有效考勤档案→false

### 返回示例

```json
{
  "success": true,
  "data": {
    "requestNoPreview": "BQKD20260601-0001",
    "employee": {
      "userId": 3037,
      "talentCode": "30028",
      "talentName": "潘跃文",
      "deptCode": "10803",
      "deptName": "技术研发部",
      "positionId": "201",
      "positionName": "高级工程师",
      "empStatus": "在职"
    },
    "supplementTypes": [
      {
        "typeCode": "REPAIR_CARD",
        "typeName": "补卡",
        "sortNo": 1
      }
    ],
    "supplementReasons": [
      {
        "reasonCode": "001",
        "reasonName": "忘记打卡",
        "allowAdvance": false,
        "sortNo": 1
      },
      {
        "reasonCode": "002",
        "reasonName": "外出公干",
        "allowAdvance": true,
        "sortNo": 2
      }
    ],
    "defaultDetail": {
      "attendanceDateTime": "2026-06-01 09:00",
      "supplementType": "REPAIR_CARD",
      "reasonCode": null,
      "reasonName": null,
      "remark": null
    },
    "canApply": true,
    "disabledReason": null
  },
  "total": null,
  "currPage": null,
  "message": "操作成功",
  "messageType": 200
}
```

---

## 三、保存补签草稿

`POST /api/supplement/request/self/save`

### 权限

`@ScopeAuthRequired`

### 请求参数

表单传参，不使用 `@RequestBody`。

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| supplementRequestId | Long | 否 | 补签单业务 ID，空=新增，非空=修改 |
| talentCode | String | 否 | 员工编码，不传默认当前登录人 |
| attendanceDateTime | String | 是 | 补签时间，格式 `yyyy-MM-dd HH:mm` |
| segmentNo | Integer | 否 | 段次，默认 1 |
| reasonCode | String | 是 | 补签原因编码 |
| remark | String | 否 | 备注 |
| attachmentIds | String | 否 | 附件 ID，逗号分隔，如 `1001,1002` |
| actionType | String | 否 | 操作类型：`save`=保存草稿（默认），`submit`=保存并提交审批 |

### 返回

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| supplementRequestId | Long | 补签单业务 ID |
| requestNo | String | 补签单号 |
| status | String | 状态（`save`→`未提交`，`submit`→`审批中`） |
| saved | Boolean | 是否保存成功 |
| submitted | Boolean | 是否已提交审批（`save`→false，`submit`→true） |
| approvalInstanceId | Long | 审批实例 ID（仅 `submit` 时有值） |
| taskId | Long | 审批任务 ID（仅 `submit` 时有值） |
| talentCode | String | 员工编码 |
| talentName | String | 员工姓名 |
| attendanceDateTime | String | 补签时间 |
| reasonCode | String | 补签原因编码 |
| reasonName | String | 补签原因名称 |

### 业务规则

- 补签类型固定 `REPAIR_CARD`，前端不传；原因名称从 `attendance_supplement_reason` 查
- 校验顺序：员工在职→有效考勤档案→时间格式→原因启用→提前补签权限→重复补签
- 重复校验：同一员工同一补签时间不允许重复（修改时排除当前单据）
- 提前补签：原因 `allowAdvance=0` 时不允许补未来日期
- 新增：生成单号 `BQKD + yyyyMMddHHmmssSSS + 3位序号`，业务 ID `毫秒时间戳*1000+序号`
- 修改：只允许 `未提交`/`已退回`；已退回保存后清空 `submit_time`/`approve_time`，禁用旧审批绑定
- 修改时先逻辑删除旧明细和旧附件，再插入新的
- `actionType` 不传或 `save`：只保存草稿，不创建审批
- `actionType=submit`：先保存/修改补签单，再调用 `ApprovalWorkflowSubmitService.submit()` 提交审批中心，状态变为 `审批中`
- 提交审批由 `ApprovalWorkflowSubmitService` 统一创建审批实例、任务、审核人、动作日志，并通过 `ApprovalBusinessStatusWriteBackService` 回写业务状态为 `审批中`
- 审批中单据不允许再次 `submit`，由审批服务重复提交拦截
- 不写考勤事件、不改打卡记录、不触发计算

### 返回示例

**actionType=save（保存草稿）**

```json
{
  "success": true,
  "data": {
    "supplementRequestId": 17487654321000123,
    "requestNo": "BQKD20260601093000123001",
    "status": "未提交",
    "saved": true,
    "talentCode": "30028",
    "talentName": "潘跃文",
    "attendanceDateTime": "2026-06-01 09:00",
    "reasonCode": "001",
    "reasonName": "忘记打卡",
    "submitted": false,
    "approvalInstanceId": null,
    "taskId": null
  },
  "total": null,
  "currPage": null,
  "message": "操作成功",
  "messageType": 200
}
```

**actionType=submit（保存并提交审批）**

```json
{
  "success": true,
  "data": {
    "supplementRequestId": 17487654321000123,
    "requestNo": "BQKD20260601093000123001",
    "status": "审批中",
    "saved": true,
    "talentCode": "30028",
    "talentName": "潘跃文",
    "attendanceDateTime": "2026-06-01 09:00",
    "reasonCode": "001",
    "reasonName": "忘记打卡",
    "submitted": true,
    "approvalInstanceId": 17487654321000456,
    "taskId": 17487654321000789
  },
  "total": null,
  "currPage": null,
  "message": "操作成功",
  "messageType": 200
}
```

---

## 四、我的补签列表分页

`POST /api/supplement/request/self/page`

### 权限

无 `@ScopeAuthRequired`，只需要 token 校验。通过 `request.getAttribute` 获取 `userId`，只查本人补签单。

### 请求参数

表单传参，不使用 `@RequestBody`。

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| pageNo | Integer | 否 | 页码，默认 1 |
| pageSize | Integer | 否 | 每页条数，默认 10，最大 100 |
| requestNo | String | 否 | 补签单号，模糊查询 |
| status | String | 否 | 单据状态：未提交/审批中/已退回/已通过/已撤回 |
| startDate | String | 否 | 补签开始日期，格式 `yyyy-MM-dd`，按明细 `attendance_datetime` 筛选 |
| endDate | String | 否 | 补签结束日期，格式 `yyyy-MM-dd`，按明细 `attendance_datetime` 筛选 |
| reasonCode | String | 否 | 补签原因编码 |

### 返回

分页格式 `ReturnInfo.page(records, total, pageNo)`。

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| supplementRequestId | Long | 补签单业务 ID |
| requestNo | String | 补签单号 |
| status | String | 单据状态 |
| approvalStatus | String | 审批状态（取审批实例状态，无则取单据状态） |
| talentCode | String | 员工编码 |
| talentName | String | 员工姓名 |
| deptCode | String | 部门编码 |
| deptName | String | 部门名称 |
| supplementType | String | 补签类型（固定 `REPAIR_CARD`） |
| supplementTypeName | String | 补签类型名称（固定 `补卡`） |
| attendanceDateTime | String | 第一条补签时间 |
| reasonCode | String | 原因编码 |
| reasonName | String | 原因名称 |
| detailCount | Integer | 明细数量 |
| submitTime | String | 提交时间 |
| createTime | String | 创建时间 |
| approvalInstanceId | Long | 审批实例 ID |
| currentApproverName | String | 当前审批人姓名，多个逗号拼接 |
| canEdit | Boolean | 本人 + 未提交/已退回 |
| canDelete | Boolean | 本人 + 未提交 |
| canSubmit | Boolean | 本人 + 未提交/已退回 |
| canAbandon | Boolean | 本人 + 审批中 + 有审批实例 + 无人处理过 |

### 业务规则

- 只查当前登录员工本人的补签单
- 日期筛选按明细 `attendance_datetime`，`endDate` 包含当天
- 排序：`create_time DESC`
- 按钮权限按状态计算，天然是本人不需要额外归属校验

### 返回示例

```json
{
  "success": true,
  "data": [
    {
      "supplementRequestId": 17487654321000123,
      "requestNo": "BQKD20260601093000123001",
      "status": "审批中",
      "approvalStatus": "审批中",
      "talentCode": "30028",
      "talentName": "潘跃文",
      "deptCode": "10803",
      "deptName": "技术研发部",
      "supplementType": "REPAIR_CARD",
      "supplementTypeName": "补卡",
      "attendanceDateTime": "2026-06-01 09:00",
      "reasonCode": "001",
      "reasonName": "忘记打卡",
      "detailCount": 1,
      "submitTime": "2026-06-01 09:30",
      "createTime": "2026-06-01 09:28",
      "approvalInstanceId": 17487654321000456,
      "currentApproverName": "张主管",
      "canEdit": false,
      "canDelete": false,
      "canSubmit": false,
      "canAbandon": true
    },
    {
      "supplementRequestId": 17487654321000999,
      "requestNo": "BQKD20260601093000999002",
      "status": "未提交",
      "approvalStatus": "未提交",
      "talentCode": "30028",
      "talentName": "潘跃文",
      "deptCode": "10803",
      "deptName": "技术研发部",
      "supplementType": "REPAIR_CARD",
      "supplementTypeName": "补卡",
      "attendanceDateTime": "2026-05-30 14:00",
      "reasonCode": "002",
      "reasonName": "外出公干",
      "detailCount": 1,
      "submitTime": null,
      "createTime": "2026-05-30 18:00",
      "approvalInstanceId": null,
      "currentApproverName": null,
      "canEdit": true,
      "canDelete": true,
      "canSubmit": true,
      "canAbandon": false
    }
  ],
  "total": 2,
  "currPage": 1,
  "message": "操作成功",
  "messageType": 200
}
```

---

## 五、补签卡详情（统一详情入口）

`POST /api/supplement/request/admin/detail`

后台和员工端共用此接口，员工端不再单独做 `/self/detail`。

### 权限

`@ScopeAuthRequired`（加载权限上下文用于按钮计算，查询本身不按权限范围过滤，任何已登录用户可查看）

### 请求参数

表单传参，不使用 `@RequestBody`。

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| supplementRequestId | Long | 是 | 补签单业务 ID |

### 返回

#### 主表字段

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| supplementRequestId | Long | 补签单业务 ID |
| requestNo | String | 补签单号 |
| status | String | 单据状态（未提交/审批中/已退回/已通过/已撤回） |
| talentCode | String | 员工编码 |
| talentName | String | 员工姓名 |
| userId | Long | 用户 ID |
| deptCode | String | 部门编码 |
| deptName | String | 部门名称 |
| positionName | String | 岗位名称 |
| supplementType | String | 补签类型（固定 `REPAIR_CARD`） |
| supplementTypeName | String | 补签类型名称（固定 `补卡`） |
| applyTime | String | 申请时间 |
| submitTime | String | 提交时间 |
| remark | String | 备注 |

#### 明细字段

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| details | List | 补签明细列表 |
| details[].detailId | Long | 明细 ID |
| details[].segmentNo | Integer | 段次 |
| details[].attendanceDateTime | String | 补签时间 |
| details[].reasonCode | String | 原因编码 |
| details[].reasonName | String | 原因名称 |
| details[].allowAdvance | Integer | 是否允许提前补签（0/1） |
| details[].effectStatus | String | 生效状态（未生效/已生效/已回滚） |
| details[].effectPunchRecordId | Long | 生效打卡记录 ID |
| details[].remark | String | 备注 |

#### 附件字段

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| attachments | List | 附件列表 |
| attachments[].attachmentId | Long | 附件 ID |
| attachments[].fileName | String | 文件名 |
| attachments[].filePath | String | 文件路径 |
| attachments[].fileUrl | String | 文件 URL |

#### 审批字段

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| approvalInstanceId | Long | 审批实例 ID |
| approvalStatus | String | 审批状态 |
| currentApproverNames | List\<String> | 当前审批人姓名列表 |
| approvalLogs | List | 审批动作日志 |
| approvalLogs[].actionId | Long | 动作 ID |
| approvalLogs[].actionType | String | 动作类型（提交/同意/退回/废弃） |
| approvalLogs[].actionResult | String | 动作结果 |
| approvalLogs[].operatorName | String | 操作人姓名 |
| approvalLogs[].actionTime | Date | 操作时间 |
| approvalLogs[].actionComment | String | 审批意见 |

#### 操作权限

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| canApprove | Boolean | 当前登录人是待审批人 + 审批中 |
| canReject | Boolean | 同 canApprove |
| canSubmit | Boolean | 申请人本人 + 未提交/已退回 |
| canEdit | Boolean | 申请人本人 + 未提交/已退回 |
| canDelete | Boolean | 申请人本人 + 未提交 |
| canAbandon | Boolean | 申请人本人 + 审批中 + 无人处理过 |
| canDirectPass | Boolean | 管理员/HR/考勤员 + 未提交/已退回 |
| canReverseApprove | Boolean | 管理员/HR/考勤员 + 已通过 |

### 返回示例

```json
{
  "success": true,
  "data": {
    "supplementRequestId": 17487654321000123,
    "requestNo": "BQKD20260601093000123001",
    "status": "审批中",
    "talentCode": "30028",
    "talentName": "潘跃文",
    "userId": 3037,
    "deptCode": "10803",
    "deptName": "技术研发部",
    "positionName": "高级工程师",
    "supplementType": "REPAIR_CARD",
    "supplementTypeName": "补卡",
    "applyTime": "2026-06-01 09:28",
    "submitTime": "2026-06-01 09:30",
    "remark": "忘记打卡",
    "details": [
      {
        "detailId": 17487654321000400,
        "segmentNo": 1,
        "attendanceDateTime": "2026-06-01 09:00",
        "reasonCode": "001",
        "reasonName": "忘记打卡",
        "allowAdvance": 0,
        "effectStatus": "未生效",
        "effectPunchRecordId": null,
        "remark": null
      }
    ],
    "attachments": [],
    "approvalInstanceId": 17487654321000456,
    "approvalStatus": "审批中",
    "currentApproverNames": ["张主管"],
    "approvalLogs": [
      {
        "actionId": 17487654321000500,
        "actionType": "提交",
        "actionResult": "审批中",
        "operatorName": "潘跃文",
        "actionTime": "2026-06-01 09:30:00",
        "actionComment": "提交补签申请"
      }
    ],
    "canApprove": false,
    "canReject": false,
    "canSubmit": false,
    "canEdit": false,
    "canDelete": false,
    "canAbandon": true,
    "canDirectPass": false,
    "canReverseApprove": false
  },
  "total": null,
  "currPage": null,
  "message": "操作成功",
  "messageType": 200
}
```

---

## 六、删除补签草稿

`POST /api/supplement/request/self/delete`

### 权限

无 `@ScopeAuthRequired`，只需要 token 校验。

### 请求参数

表单传参，不使用 `@RequestBody`。

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| supplementRequestId | Long | 否 | 单条补签单业务 ID |
| supplementRequestIds | String | 否 | 批量补签单业务 ID，逗号分隔，优先使用 |

### 返回

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| deleted | Boolean | 是否删除成功 |
| successCount | Integer | 成功删除条数 |
| supplementRequestIds | List\<Long\> | 本次删除的补签单 ID 列表 |
| supplementRequestId | Long | 单条删除时的补签单 ID（兼容前端） |

### 业务规则

- 只允许删除本人 `未提交` 或 `已退回` 的补签单
- 删除 `已退回` 单据时自动禁用旧审批绑定（`approval_biz_rel.is_current` 置为 0）
- `supplementRequestIds` 优先，`supplementRequestId` 兜底，两者同时传入只处理批量
- 支持批量，最多 100 条
- 逻辑删除主表、明细、附件关联

### 返回示例

```json
{
  "success": true,
  "data": {
    "deleted": true,
    "successCount": 2,
    "supplementRequestIds": [17487654321000123, 17487654321000456],
    "supplementRequestId": null
  },
  "total": null,
  "currPage": null,
  "message": "操作成功",
  "messageType": 200
}
```

---

## 七、废弃/撤回补签单

`POST /api/supplement/request/self/abandon`

### 权限

无 `@ScopeAuthRequired`，只需要 token 校验。

### 请求参数

表单传参，不使用 `@RequestBody`。

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| supplementRequestId | Long | 否 | 单条补签单业务 ID |
| supplementRequestIds | String | 否 | 批量补签单业务 ID，逗号分隔，优先使用 |
| remark | String | 否 | 废弃/撤回原因 |

### 返回

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| abandoned | Boolean | 是否撤回成功 |
| successCount | Integer | 成功撤回条数 |
| supplementRequestIds | List\<Long\> | 本次撤回的补签单 ID 列表 |
| supplementRequestId | Long | 单条撤回时的补签单 ID（兼容前端） |

### 业务规则

- 只允许申请人本人操作
- 只允许 `审批中` 的补签单撤回
- 已有人审批过（同意/退回）的单据不能撤回
- `未提交` 草稿请使用删除功能
- `已退回` 补签单请编辑后重新提交
- `已通过` 补签单不允许员工端撤回
- 撤回后：补签单状态改为 `未提交`，审批实例状态改为 `已撤回`，业务绑定失效，待审批任务关闭，写入动作日志（`废弃/未提交`）
- `supplementRequestIds` 优先，`supplementRequestId` 兜底，两者同时传入只处理批量
- 支持批量，最多 100 条，任意一条校验失败则整体回滚
- 不触发考勤快照事件、不改打卡记录、不触发计算

### 返回示例

```json
{
  "success": true,
  "data": {
    "abandoned": true,
    "successCount": 1,
    "supplementRequestIds": [17487654321000123],
    "supplementRequestId": 17487654321000123
  },
  "total": null,
  "currPage": null,
  "message": "操作成功",
  "messageType": 200
}
```

---

## 八、补签卡后台列表分页

`POST /api/supplement/request/admin/page`

### 权限

`@ScopeAuthRequired`

### 请求参数

表单传参，不使用 `@RequestBody`。

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| pageNo | Integer | 否 | 页码，默认 1 |
| pageSize | Integer | 否 | 每页条数，默认 20，最大 200 |
| deptCode | String | 否 | 部门编码，传父部门会展开子部门 |
| talentCode | String | 否 | 员工编码，精确匹配 |
| talentName | String | 否 | 员工姓名，模糊查询 |
| startDate | String | 否 | 补签开始日期，格式 `yyyy-MM-dd`，按明细 `attendance_datetime` 筛选 |
| endDate | String | 否 | 补签结束日期，格式 `yyyy-MM-dd`，包含当天 |
| status | String | 否 | 单据状态：未提交/审批中/已通过/已退回/已撤回/已废弃 |
| reasonCode | String | 否 | 补签原因编码 |
| requestNo | String | 否 | 补签单号，模糊查询 |

### 返回

分页格式 `ReturnInfo.page(records, total, pageNo)`。

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| supplementRequestId | Long | 补签单业务 ID |
| requestNo | String | 补签单号 |
| talentCode | String | 员工编码 |
| talentName | String | 员工姓名 |
| userId | Long | 用户 ID |
| deptCode | String | 部门编码 |
| deptName | String | 部门名称 |
| posName | String | 岗位名称 |
| attendanceDateTime | String | 第一条补签时间 |
| reasonCode | String | 原因编码 |
| reasonName | String | 原因名称 |
| supplementType | String | 补签类型（固定 `REPAIR_CARD`） |
| supplementTypeName | String | 补签类型名称（固定 `补卡`） |
| status | String | 单据状态 |
| sourceEntry | String | 来源入口 |
| applyTime | String | 申请时间 |
| submitTime | String | 提交时间 |
| approveTime | String | 通过时间 |
| approvalInstanceId | Long | 审批实例 ID |
| approvalStatus | String | 审批状态 |
| currentApproverNames | String | 当前审批人姓名，多个逗号分隔 |
| canApprove | Boolean | 当前用户可审批 |
| canReject | Boolean | 当前用户可退回 |
| canDirectPass | Boolean | 管理员/HR/考勤员可提交生效 |
| canReverseApprove | Boolean | 管理员/HR/考勤员可反审批 |
| canAbandon | Boolean | 后台固定 false |

### 业务规则

- `@ScopeAuthRequired` 加载权限上下文
- 普通员工（selfOnly）返回空列表
- 部门范围：`deptCode` 为空时使用 `deptScopes`；不为空时展开父部门子部门并与 `deptScopes` 取交集
- 系统管理员 `deptCode` 为空时不限制部门
- 日期筛选按明细 `attendance_datetime`，`endDate` 包含当天
- 排序：`submit_time DESC, create_time DESC, request_id DESC`
- 按钮权限：
  - `canApprove/canReject`：当前用户是待审批人 + 状态=审批中
  - `canDirectPass`：管理员/HR/考勤员 + 未提交/已退回
  - `canReverseApprove`：管理员/HR/考勤员 + 已通过
  - `canAbandon`：后台固定 false

### 返回示例

```json
{
  "success": true,
  "data": [
    {
      "supplementRequestId": 17487654321000123,
      "requestNo": "BQKD20260601093000123001",
      "talentCode": "30028",
      "talentName": "潘跃文",
      "userId": 3037,
      "deptCode": "10803",
      "deptName": "技术研发部",
      "posName": "高级工程师",
      "attendanceDateTime": "2026-06-01 09:00",
      "reasonCode": "001",
      "reasonName": "忘记打卡",
      "supplementType": "REPAIR_CARD",
      "supplementTypeName": "补卡",
      "status": "审批中",
      "sourceEntry": "SELF",
      "applyTime": "2026-06-01 09:28",
      "submitTime": "2026-06-01 09:30",
      "approveTime": null,
      "approvalInstanceId": 17487654321000456,
      "approvalStatus": "审批中",
      "currentApproverNames": "张主管",
      "canApprove": false,
      "canReject": false,
      "canDirectPass": false,
      "canReverseApprove": false,
      "canAbandon": false
    },
    {
      "supplementRequestId": 17487654321000999,
      "requestNo": "BQKD20260530180000999002",
      "talentCode": "30028",
      "talentName": "潘跃文",
      "userId": 3037,
      "deptCode": "10803",
      "deptName": "技术研发部",
      "posName": "高级工程师",
      "attendanceDateTime": "2026-05-30 14:00",
      "reasonCode": "002",
      "reasonName": "外出公干",
      "supplementType": "REPAIR_CARD",
      "supplementTypeName": "补卡",
      "status": "未提交",
      "sourceEntry": "SELF",
      "applyTime": "2026-05-30 17:00",
      "submitTime": null,
      "approveTime": null,
      "approvalInstanceId": null,
      "approvalStatus": "未提交",
      "currentApproverNames": null,
      "canApprove": false,
      "canReject": false,
      "canDirectPass": true,
      "canReverseApprove": false,
      "canAbandon": false
    }
  ],
  "total": 2,
  "currPage": 1,
  "message": "操作成功",
  "messageType": 200
}
```

---

## 九、补签卡提交生效

`POST /api/supplement/request/admin/direct-pass`

### 权限

`@ScopeAuthRequired`，仅系统管理员和考勤员可操作。

### 请求参数

表单传参，不使用 `@RequestBody`。

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| supplementRequestId | Long | 否 | 单条补签单业务 ID |
| supplementRequestIds | String | 否 | 批量补签单业务 ID，逗号分隔，优先使用 |
| remark | String | 否 | 备注，不传默认 `后台提交生效` |

### 返回

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| success | Boolean | 是否成功 |
| successCount | Integer | 成功条数 |
| supplementRequestIds | List\<Long\> | 本次操作的补签单 ID 列表 |
| supplementRequestId | Long | 第一条补签单 ID（兼容） |
| requestNo | String | 第一条补签单号 |
| approvalInstanceId | Long | 第一条审批实例 ID |
| approvalStatus | String | 审批状态（固定 `已通过`） |
| requestStatus | String | 单据状态（固定 `已通过`） |
| submitActionId | Long | 提交动作日志 ID |
| approveActionId | Long | 同意动作日志 ID |

### 业务规则

- 仅系统管理员和考勤员可操作（`isOperatorWritable`）
- 只允许 `未提交`/`已退回` 的补签单提交生效
- 操作前校验部门权限：管理员不限制，考勤员需在 `deptScopes` 范围内
- 调用 `ApprovalWorkflowDirectPassService` 创建已通过审批实例，不生成待办任务
- 审批直通服务会：创建审批实例（status=`已通过`，currentNodeCode=`finish`）、写入提交+同意动作日志、创建业务绑定、回写补签单状态为 `已通过`
- `supplementRequestIds` 优先，`supplementRequestId` 兜底，两者同时传入只处理批量
- 支持批量，最多 100 条，去重，任意一条校验失败则整体事务回滚
- 操作人固定取当前登录人（`UserAuthContext`），不接受前端传入
- 提交生效后自动标记日历快照脏数据：根据补签明细中的员工和日期登记 `attendance_calendar_snapshot_refresh_event`（event_type=`supplement_direct_pass`）
- 标记脏数据在同一事务内，标记失败则整体回滚

### 返回示例

**单条提交生效**

```json
{
  "success": true,
  "data": {
    "success": true,
    "successCount": 1,
    "supplementRequestIds": [17487654321000123],
    "supplementRequestId": 17487654321000123,
    "requestNo": "BQKD20260601093000123001",
    "approvalInstanceId": 17487654321000456,
    "approvalStatus": "已通过",
    "requestStatus": "已通过",
    "submitActionId": 17487654321000500,
    "approveActionId": 17487654321000600
  },
  "total": null,
  "currPage": null,
  "message": "操作成功",
  "messageType": 200
}
```

**批量提交生效**

```json
{
  "success": true,
  "data": {
    "success": true,
    "successCount": 2,
    "supplementRequestIds": [17487654321000123, 17487654321000999],
    "supplementRequestId": 17487654321000123,
    "requestNo": "BQKD20260601093000123001",
    "approvalInstanceId": 17487654321000456,
    "approvalStatus": "已通过",
    "requestStatus": "已通过",
    "submitActionId": 17487654321000500,
    "approveActionId": 17487654321000600
  },
  "total": null,
  "currPage": null,
  "message": "操作成功",
  "messageType": 200
}
```

---

## 十、补签卡后台撤回

`POST /api/supplement/request/admin/abandon`

### 权限

`@ScopeAuthRequired`，仅系统管理员和考勤员可操作。

### 请求参数

表单传参，不使用 `@RequestBody`。

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| supplementRequestId | Long | 否 | 单条补签单业务 ID |
| supplementRequestIds | String | 否 | 批量补签单业务 ID，逗号分隔，优先使用 |
| remark | String | 否 | 撤回原因/备注，不传默认 `后台撤回审批` |

### 返回

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| success | Boolean | 是否成功 |
| successCount | Integer | 成功条数 |
| supplementRequestIds | List\<Long\> | 本次操作的补签单 ID 列表 |
| supplementRequestId | Long | 第一条补签单 ID（兼容） |
| requestNo | String | 第一条补签单号 |
| approvalInstanceId | Long | 第一条审批实例 ID |
| beforeStatus | String | 撤回前状态（`审批中`） |
| afterStatus | String | 撤回后状态（`未提交`） |
| abandonActionId | Long | 废弃动作日志 ID |

### 业务规则

- 仅系统管理员和考勤员可操作（`isOperatorWritable`）
- 只允许 `审批中` 的补签单撤回
- 操作前校验部门权限：管理员不限制，考勤员需在 `deptScopes` 范围内
- 补签单必须存在审批实例（`approvalInstanceId` 非空）
- 审批实例必须无人处理过（无"同意"/"退回"动作日志）
- 撤回后：
  - 补签单状态：`审批中` → `未提交`
  - 审批实例状态：`审批中` → `已撤回`（currentNodeCode=`finish`）
  - 当前审批绑定：`is_current` 置为 0，`rel_status` 置为 `失效`
  - 待处理审批任务：关闭（taskStatus=`已撤回`）
  - 待审批人：关闭（status=`已跳过`）
  - 写入动作日志：`废弃 / 未提交`
- `supplementRequestIds` 优先，`supplementRequestId` 兜底，两者同时传入只处理批量
- 支持批量，最多 100 条，去重，任意一条校验失败则整体事务回滚
- 操作人固定取当前登录人（`UserAuthContext`），不接受前端传入
- 不触发考勤快照刷新、不改打卡记录、不触发计算（审批中未生效，无需刷新）

### 返回示例

**单条撤回**

```json
{
  "success": true,
  "data": {
    "success": true,
    "successCount": 1,
    "supplementRequestIds": [17487654321000123],
    "supplementRequestId": 17487654321000123,
    "requestNo": "BQKD20260601093000123001",
    "approvalInstanceId": 17487654321000456,
    "beforeStatus": "审批中",
    "afterStatus": "未提交",
    "abandonActionId": 17487654321000500
  },
  "total": null,
  "currPage": null,
  "message": "操作成功",
  "messageType": 200
}
```

**批量撤回**

```json
{
  "success": true,
  "data": {
    "success": true,
    "successCount": 2,
    "supplementRequestIds": [17487654321000123, 17487654321000999],
    "supplementRequestId": 17487654321000123,
    "requestNo": "BQKD20260601093000123001",
    "approvalInstanceId": 17487654321000456,
    "beforeStatus": "审批中",
    "afterStatus": "未提交",
    "abandonActionId": 17487654321000500
  },
  "total": null,
  "currPage": null,
  "message": "操作成功",
  "messageType": 200
}
```

---

## 十一、补签卡反审批

`POST /api/supplement/request/admin/reverse-approve`

### 权限

`@ScopeAuthRequired`，仅系统管理员和考勤员可操作。

### 请求参数

表单传参，不使用 `@RequestBody`。

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| supplementRequestId | Long | 否 | 单条补签单业务 ID |
| supplementRequestIds | String | 否 | 批量补签单业务 ID，逗号分隔，优先使用 |
| remark | String | 否 | 反审批原因/备注，不传默认 `后台反审批` |

### 返回

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| success | Boolean | 是否成功 |
| successCount | Integer | 成功条数 |
| supplementRequestIds | List\<Long\> | 本次操作的补签单 ID 列表 |
| supplementRequestId | Long | 第一条补签单 ID（兼容） |
| requestNo | String | 第一条补签单号 |
| beforeStatus | String | 反审批前状态（`已通过`） |
| afterStatus | String | 反审批后状态（`未提交`） |
| reverseActionId | Long | 反审批动作日志 ID |

### 业务规则

- 仅系统管理员和考勤员可操作（`isOperatorWritable`）
- 只允许 `已通过` 的补签单反审批
- 操作前校验部门权限：管理员不限制，考勤员需在 `deptScopes` 范围内
- 补签单必须存在有效审批绑定（`approval_biz_rel.is_current=1`）和审批实例
- 反审批后：
  - 补签单状态：`已通过` → `未提交`，同时清空 `submit_time`、`approve_time`
  - 审批实例状态：`已通过` → `已撤回`（currentNodeCode=`finish`）
  - 当前审批绑定：`is_current` 置为 0
  - 写入动作日志：`反审批 / 未提交`
- `supplementRequestIds` 优先，`supplementRequestId` 兜底，两者同时传入只处理批量
- 支持批量，最多 100 条，去重，任意一条校验失败则整体事务回滚
- 操作人固定取当前登录人（`UserAuthContext`），不接受前端传入
- 反审批后自动标记日历快照脏数据：根据补签明细中的员工和日期登记 `attendance_calendar_snapshot_refresh_event`（event_type=`supplement_reverse_approve`）
- 标记脏数据在同一事务内，标记失败则整体回滚
- 第一版不删除/失效补签打卡记录

### 返回示例

**单条反审批**

```json
{
  "success": true,
  "data": {
    "success": true,
    "successCount": 1,
    "supplementRequestIds": [17487654321000123],
    "supplementRequestId": 17487654321000123,
    "requestNo": "BQKD20260601093000123001",
    "beforeStatus": "已通过",
    "afterStatus": "未提交",
    "reverseActionId": 17487654321000500
  },
  "total": null,
  "currPage": null,
  "message": "操作成功",
  "messageType": 200
}
```

**批量反审批**

```json
{
  "success": true,
  "data": {
    "success": true,
    "successCount": 2,
    "supplementRequestIds": [17487654321000123, 17487654321000999],
    "supplementRequestId": 17487654321000123,
    "requestNo": "BQKD20260601093000123001",
    "beforeStatus": "已通过",
    "afterStatus": "未提交",
    "reverseActionId": 17487654321000500
  },
  "total": null,
  "currPage": null,
  "message": "操作成功",
  "messageType": 200
}
```

---

## 十二、补签卡导出

### 12.1 基本信息

| 项目 | 说明 |
| ---- | ---- |
| 路径 | `POST /api/supplement/request/admin/export` |
| 权限 | 需登录，需后台权限（`@ScopeAuthRequired`） |
| 说明 | 支持两种导出模式，返回文件路径，不返回文件流 |

### 12.2 请求参数

表单传参（`application/x-www-form-urlencoded`），不使用 `@RequestBody`。

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| exportMode | String | 是 | 导出模式：`ALL`（按查询条件导出）、`SELECTED`（按选中 ID 导出） |
| supplementRequestIds | String | SELECTED 时必填 | 选中的补签单 ID，逗号分隔，最多 100 条 |
| deptCode | String | 否 | 部门编码，传父部门会展开子部门 |
| talentCode | String | 否 | 员工编码，精确匹配 |
| talentName | String | 否 | 员工姓名，模糊匹配 |
| startDate | String | 否 | 补签开始日期，格式 yyyy-MM-dd |
| endDate | String | 否 | 补签结束日期，格式 yyyy-MM-dd |
| status | String | 否 | 单据状态 |
| reasonCode | String | 否 | 补签原因编码 |
| requestNo | String | 否 | 补签单号，模糊匹配 |

### 12.3 导出模式说明

| 模式 | 说明 |
| ---- | ---- |
| `ALL` | 按请求中的筛选条件（部门、员工、日期、状态等）导出当前用户权限范围内全部数据，最多 10000 条 |
| `SELECTED` | 按选中的补签单 ID 导出，但仍套用权限过滤（不会绕过权限），最多 100 条 |

### 12.4 返回参数

```json
{
  "success": true,
  "data": {
    "fileName": "补签卡_1780379999000.xlsx",
    "filePath": "/template/ip_attendance/supplement_request/2026-06-02/1780379999000/补签卡_1780379999000.xlsx"
  },
  "total": null,
  "currPage": null,
  "message": "操作成功",
  "messageType": 200
}
```

### 12.5 错误码

| 场景 | 错误信息 |
| ---- | -------- |
| exportMode 缺失或非法 | `exportMode 只能为 ALL 或 SELECTED` |
| SELECTED 未传 supplementRequestIds | `SELECTED 模式下 supplementRequestIds 必填` |
| supplementRequestIds 格式错误 | `supplementRequestIds 格式错误` |
| 选中超过 100 条 | `选中数量超过 100 条限制` |
| 无后台权限 | `无权导出` |
| 无数据 | `无导出数据` |
| ALL 模式超过 10000 条 | `导出数据量超过 10000 条限制，请缩小筛选范围` |

---

## 十三、缺卡检查分页

### 13.1 基本信息

| 项目 | 说明 |
| ---- | ---- |
| 路径 | `POST /api/supplement/request/admin/missing/page` |
| 权限 | 需登录，需后台权限（`@ScopeAuthRequired`） |
| 说明 | 查询已计算的考勤结果中的缺卡点，排除已被请假/出差/加班/补签覆盖的记录 |

### 13.2 请求参数

表单传参（`application/x-www-form-urlencoded`），不使用 `@RequestBody`。

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| pageNo | Integer | 否 | 页码，默认 1 |

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| pageNo | Integer | 否 | 页码，默认 1 |
| pageSize | Integer | 否 | 每页条数，默认 20，最大 200 |
| deptCode | String | 否 | 部门编码，传父部门会展开子部门 |
| talentCode | String | 否 | 员工编码，精确匹配 |
| talentName | String | 否 | 员工姓名，模糊匹配 |
| startDate | String | 建议必填 | 开始日期，格式 yyyy-MM-dd |
| endDate | String | 建议必填 | 结束日期，格式 yyyy-MM-dd |
| missingType | String | 否 | 缺卡类型：`on_duty`（缺上班卡）/ `off_duty`（缺下班卡）/ `all`，默认 `all` |
| periodCode | String | 否 | 考勤周期编码，不传则按 startDate/endDate 查 |

### 13.3 返回参数

```json
{
  "success": true,
  "data": [
    {
      "missingKey": "10195_2026-06-01_on_duty",
      "talentCode": "10195",
      "talentName": "门雪",
      "userId": 3037,
      "deptCode": "13401",
      "deptName": "技术研发部",
      "positionName": "高级工程师",
      "attendanceDate": "2026-06-01",
      "periodCode": "202606",
      "missingType": "on_duty",
      "missingTypeName": "缺上班卡",
      "shiftCode": "S001",
      "shiftName": "标准班",
      "workTimeRange": "09:00-18:00",
      "firstOnDutyTime": null,
      "firstOffDutyTime": "2026-06-01 18:05:00",
      "missingOnDutyPunch": 1,
      "missingOffDutyPunch": 0,
      "leaveDays": 0,
      "tripDays": 0,
      "overtimeHours": 0,
      "supplementCount": 0,
      "exceptionTags": "缺上班卡",
      "calcTime": "2026-06-02 01:30:00"
    }
  ],
  "total": 1,
  "currPage": 1,
  "message": "操作成功",
  "messageType": 200
}
```

### 13.4 数据来源

- 查询 `attendance_result_day_detail`（已计算的考勤日结果明细）
- 关联 `attendance_calc_cycle_state`（只取 `status='已计算'` 的周期）
- 排除条件：`leave_days=0 AND trip_days=0 AND overtime_hours=0 AND supplement_count=0`
- 同一天上下班都缺时返回两条记录

### 13.5 错误码

| 场景 | 错误信息 |
| ---- | -------- |
| missingType 非法 | `missingType 只能为 on_duty、off_duty 或 all` |
| 无后台权限 | 返回空列表 |

---

## 十四、缺卡检查批量生成补签卡

### 14.1 基本信息

| 项目 | 说明 |
| ---- | ---- |
| 路径 | `POST /api/supplement/request/admin/missing/generate` |
| 权限 | 需登录，需后台写权限（`@ScopeAuthRequired`，仅系统管理员和考勤员） |
| 说明 | 从缺卡检查结果中勾选缺卡点，批量生成补签草稿。一条缺卡点生成一张补签单。 |

### 14.2 请求参数

表单传参（`application/x-www-form-urlencoded`），不使用 `@RequestBody`。

| 字段 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| items | List\<Item\> | 是 | 勾选的缺卡点，最多 100 条 |
| items[].missingKey | String | 否 | 前端展示 key，后端原样返回 |
| items[].talentCode | String | 是 | 员工编码 |
| items[].attendanceDate | String | 是 | 缺卡日期，格式 yyyy-MM-dd |
| items[].missingType | String | 是 | 缺卡类型：`on_duty` / `off_duty` |
| items[].periodCode | String | 是 | 考勤周期编码 |
| reasonCode | String | 是 | 补签原因编码 |
| remark | String | 否 | 备注 |

### 14.3 返回参数

```json
{
  "success": true,
  "data": {
    "success": true,
    "successCount": 1,
    "supplementRequestIds": [17487654321000123],
    "requestNos": ["BQKD20260601093000123001"],
    "items": [
      {
        "missingKey": "30028_2026-06-01_on_duty",
        "talentCode": "30028",
        "talentName": "潘跃文",
        "attendanceDate": "2026-06-01",
        "missingType": "on_duty",
        "missingTypeName": "缺上班卡",
        "supplementDatetime": "2026-06-01 09:00",
        "supplementRequestId": 17487654321000123,
        "requestNo": "BQKD20260601093000123001",
        "status": "未提交"
      }
    ]
  },
  "total": null,
  "currPage": null,
  "message": "操作成功",
  "messageType": 200
}
```

### 14.4 业务规则

1. **一条缺卡点生成一张补签单**，不做员工合并，不做多明细合并。
2. **后端二次校验**：回查 `attendance_result_day_detail` + `attendance_calc_cycle_state`，验证 `missing_*_punch > 0`，不信任前端缺卡状态。
3. **补签时间后端解析**：根据员工排班确定应上班/应下班时间，支持多段班次（缺上班卡取第一个需打卡段，缺下班卡取最后一个需打卡段），支持跨天（`refDate=次日`）。
4. **默认只生成未提交补签单**，不提交审批，不直接生效。
5. **单次最多 100 条**，超过拒绝。
6. **任一失败整体回滚**（`@Transactional(rollbackFor = Exception.class)`）。
7. **权限控制**：仅系统管理员和考勤员（`isOperatorWritable`），部门范围校验（`resolveReadableDeptCodes`）。
8. **重复校验**：同一 `talentCode + attendanceDatetime` 不能与有效补签单冲突（状态为未提交/审批中/已通过/已退回）。
9. 生成的补签单：`source_entry=admin_missing`，明细 `supplement_type=REPAIR_CARD`，`source_type=admin_missing`，`effect_status=未生效`。

### 14.5 错误码

| 场景 | 错误信息 |
| ---- | -------- |
| 超过 100 条 | `单次最多生成 100 条缺卡补签` |
| 缺卡点不存在 | `缺卡点不存在、已不缺卡或无权限` |
| 缺卡点已不缺卡 | `缺卡点已不缺卡` |
| 无排班 | `缺卡日期无有效排班` |
| 无考勤档案 | `员工未建立有效考勤档案` |
| 重复补签 | `员工 xxx 在 yyyy-MM-dd HH:mm 已存在补签单` |
| 原因无效 | `补签原因不存在或已停用` |
| 无权限 | `无权生成补签单` |
| 重复缺卡点 | `存在重复缺卡点` |
