# 审批中心

## 接口列表

| 序号 | 接口名 | 路径 | 是否完成 |
| ---- | ------ | ---- | -------- |
| 1 | 审批中心列表分页查询 | `POST /api/approval/center/page` | 未完成 |
| 2 | 审批任务详情查询 | `POST /api/approval/center/detail` | 未完成 |
| 3 | 审批通过 | `POST /api/approval/center/approve` | 未完成 |
| 4 | 审批退回 | `POST /api/approval/center/reject` | 未完成 |
| 5 | 审批任务转签 | `POST /api/approval/center/transfer` | 未完成 |
| 6 | 调整审核人 | `POST /api/approval/center/assignee/adjust` | 未完成 |
| 7 | 调整审核人记录查询 | `POST /api/approval/center/assignee/adjust-record` | 未完成 |

## 说明

审批中心不提供新建审批接口。审批任务必须由请假、加班、补签、出差等业务单据提交后在后端内部生成。

提交生效不是审批中心独立接口。提交生效属于业务单据提交模式，由单据提交接口传入提交方式后，后端写入审批中心并直接置为审批通过。
