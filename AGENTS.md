# Codex Project Instructions

## IPS V2 Frontend 技能包执行规则

每次在本项目中添加功能或修改功能时，必须先执行 `ips-v2-frontend` 技能包，读取并遵循：

```text
C:\Users\dell\.codex\skills\ips-v2-frontend\SKILL.md
```

尤其是涉及 `views`、`components`、`router/modules`、`api`、`state`、`permission`、`menu` 等前端功能文件时，需要按该技能包要求对齐现有目录结构、页面风格、公共组件用法、路由权限接入方式和自测交付规则。

## 代码变更后的构建与提交规则

为减少等待时间，构建验证与自动提交默认暂停，除非用户在当前任务中明确输入开启提示词。

### 构建验证开关

- 默认不执行 `npm run build`。
- 用户输入以下任一提示词时，当前任务开启构建验证：
  - `开启构建`
  - `执行构建`
  - `需要构建`
  - `build on`
- 用户输入以下任一提示词时，当前任务关闭构建验证：
  - `关闭构建`
  - `跳过构建`
  - `不执行构建`
  - `build off`
- 即使未执行构建，也应根据改动范围尽量执行更轻量的验证，例如目标文件 lint、类型检查、语法检查或说明未验证原因。

### 自动提交开关

- 默认不执行 `git add`、`git commit`。
- 用户输入以下任一提示词时，当前任务开启自动提交：
  - `开启提交`
  - `自动提交`
  - `需要提交`
  - `commit on`
- 用户输入以下任一提示词时，当前任务关闭自动提交：
  - `关闭提交`
  - `跳过提交`
  - `不提交`
  - `commit off`
- 未开启自动提交时，最终回复只说明变更文件与验证情况，不执行提交。
- 开启自动提交时，提交前必须查看 `git status --short`，确认本次将提交的内容；如果有代码或项目文件变更，执行：

```bash
git add .
git commit -m "codex：本次修改内容描述"
```

- 如果没有变更，不执行 `git add` 或 `git commit`。
- 如果构建、测试或提交失败，需要先说明原因，不要强行提交。

## 回退上一版本规则

当 Codex 执行代码变更后，如果用户明确说明本次变更有问题，并要求返回上一个版本或回滚上一提交时，执行：

```bash
git reset --hard HEAD~1
```

执行前需要提醒该命令会丢弃最近一次提交及工作区未提交变更；仅在用户明确要求回退时使用。
