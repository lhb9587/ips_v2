# Codex Project Instructions

## 代码变更后的提交规则

每次完成涉及代码变更的任务后，在最终回复前按以下流程处理：

1. 先执行必要的构建或测试验证；如果只是文档或规则文件变更，可以说明无需构建。
2. 查看 `git status --short`，确认本次将提交的内容。
3. 如果有代码或项目文件变更，执行：

```bash
git add .
git commit -m "codex：本次修改内容描述"
```

4. 如果没有变更，不执行 `git add` 或 `git commit`。
5. 如果构建、测试或提交失败，需要先说明原因，不要强行提交。

## 回退上一版本规则

当 Codex 执行代码变更后，如果用户明确说明本次变更有问题，并要求返回上一个版本或回滚上一提交时，执行：

```bash
git reset --hard HEAD~1
```

执行前需要提醒该命令会丢弃最近一次提交及工作区未提交变更；仅在用户明确要求回退时使用。
