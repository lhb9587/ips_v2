# 考勤总需求文档

## 0. 需求变更记录

| 日期 | 变更编号 | 变更类型 | 变更目标 | 变更摘要 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 2026-05-15 | CR-2026-05-15-02 | UPDATE | 3.2.2.1 考勤档案 | 完善纠正档案信息和变更档案信息规则，明确生效日期控制默认班次等字段 | 已合并 |
| 2026-05-15 | CR-2026-05-15-01 | UPDATE | 3.2.2.1 考勤档案 | 完善考勤档案禁用逻辑，禁用后删除当日及以后排班并排除后续自动排班 | 已合并 |
| 2026-05-13 | CR-2026-05-13-01 | UPDATE | 3.2.2.8 排班管理-排班向导 | 初版排班向导移除两类暂不支持的复杂排班交互，保留自动排班和轮班规则生成排班结果能力 | 已合并 |

## 1. 权限总览

### 1.1 角色任务权限汇总表

| 角色 | 主要任务权限 | 备注 |
| --- | --- | --- |
| 系统管理员 | 后台全局查看 | 后台功能已集成至后端，系统管理员仅保留查看能力 |
| 考勤员 | 假期管理、考勤管理、排班管理、报表查看、后台配置维护 | 可查看自己负责部门下人员信息，并执行后台操作、反审批、废弃、审批中心详情页调整审核人等处理 |
| 部门负责人 | 审批中心、出勤记录、业务结果查看 | 可查看自己管理权限范围内人员信息，并在审批中心处理相关申请 |
| 普通员工 | 自助功能 | 明确拥有员工自助平台所有功能 |

### 1.2 编写约定

- 本文正文按功能域编排，顺序为：员工自助 -> 后台功能 -> 实时提醒。
- 角色边界以本章权限表为准；进入正式 PRD 的功能章节可按需补充角色范围表，但不再使用【角色】前缀式写法。
- 涉及审批角色时，本文统一使用`部门负责人`称谓；不再混用“组长”“审核人”等同义表述。
- 单据回退后的“保存状态”在本文统一落为`未提交状态`，表示单据退回呈批人后可继续编辑、重新提交。
- 员工自助模块已按 PRD 结构细化，后台功能和实时提醒模块可继续沿用同一模板逐节补充页面、字段、操作、规则、提醒和数据关系。

---

## 2. 员工自助

### 2.2 我的考勤

#### 2.2.1 考勤异常日历

##### 2.2.1.1 功能概述
用于以日历看板方式展示员工月度考勤信息，帮助员工快速查看月度统计、每日考勤状态和单日考勤详情，并提供相关业务申请入口。

##### 2.2.1.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 否 | 否 | 否 | 否 | 不作为员工自助入口角色定义 |
| 考勤员 | 是 | 是 | 否 | 否 | 作为员工身份进入自助端时查看本人考勤 |
| 部门负责人 | 是 | 是 | 否 | 否 | 作为员工身份进入自助端时查看本人考勤 |
| 普通员工 | 是 | 是 | 否 | 否 | 查看本人考勤看板 |

##### 2.2.1.3 功能范围
1. 展示顶部业务申请按钮。
2. 展示左侧月度考勤统计面板。
3. 展示中间考勤日历。
4. 展示右侧单日考勤详情面板。
5. 支持月份切换和日期选择。

##### 2.2.1.4 页面模块说明
1. 顶部操作栏：用于展示业务申请入口。
2. 左侧统计面板：用于展示当月考勤汇总数据。
3. 中间考勤日历：用于按月展示每日考勤状态。
4. 右侧单日详情面板：用于展示选中日期的考勤详情。

##### 2.2.1.5 展示字段定义
1. 顶部操作栏：我要请假、我要出差、我要加班、我要调班。
2. **左侧统计面板：加班小时数、请假天数、出差天数、补签次数、迟到次数、早退次数、旷工次数、显示排班信息。
3. **中间考勤日历：当前展示月份、日期、每日考勤状态标记、跨月日期展示，每日考勤状态标记包括上面“左侧统计面板”的内容。**
4. 右侧单日详情面板：选中日期、考勤异常信息、实际工作时间、我要补签入口（考勤异常情况）、查看打卡记录入口、补卡或请假信息（如果有补卡或请假记录）。

##### 2.2.1.6 交互逻辑
1. 用户进入“我的考勤”页面。
2. 系统默认加载当前月份数据，并默认选中当日。
3. 用户切换月份后，系统刷新统计面板、考勤日历和单日详情。
4. 用户点击日历中的某一天后，系统展示该日详情。
5. 用户点击顶部业务按钮后，系统跳转至对应功能入口。

##### 2.2.1.7 数据规则
1. 页面默认展示当前月份数据，并默认选中当日。
2. 切换月份后，统计面板、日历和详情需同步按新月份刷新。
3. 统计面板数据来源于**考勤打卡记录**和已审批单据。
4. 日历数据按月展示每日**考勤状态**，跨月日期可展示但不渲染考勤标记。
5. 单日详情数据基于当日打卡记录和**异常判断结果**生成。
6. 无统计数据时，统计项仍展示为 0。

##### 2.2.1.7.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 我的考勤-日历与异常 | 考勤周期 | `3.2.3.2.1 考勤数据设置` |
| 我的考勤-日历与异常 | 工作日历 | `3.2.3.2.3 业务字段配置映射` |
| 我的考勤-日历与异常 | 法定假日 | `3.2.3.2.1 考勤数据设置` |
| 我的考勤-日历与异常 | 异常判断规则 | `3.2.3.1.2 取卡规则`、`3.2.3.1.4 考勤制度` |
| 我的考勤-日历与异常 | 审批状态联动 | `3.2.3.3 审批工作流` |

##### 2.2.1.8 业务规则
1. 页面默认展示当前月份数据。
2. 点击日期后应展示对应日期详情。
3. 右侧面板支持关闭。
4. 顶部业务按钮作为申请入口，不在本需求内展开审批逻辑。

##### 2.2.1.9 验收标准
1. 页面加载后可正确展示当前月份考勤统计与日历状态。
2. 点击日历日期后可展示对应日期详情。
3. 切换月份后，页面数据同步更新。
4. 顶部业务按钮可正常跳转。

#### 2.2.2 打卡记录

##### 2.2.2.1 功能概述
用于按日期范围查询员工每日上下班打卡记录、打卡来源和补卡信息，帮助员工核对出勤情况，并提供补卡、请假、出差等关联操作入口。

##### 2.2.2.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 否 | 否 | 否 | 否 | 不作为员工自助入口角色定义 |
| 考勤员 | 是 | 否 | 是 | 否 | 作为员工身份进入自助端时可查看本人记录并发起关联操作 |
| 部门负责人 | 是 | 否 | 是 | 否 | 作为员工身份进入自助端时可查看本人记录并发起关联操作 |
| 普通员工 | 是 | 否 | 是 | 否 | 可查看本人记录并发起关联操作 |

##### 2.2.2.3 功能范围
1. 展示打卡记录列表。
2. 支持开始日期和结束日期筛选。
3. 默认查询最近 30 天打卡记录。
4. 提供“我要补卡”“我要请假”“我要出差”操作入口。

##### 2.2.2.4 页面模块说明
1. 筛选区：用于输入开始日期和结束日期。
2. 列表区：用于展示打卡记录结果。
3. 操作区：用于发起补卡、请假、出差等关联操作。

##### 2.2.2.5 列表字段定义

| 字段名称 | 说明 |
| --- | --- |
| 序号 | 打卡记录顺序编号 |
| 日期 | 打卡所属日期 |
| 星期 | 对应星期信息 |
| 打卡时间 | 单列展示上班时间和下班时间 |
| 打卡次数 | 当日打卡次数 |
| 补签卡时间 | 补签相关时间信息 |
| 操作 | 提供我要补卡、我要请假、我要出差入口 |

##### 2.2.2.6 展示字段定义
1. 筛选区：开始日期、结束日期。
2. 操作区：我要补卡、我要请假、我要出差。

##### 2.2.2.7 交互逻辑
1. 用户进入“打卡记录”页面。
2. 系统自动带入默认日期区间并触发查询。
3. 用户可调整开始日期和结束日期后重新查询。
4. 系统返回符合条件的打卡记录列表。
5. 用户可点击操作列按钮进入对应关联功能。

##### 2.2.2.8 数据规则
1. 默认查询区间为今天往前 30 天至今天。
2. 用户修改日期区间后，列表应按新条件刷新。
3. 同一日期存在多条打卡记录时，按**取卡规则**计算有效**上班卡**和**下班卡**。
4. 无记录日期显示缺卡。
5. **跨天下班时间**按“下班时间(+1)”格式展示。
6. 日期筛选仅影响打卡记录列表查询结果。

##### 2.2.2.8.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 打卡记录 | 有效上班卡/下班卡判定 | `3.2.3.1.2 取卡规则` |
| 打卡记录 | 跨天显示规则 | `3.2.3.1.3 班次设置` |
| 打卡记录 | 打卡设备来源 | `3.2.3.2.3 业务字段配置映射` |

##### 2.2.2.9 业务规则
1. 列表应展示本人打卡记录。
2. 操作列三个按钮应在每行始终显示。
3. 关联操作入口仅负责跳转，不在本需求内展开申请逻辑。

##### 2.2.2.10 验收标准
1. 页面打开后默认展示最近 30 天打卡记录。
2. 用户调整日期区间后，查询结果正确更新。
3. 列表可展示打卡时间、打卡次数和补签卡时间。
4. 跨天下班场景按“(+1)”显示。
5. 每行操作列均可展示“我要补卡”“我要请假”“我要出差”按钮。

### 2.3 我的假勤

#### 2.3.1 功能概述
用于发起请假、出差、加班、补签等申请。各子项按业务类型分别建设独立申请能力，申请人均为当前登录员工本人，并按各自状态流转规则进入后续审批或外部系统流程。

#### 2.3.2 子项清单
1. 我要请假
2. 我要出差
3. 我要加班
4. 我要补签

#### 2.3.3 [我的假勤] 我要请假

##### 2.3.3.1 功能概述
用于在线发起请假申请、查看各假期类型的剩余/在途额度，并跟踪本人请假单据状态；其中年假、司龄假选项下需区分展示今年剩余和去年剩余假数。除管理后台保留的`导出选中`、`导出全部`、`反审批`、`废弃`等公共处理动作外，请假申请的创建、编辑、删除、额度校验、附件校验、半天时段、产假组成项、详情查看等业务逻辑统一由员工自助端“我要请假”承接。当前已明确的创建/编辑页支持保存、提交、请假列表入口、假期类型卡片选择、请假时间填写、请假长度自动展示、请假说明填写和附件上传；请假列表用于管理本人保存的草稿和已提交请假单据。

##### 2.3.3.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 否 | 否 | 否 | 否 | 不作为员工自助入口角色定义 |
| 考勤员 | 是 | 否 | 是 | 否 | 作为员工身份进入自助端时仅发起和查看本人请假申请 |
| 部门负责人 | 是 | 否 | 是 | 否 | 作为员工身份进入自助端时仅发起和查看本人请假申请 |
| 普通员工 | 是 | 否 | 是 | 否 | 发起和查看本人请假申请 |

##### 2.3.3.3 功能范围
1. 支持请假申请创建与编辑。
2. 支持以卡片方式展示并选择假期类型。
3. 支持展示各假期类型的剩余和在途天数。
4. 支持在年假、司龄假选项下区分展示今年剩余和去年剩余假数。
5. 支持年假、司龄假、事假按统一假期额度扣除顺序占用额度。
6. 支持填写请假开始时间、结束时间和请假说明。
7. 支持自动展示请假长度。
8. 支持上传附件。
9. 支持保存草稿、提交审批，并提供请假列表入口。
10. 支持请假列表管理本人保存的草稿和已提交请假单据。
11. 支持删除未提交状态草稿。
12. 支持查看请假详情、审批状态、审批意见和流程图。

##### 2.3.3.4 页面模块说明
1. 顶部操作栏：用于执行保存、提交和进入请假列表。
2. 温馨提示区：用于展示本月已提交请假申请次数、上一次请假时间和查看更多请假记录入口。
3. 基础信息区：用于展示姓名、员工编码、所属行政组织。
4. 请假信息区：用于展示假期类型卡片并填写请假时间、请假长度和请假说明；年假、司龄假卡片下区分展示今年剩余和去年剩余假数。
5. 附件区：用于上传附件。
6. 单据信息区：用于展示单据编号、申请人、申请日期。
7. 请假单据列表页：用于展示本人保存的草稿和已提交请假单据，并执行新建请假申请、继续编辑草稿、删除未提交草稿、查看单据详情等操作。
8. 请假详情页：用于查看单据信息、审批状态、审批意见和流程图。
9. 交互弹窗：用于提交确认和操作限制提示。

##### 2.3.3.5 列表字段定义

| 字段名称 | 说明 |
| --- | --- |
| 单据编号 | 请假单唯一编号 |
| 姓名 | 申请员工姓名 |
| 申请日期 | 请假申请日期 |
| 假期类型 | 当前请假所属假期类型 |
| 开始时间 | 请假开始时间 |
| 结束时间 | 请假结束时间 |
| 请假长度 | 自动计算的请假时长 |
| 单位 | 请假长度单位 |
| 单据状态 | 当前单据状态 |
| 审批人 | 当前或最近审批人 |

##### 2.3.3.6 展示字段定义
1. 顶部操作栏：保存、提交、请假列表。
2. 温馨提示区：本月已提交请假申请次数、上一次请假时间、查看更多请假记录入口。
3. 基础信息区：姓名、员工编码、所属行政组织。
4. 假期类型区：法定年假、司龄假、事假、病假、其他假期入口；各假期卡片展示剩余/在途天数，其中年假、司龄假卡片下区分展示今年剩余和去年剩余假数。
5. 请假时间区：请假开始时间、开始时段，请假结束时间、结束时段。
6. 请假长度区：请假长度、单位（天）。
7. 请假说明区：请假说明。
8. 附件区：上传附件入口、已上传附件列表。
9. 单据信息区：单据编号、申请人、申请日期。
10. 详情信息区：请假单审批状态、审批意见。

##### 2.3.3.7 交互逻辑
1. 用户进入请假申请页后，系统自动填充姓名、员工编码、所属行政组织、单据编号、申请人、申请日期等基础信息。
2. 系统展示各假期类型卡片及对应剩余/在途天数；年假、司龄假卡片下同时区分展示今年剩余和去年剩余假数，用户点击后选择请假类型。
3. 用户填写请假开始时间和结束时间后，系统自动计算并回填请假长度。
4. 用户填写请假说明并按需上传附件。
5. 当请假类型为`病假`或`孕检假`时，用户保存草稿可暂不上传附件；提交前系统应校验至少已上传 1 份附件。
6. 用户点击“保存”后，系统将单据保存为`未提交`状态草稿。
7. 用户点击“提交”后，系统弹出确认弹窗；确认前若请假类型为`病假`或`孕检假`且未上传附件，系统应阻止提交并提示补充附件；校验通过后单据状态变更为`审批中`并进入审批流程。
8. 用户点击“请假列表”或“查看更多请假记录”后，进入请假列表页。
9. 用户在请假列表中双击某条记录后，进入请假详情页。
10. 请假详情页直接展示该单据的审批状态和审批意见，不再单独进入审批意见页面。
11. 请假列表页默认按申请日期倒序展示本人保存的草稿和已提交请假单据。
12. 用户在请假列表页可对`未提交`状态草稿继续编辑或删除；已提交单据仅支持查看详情。
13. 后台对审批中的请假单执行废弃后，员工侧列表中的对应单据恢复为`未提交`状态，并可继续编辑或重新提交。

##### 2.3.3.8 数据规则
1. **假期余额**展示格式为“剩余[X.XX]，在途[X.XX]”。
2. **在途天数**定义为请假单据提交后、审批完成前提前占用的请假天数，用于表示“未审批完成但已占用”的假期额度。
3. **年假、司龄假剩余**展示时，应在对应假期选项下区分今年剩余和去年剩余假数。
4. **年假、司龄假、事假扣除顺序**为：调休假 > 去年结余法定年假 > 结余司龄假 > 法定年假 > 司龄假。
5. **调休假最小可请时长**为 4 小时，调休时长不足 4 小时时不能申请调休假。
6. **假期类型**以卡片方式展示，点击卡片后作为当前申请的假期类型。
7. 选择请假开始和结束时间后，系统自动计算**请假长度**。
8. 请假列表默认展示本人保存的草稿和已提交请假单据，并按申请日期倒序排列。
9. 已提交后的请假单据继续保留在请假列表中查看和管理。
10. `未提交`状态草稿支持继续编辑和删除；删除后该单据不再出现在请假列表中。
11. 单据提交后，对应**假期类型**的在途天数增加、剩余天数同步扣减；审批完成后的额度处理以 `3.1.3.2 额度规则` 为准。
12. `病假`、`孕检假`在提交请假单时必须至少上传 1 份附件；保存草稿时可暂不上传，其他假期当前不强制附件。
13. 当请假单位为半天时，请假时段按固定口径处理：`上午`按 `09:00-14:00`，`下午`按 `11:45-18:00`。
14. 最小请假申请单位固定为半天；调休假申请达到 `4 小时及以上` 时，按半天计入请假时长。
15. `病假`、`产假`、`工伤假`为无预置额度假期，申请时不校验剩余额度，也不冻结预置额度。
16. `产假`按组成项申请：`基础产假98天`默认存在，`延长生育假30天（第一段）`、`延长生育假30天（第二段）`按勾选参与申请，`难产`增加 `15天`，`多胞胎`按每多 `1` 个婴儿增加 `15天`。
17. 产假申请时，系统应自动汇总本次申请总天数；已申请或已生效的产假组成项不得重复申请，后续仅可对剩余未使用组成项继续发起申请。
18. 后台对审批中的请假单执行废弃后，员工侧对应单据状态恢复为`未提交`，已占用/冻结的额度同步恢复到对应剩余额度。

##### 2.3.3.8.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 我要请假 | 假期类型 | `3.2.3.2.3 业务字段配置映射` |
| 我要请假 | 剩余额度（含今年剩余、去年剩余）、在途额度 | `3.1.3.2 额度规则` |
| 我要请假 | 年假、司龄假、事假扣除顺序 | `3.1.3.2 额度规则` |
| 我要请假 | 调休假最小可请时长 | `3.1.3.2 额度规则` |
| 我要请假 | 无预置额度假期口径 | `3.1.3.2 额度规则` |
| 我要请假 | 半天请假时段与产假组成项规则 | `3.1.3.3 假期制度定义` |
| 我要请假 | 请假长度计算规则 | `3.1.3.3 假期制度定义` |
| 我要请假 | 审批状态、审批意见 | `3.2.3.3 审批工作流` |

##### 2.3.3.9 业务规则
1. 系统应支持请假申请创建与编辑，字段包含假期类型、开始时间、结束时间、请假长度、请假说明、附件。
2. 提交前应校验请假天数不超过对应假期类型剩余天数。
3. 额度不足时应阻止提交并提示，不进入审批流程。
4. 请假长度应由系统根据开始时间和结束时间自动计算展示。
5. 删除操作仅允许作用于`未提交`状态单据。
6. 员工仅可查看和操作本人的请假单据。
7. 请假列表用于管理草稿和已提交单据，不再单独设置统一申请历史查询入口。
8. 已提交单据的详情页应直接展示审批状态和审批意见。
9. 审批意见不再单独定义独立查看页面。
10. 单据处于`审批中`状态时，应提前占用对应假期类型额度，并计入在途天数。
11. 年假、司龄假选项下的剩余假数应区分今年剩余和去年剩余，不作为独立假期类型展示。
12. 员工申请年假、司龄假、事假时，应按调休假、去年结余法定年假、结余司龄假、法定年假、司龄假的顺序依次占用/扣减额度。
13. 员工申请调休假时，若调休时长不足 4 小时，应阻止提交并提示。
14. 员工申请`病假`、`孕检假`时，系统应在提交前校验附件必填；未上传附件时仅允许保存草稿，不允许提交进入审批流程。
15. 半天请假按固定时段定义执行：`上午`按 `09:00-14:00`，`下午`按 `11:45-18:00`。
16. 最小请假申请单位固定为半天；调休假达到 `4 小时及以上` 时按半天处理。
17. `病假`、`产假`、`工伤假`不做事先额度分配，用户可直接发起请假申请；经审批通过后单据按请假结果生效。
18. `产假`应按组成项进行申请与记录，不能仅按总天数粗放处理；后端必须能识别员工已使用到`98天基础段`、两个`30天`延长段及附加项中的哪些部分。
19. 当员工已申请部分产假组成项后，后续再次申请产假时，系统仅允许选择剩余未使用组成项，并重新计算本次申请天数。
20. 后台废弃审批中的请假单后，员工侧对应单据应恢复为`未提交`状态，额度同步恢复，并允许继续编辑或重新提交。

##### 2.3.3.10 验收标准
1. 请假申请页可展示保存、提交、请假列表三个顶部按钮。
2. 页面可展示温馨提示、本月已提交请假申请次数、上一次请假时间和查看更多请假记录入口。
3. 页面可展示姓名、员工编码、所属行政组织、假期类型卡片、请假时间、请假长度、请假说明、附件和单据信息；年假、司龄假卡片下可区分展示今年剩余和去年剩余假数。
4. 用户选择请假开始时间和结束时间后，系统可自动计算请假长度。
5. 提交单据时弹出确认弹窗，确认后状态变更为`审批中`。
6. 请假列表可同时看到草稿和已提交单据。
7. 在请假列表中双击单据后，可进入详情页并直接查看审批状态和审批意见。
8. 请假单提交后、审批完成前，对应假期卡片可展示增加后的在途天数，并体现额度已被提前占用。
9. 年假、司龄假选项下可区分展示今年剩余和去年剩余假数，且不新增独立的去年剩余额度选项。
10. 员工提交年假、司龄假、事假申请后，系统可按调休假、去年结余法定年假、结余司龄假、法定年假、司龄假的顺序占用/扣减额度。
11. 员工申请调休假且调休时长不足 4 小时时，系统可阻止提交并给出提示。
12. 员工申请`病假`、`孕检假`且未上传附件时，系统可阻止提交并给出提示；上传至少 1 份附件后方可提交。
13. 半天请假时，系统可按 `上午 09:00-14:00 / 下午 11:45-18:00` 的固定口径处理请假时段。
14. `病假`、`产假`、`工伤假`申请时，系统可不校验剩余额度并允许直接提交审批。
15. `产假`申请时，系统可按组成项展示并自动汇总申请总天数；已申请或已生效的组成项不可重复选择。
16. `未提交`状态草稿可在请假列表中继续编辑或删除；删除后列表中不再展示该单据。
17. 后台废弃审批中的请假单后，员工侧对应单据可恢复为`未提交`状态，已占用/冻结额度恢复，并可继续处理。

#### 2.3.4 [我的假勤] 我要出差

##### 2.3.4.1 功能概述
当前阶段点击“我要出差”后，系统直接跳转到 IPS 出差申请页面，由外部系统承接出差申请创建和后续流程。

##### 2.3.4.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 否 | 否 | 否 | 否 | 不作为员工自助入口角色定义 |
| 考勤员 | 是 | 否 | 是 | 否 | 作为员工身份进入自助端时可点击跳转 IPS |
| 部门负责人 | 是 | 否 | 是 | 否 | 作为员工身份进入自助端时可点击跳转 IPS |
| 普通员工 | 是 | 否 | 是 | 否 | 可点击跳转 IPS 发起出差申请 |

##### 2.3.4.3 功能范围
1. 提供“我要出差”入口。
2. 点击后跳转到 IPS 出差申请页面。
3. 当前阶段不在本系统内建设本地出差申请页、列表页和详情页。

##### 2.3.4.4 页面模块说明
1. 出差入口：用于触发跳转到 IPS 出差申请页面。

##### 2.3.4.5 展示字段定义
1. 出差入口：我要出差按钮。

##### 2.3.4.6 交互逻辑
1. 用户点击“我要出差”入口。
2. 系统跳转到 IPS 出差申请页面。

##### 2.3.4.7 数据规则
1. 当前阶段本系统不落地出差申请本地业务数据。
2. **出差申请**的表单、状态和审批数据由 IPS 系统承接。

##### 2.3.4.7.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 我要出差 | 出差类型 | `3.2.3.2.3 业务字段配置映射` |
| 我要出差 | 表单与审批数据 | `未确定` |

##### 2.3.4.8 业务规则
1. 当前阶段系统应在点击“我要出差”时直接跳转到 IPS 出差申请页面。
2. 当前阶段不在本系统内提供本地创建、保存、提交和查询能力。

##### 2.3.4.9 验收标准
1. 点击“我要出差”入口后可正常跳转到 IPS 出差申请页面。
2. 本系统内不出现本地出差申请表单页。

#### 2.3.5 [我的假勤] 我要加班

##### 2.3.5.1 功能概述
用于在线发起和管理加班申请，并在加班申请后生成调休假。加班单创建入口统一由员工自助端“我要加班”承接；除加班业务字段不同外，其余交互逻辑与“我要请假”保持一致：支持保存、提交、加班列表入口、本人草稿与已提交单据管理、详情查看以及审批状态跟踪。当前已明确的创建/编辑页支持填写加班日期、开始/结束时间、休息时长、申请加班小时数、备注，并对加班原因、加班类型、补偿方式提供可选选择能力；生成的调休假未使用时，3 个月后失效。

##### 2.3.5.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 否 | 否 | 否 | 否 | 不作为员工自助入口角色定义 |
| 考勤员 | 是 | 否 | 是 | 否 | 作为员工身份进入自助端时仅发起和查看本人加班申请 |
| 部门负责人 | 是 | 否 | 是 | 否 | 作为员工身份进入自助端时仅发起和查看本人加班申请 |
| 普通员工 | 是 | 否 | 是 | 否 | 发起和查看本人加班申请 |

##### 2.3.5.3 功能范围
1. 支持加班申请创建与编辑。
2. 支持填写加班日期、开始时间、结束时间、休息时长、申请加班小时数、备注等字段。
3. 支持对加班原因、加班类型、补偿方式进行可选选择。
4. 支持保存草稿、提交审批，并提供加班列表入口。
5. 支持加班列表管理本人保存的草稿和已提交单据。
6. 支持加班单据详情查看。
7. 支持套打模板选择和 PDF 输出。
8. 支持加班申请后按加班时长与调休时长 1:1 生成调休假，未使用的调休假 3 个月后失效。

##### 2.3.5.4 页面模块说明
1. 顶部操作栏：用于执行保存、提交和进入加班列表。
2. 基础信息区：用于展示单据编号、员工编码、职位、姓名、组织。
3. 加班信息区：用于填写和展示加班业务信息。
4. 加班单据列表页：用于展示本人保存的草稿和已提交单据，并执行创建、提交、删除、套打、废弃等操作。
5. 加班详情页：用于查看单据信息、审批状态、审批意见和流程图。
6. 套打模板弹窗：用于选择套打模板并确认输出。

##### 2.3.5.5 列表字段定义

| 字段名称 | 说明 |
| --- | --- |
| 单据编号 | 加班单唯一编号 |
| 姓名 | 申请员工姓名 |
| 加班日期 | 加班发生日期 |
| 加班开始时间 | 加班开始时间 |
| 加班结束时间 | 加班结束时间 |
| 休息时长（分） | 休息分钟数 |
| 申请加班小时数 | 申请加班时长 |
| 加班原因 | 加班原因 |
| 加班类型 | 加班类型 |
| 单据状态 | 当前单据状态 |
| 审批人 | 当前或最近审批人 |
| 来源 | 数据来源 |

##### 2.3.5.6 展示字段定义
1. 顶部操作栏：保存、提交、加班列表。
2. 基础信息区：单据编号、员工编码、职位、姓名、组织。
3. 加班信息区：加班日期、加班开始时间、加班结束时间、休息时长（分钟）、申请加班小时数、加班原因、加班类型、补偿方式、备注、申请日期、实际加班小时数、实际加班开始时间、实际加班结束时间。
4. 可选字段区：加班原因、加班类型、补偿方式；支持下拉框或弹窗选择。
5. 详情信息区：加班单审批状态、审批意见。
6. 套打模板弹窗：模板编码、模板名称、状态。

##### 2.3.5.7 交互逻辑
1. 用户进入加班申请页后，系统自动填充单据编号、员工编码、职位、姓名、组织等基础信息。
2. 用户填写加班日期、开始时间、结束时间、休息时长、申请加班小时数、备注等信息。
3. 用户在加班原因、加班类型、补偿方式字段处通过下拉框或弹窗选择对应值。
4. 用户点击“保存”后，系统将单据保存为`未提交`状态草稿。
5. 用户点击“提交”后，系统弹出确认弹窗；确认后单据状态变更为`审批中`并进入审批流程。
6. 加班申请后，系统按加班时长与调休时长 1:1 生成对应调休假；生成的调休假未使用时，自生成日起 3 个月后失效。
7. 用户点击“加班列表”后，进入加班列表页。
8. 加班列表页展示本人保存的草稿和已提交单据。
9. 用户在加班列表中双击某条记录后，进入加班详情页。
10. 加班详情页直接展示该单据的审批状态和审批意见，不再单独进入审批意见页面。
11. 用户在列表页点击“套打”后选择模板并确认，系统生成文档并返回 PDF。

##### 2.3.5.8 数据规则
1. **加班原因**、**加班类型**、**补偿方式**均为可选字段，支持通过下拉框或查询弹窗选择。
2. 加班列表默认展示本人保存的草稿和已提交单据，并按申请日期倒序排列。
3. 已提交后的加班单据继续保留在加班列表中查看和管理。
4. 列表页可按申请日期等条件筛选，并支持分页。
5. 列表页应按单据状态控制“提交”“删除”“废弃”“套打”等操作可用性。
6. **申请单状态**使用统一口径：`未提交`、`审批中`、`已通过`、`已驳回`、`已废弃`。
7. **调休假生成规则**为加班时长与调休时长 1:1 转化。
8. **调休假有效期**为自生成日起 3 个月；生成后未使用的调休假在有效期结束后失效。

##### 2.3.5.8.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 我要加班 | 加班原因 | `3.2.3.2.3 业务字段配置映射` |
| 我要加班 | 加班类型 | `3.2.3.2.3 业务字段配置映射` |
| 我要加班 | 补偿方式 | `3.2.3.2.3 业务字段配置映射` |
| 我要加班 | 加班时间有效性/计算规则 | `3.2.3.1.2 取卡规则`、`3.2.3.1.3 班次设置`、`3.2.3.1.4 考勤制度` |
| 我要加班 | 调休假生成、1:1 转化与失效规则 | `3.1.3.2 额度规则` |
| 我要加班 | 审批状态、审批意见 | `3.2.3.3 审批工作流` |

##### 2.3.5.9 业务规则
1. 系统应支持“保存草稿”和“提交审批”两种动作。
2. 加班原因、加班类型、补偿方式应支持选择输入，不要求手工自由录入。
3. 删除操作仅允许作用于`未提交`状态单据。
4. 废弃操作仅允许作用于`审批中`状态单据。
5. 员工仅可查看和操作本人的加班单据。
6. 加班列表用于管理草稿和已提交单据，不再单独设置统一申请历史查询入口。
7. 已提交单据的详情页应直接展示审批状态和审批意见。
8. 审批意见不再单独定义独立查看页面。
9. 套打前应先选择启用状态模板并确认。
10. 套打完成后应返回 PDF 给前端。
11. 加班申请后按加班时长与调休时长 1:1 生成调休假；调休假生成后未使用的，自生成日起 3 个月后失效。

##### 2.3.5.10 验收标准
1. 加班申请页可展示保存、提交、加班列表三个顶部按钮。
2. 页面可展示单据编号、员工编码、职位、姓名、组织、加班日期、开始时间、结束时间、休息时长、申请加班小时数、备注、申请日期、实际加班字段等信息。
3. 加班原因、加班类型、补偿方式可通过下拉框或弹窗选择并正确回填。
4. 加班列表可同时看到草稿和已提交单据。
5. 在加班列表中双击单据后，可进入详情页并直接查看审批状态和审批意见。
6. 点击“套打”后可选择模板并完成输出，前端可收到 PDF。
7. 加班申请后可按加班时长与调休时长 1:1 生成调休假；生成的调休假未使用时，可在生成日起 3 个月后失效。

#### 2.3.6 [我的假勤] 我要补签

##### 2.3.6.1 功能概述
用于员工在线发起和管理补签申请。补签卡创建入口统一由员工自助端“我要补签”承接；整体交互逻辑与“我要请假”“我要加班”保持一致，支持保存、提交、补签卡列表入口、本人草稿与已提交单据管理、详情查看以及审批状态跟踪。当前已明确的创建/编辑页仅支持单条补签卡信息录入，用于描述一个需要补录或说明的异常打卡时间点，并填写补签卡原因和备注；如需补签多个时间点，须分别创建多张补签单。

##### 2.3.6.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 否 | 否 | 否 | 否 | 不作为员工自助入口角色定义 |
| 考勤员 | 是 | 否 | 是 | 否 | 作为员工身份进入自助端时仅发起和查看本人补签申请 |
| 部门负责人 | 是 | 否 | 是 | 否 | 作为员工身份进入自助端时仅发起和查看本人补签申请 |
| 普通员工 | 是 | 否 | 是 | 否 | 发起和查看本人补签申请 |

##### 2.3.6.3 功能范围
1. 支持补签申请创建与编辑。
2. 支持填写考勤日期/时间点、补签卡原因、备注。
3. 支持通过下拉框或弹窗选择补签卡原因。
4. 支持保存草稿、提交审批，并提供补签卡列表入口。
5. 支持补签卡列表管理本人保存的草稿和已提交单据。
6. 支持补签单据详情查看。
7. 支持补签申请与打卡记录异常联动。

##### 2.3.6.4 页面模块说明
1. 顶部操作栏：用于执行保存、提交和进入补签卡列表。
2. 基础信息区：用于展示单据编号、员工编码、姓名、职位、组织。
3. 补签卡信息区：用于填写单条补签卡信息。
4. 补签卡列表页：用于展示本人保存的草稿和已提交单据，并执行创建、提交、删除、废弃等操作。
5. 补签详情页：用于查看单据信息、审批状态、审批意见和流程图。
6. 交互弹窗：用于提交确认和操作限制提示。

##### 2.3.6.5 列表字段定义

| 字段名称 | 说明 |
| --- | --- |
| 单据编号 | 补签单唯一编号 |
| 员工姓名 | 申请员工姓名 |
| 考勤日期 | 补签所属考勤日期 |
| 补签时间点 | 申请补签的时间点 |
| 补签原因 | 补签原因 |
| 单据状态 | 当前单据状态 |
| 审批人 | 当前或最近审批人 |
| 备注 | 补充说明 |

##### 2.3.6.6 展示字段定义
1. 顶部操作栏：保存、提交、补签卡列表。
2. 基础信息区：单据编号、员工编码、姓名、职位、组织。
3. 补签卡信息区：考勤日期/时间点、补签卡原因、备注。
4. 详情信息区：补签单审批状态、审批意见。

##### 2.3.6.7 交互逻辑
1. 用户进入补签申请页后，系统自动填充单据编号、员工编码、姓名、职位、组织等基础信息。
2. 用户在补签卡信息区填写考勤日期/时间点，并选择补签卡原因，按需填写备注。
3. 用户点击“保存”后，系统将单据保存为`未提交`状态草稿。
4. 用户点击“提交”后，系统弹出确认弹窗；确认后单据状态变更为`审批中`并进入审批流程。
5. 用户点击“补签卡列表”后，进入补签卡列表页。
6. 补签卡列表页展示本人保存的草稿和已提交单据。
7. 用户在补签卡列表中双击某条记录后，进入补签详情页。
8. 补签详情页直接展示该单据的审批状态和审批意见，不再单独进入审批意见页面。
9. 若用户从打卡记录异常入口发起补签，系统可带入对应异常日期或异常时间点。

##### 2.3.6.8 数据规则
1. 一个补签单仅包含单条补签卡信息，不支持多条补签卡子项。
2. 补签卡信息用于描述一个需要补录或说明的异常打卡时间点。
3. 页面字段虽命名为“**考勤日期**”，但录入值应支持精确到时间点，用于表示具体需补签的打卡时间。
4. **补签卡原因**支持通过下拉框或弹窗选择。
5. **补签卡原因**来源于后台配置，原因项可配置启用状态及**是否允许提前补签**。
6. 列表页按申请日期倒序展示补签单据。
7. 补签卡列表默认展示本人保存的草稿和已提交单据。
8. 打卡记录中的异常记录可作为补签申请来源，发起补签时可带入对应**异常日期或时间点**。
9. 原始打卡记录保持真实数据，不因补签申请直接被覆盖；审批通过后应影响异常处理结果和考勤计算结果。
10. 单据编号按 `BQKD-YYYYMMDD-XXXX` 规则生成，其中 `XXXX` 为当日自增序号。

##### 2.3.6.8.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 我要补签 | 补签卡原因 | `3.2.3.2.3 业务字段配置映射` |
| 我要补签 | 是否允许提前补签 | `3.2.3.2.1 考勤数据设置` |
| 我要补签 | 异常时间点识别规则 | `3.2.3.1.2 取卡规则`、`3.2.3.1.4 考勤制度` |
| 我要补签 | 审批状态、审批意见 | `3.2.3.3 审批工作流` |

##### 2.3.6.9 业务规则
1. 系统应支持“保存草稿”和“提交审批”两种动作。
2. 一个补签单仅对应单条补签卡信息，不支持在同一单据中录入多条补签卡子项；如需补签多个时间点，须分别提交多张补签单。
3. 补签卡原因应支持选择输入，不要求手工自由录入。
4. 若补签卡原因未配置“允许提前补签”，则仅允许在异常发生后提交补签申请。
5. 删除操作仅允许作用于`未提交`状态单据。
6. 废弃操作仅允许作用于`审批中`状态单据。
7. 员工仅可查看和操作本人的补签单据。
8. 补签卡列表用于管理草稿和已提交单据，不再单独设置统一申请历史查询入口。
9. 已提交单据的详情页应直接展示审批状态和审批意见。
10. 审批意见不再单独定义独立查看页面。
11. 补签审批通过后，应按补签卡原因和相关规则更新异常处理结果，不直接篡改原始打卡数据。

##### 2.3.6.10 验收标准
1. 补签申请页可展示保存、提交、补签卡列表三个顶部按钮。
2. 页面可展示单据编号、员工编码、姓名、职位、组织以及补签卡信息录入区域。
3. 用户在一个补签单中仅可录入单条补签卡信息，页面不提供新增或删除子项能力。
4. 补签卡原因可通过下拉框或弹窗选择并正确回填。
5. 补签卡列表可同时看到草稿和已提交单据。
6. 在补签卡列表中双击单据后，可进入详情页并直接查看审批状态和审批意见。
7. 从打卡异常入口发起补签时，系统可带入对应异常日期或异常时间点。

### 2.4 我的在办

#### 2.4.1 功能概述
用于在员工自助端查看“审批中心-我的在办”中的审批中任务。该页面仅保留查看能力，不承载审批处理逻辑；审批处理统一在 `3.2.2.10 审批中心` 执行。

#### 2.4.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 否 | 否 | 否 | 否 | 不作为员工自助入口角色定义 |
| 考勤员 | 是 | 是 | 否 | 否 | 作为员工身份仅查看本人“我的在办（审批中）” |
| 部门负责人 | 是 | 是 | 否 | 否 | 作为员工身份仅查看本人“我的在办（审批中）” |
| 普通员工 | 是 | 是 | 否 | 否 | 仅查看本人“我的在办（审批中）” |

#### 2.4.3 功能范围
1. 仅展示“我的在办（审批中）”任务记录。
2. 支持按关键字搜索本人在办任务。
3. 支持点击列表项查看任务详情。
4. 支持显示任务基础字段（流程主题、状态、当前处理人、任务到达时间、任务运行时间）。
5. 不提供审批通过、退回等处理操作。

#### 2.4.4 页面模块说明
1. 输入框：搜索输入框，用于关键字查询“我的在办（审批中）”记录。
2. 列表：任务列表，用于展示本人在办任务记录。
3. 详情页：任务详情页，用于展示选中任务的只读详情与流程信息。

#### 2.4.5 列表字段定义

| 字段名称 | 说明 |
| --- | --- |
| 流程主题 | 当前任务或申请主题 |
| 状态 | 当前记录状态 |
| 当前处理人 | 当前节点处理人 |
| 任务到达时间 | 当前任务到达当前处理人的时间 |
| 任务运行时间 | 当前任务已运行时长 |

#### 2.4.6 展示字段定义
1. 任务列表：流程主题、状态、当前处理人、任务到达时间、任务运行时间。
2. 任务详情：任务基础信息、流程处理信息（只读）。

#### 2.4.7 交互逻辑
1. 用户进入“我的在办”页面。
2. 系统加载当前用户“我的在办（审批中）”任务记录列表。
3. 用户输入关键字后，系统按匹配条件过滤任务列表。
4. 用户点击某条记录后，系统进入任务详情页展示只读信息。
5. 页面不展示审批通过、退回等处理按钮。

#### 2.4.8 数据规则
1. 本页面仅展示申请人为当前用户且状态为“处理中”的任务记录。
2. 数据查询和可见范围遵循当前用户数据权限范围。
3. 页面数据口径与 `3.2.2.10 审批中心-我的在办` 保持一致。
4. 本页面不改变任务状态，不写入审批处理结果。

#### 2.4.8.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 我的在办 | 我的在办（审批中）任务记录 | `3.2.2.10 审批中心` |
| 我的在办 | 用户可见范围 | `3.2.3.2.2 数据权限设置` |

#### 2.4.9 业务规则
1. 员工自助“我的在办”仅提供查看能力，不提供审批处理能力。
2. 审批通过、退回等处理动作统一在 `3.2.2.10 审批中心` 执行。
3. 本页面作为审批中心“我的在办（审批中）”的自助查看入口，口径保持一致。

#### 2.4.10 验收标准
1. 页面仅展示“我的在办（审批中）”任务，不再出现“待审批”“全部”分类切换。
2. 列表可展示流程主题、状态、当前处理人、任务到达时间、任务运行时间。
3. 点击列表项后可查看任务详情，且详情为只读。
4. 页面不展示通过、退回等审批处理按钮。
5. 页面数据与 `3.2.2.10 审批中心-我的在办` 口径一致。

---

## 3. 后台功能

### 3.1 假期管理

#### 3.1.1 假期管理入口页

##### 3.1.1.1 功能概述
用于作为“假期管理”模块的统一入口页，集中展示当前文档已确认的假期管理后台功能和系统设置功能，帮助用户快速进入对应业务页面。页面布局参考现有系统“假期管理”导航页样式，采用分组卡片方式展示入口；该页面仅承担功能导航和分组展示作用，不直接承载档案维护、单据处理、额度计算、规则配置等具体业务操作。

##### 3.1.1.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看入口页与系统设置类入口，不执行后台操作 |
| 考勤员 | 是 | 是 | 否 | 否 | 主要使用角色，可进入假期管理相关后台功能 |
| 部门负责人 | 否 | 否 | 否 | 否 | 当前不进入假期管理统一入口页 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台假期管理入口页 |

##### 3.1.1.3 功能范围
1. 提供假期管理模块统一入口页。
2. 按功能分组展示当前已确认的假期管理能力。
3. 支持点击功能卡片进入对应业务页面。
4. 支持按角色权限控制功能入口是否展示。
5. 支持展示功能名称和功能说明。
6. 支持按页面布局展示“日常假期管理”“假期业务设置”“假期报表”三个分组，其中假期报表分组当前仅保留布局位置。

##### 3.1.1.4 页面模块说明
1. 页面标题区：用于展示“假期管理”页面标题。
2. 日常假期管理分组区：用于展示假期额度维护、请假单入口。
3. 假期业务设置分组区：用于展示假期类型、额度规则、假期制度定义、调休规则入口。
4. 假期报表分组区：用于按参考布局保留报表入口展示区域；当前文档暂无已确认功能卡片。
5. 功能卡片区：用于展示功能名称和简要说明，并支持点击跳转。

##### 3.1.1.5 展示字段定义
1. 页面标题区：页面标题。
2. 分组区：分组名称。
3. 功能卡片区：功能名称、功能说明。

##### 3.1.1.6 交互逻辑
1. 用户点击左侧导航“假期管理”后，进入假期管理入口页。
2. 系统按当前用户权限加载其可见的功能分组和功能卡片。
3. 用户点击某一功能卡片后，系统跳转到对应功能页面。
4. 若用户无某功能权限，则该功能卡片不展示。
5. 当前文档未确认的假期报表类功能不生成可点击卡片，仅保留分组布局位置。
6. 用户从具体功能页面返回后，可继续通过入口页进入其他功能。

##### 3.1.1.7 数据规则
1. 入口页本身不沉淀业务单据数据、档案数据或配置数据，仅维护功能导航关系。
2. 页面展示的功能卡片按角色权限动态过滤。
3. 每个功能卡片需配置名称、说明文案和跳转目标。
4. 跳转目标应与文档中已定义的功能模块一一对应。
5. 入口页分组仅用于导航展示，不改变原有模块归属关系。
6. 假期报表分组当前不配置实际跳转目标。

##### 3.1.1.8 业务规则
1. 假期管理入口页仅作为导航页，不直接承载新增、删除、审批、导入、导出、额度生成、规则配置等业务操作。
2. 入口页仅展示当前文档已确认的功能，不因页面样式参考图新增未确认功能。
3. 同一功能在入口页中仅展示一次，不重复出现在多个分组下。
4. 功能名称应与正文对应模块名称保持一致。
5. 若某功能在文档中仅明确到一级模块，则入口页展示一级模块；若已明确到子项且适合作为独立入口，则可展示子项入口。
6. 假期报表分组当前仅作为页面布局占位，待后续报表需求确认后再补充具体入口。

##### 3.1.1.9 验收标准
1. 点击左侧“假期管理”后可进入统一入口页。
2. 页面可按分组展示当前已确认的假期管理功能。
3. 点击各功能卡片后可正确跳转到对应功能页面。
4. 无权限的功能卡片不展示。
5. 假期报表分组当前仅展示分组标题，不展示未确认功能卡片。
6. 入口页不直接执行具体业务处理。

##### 3.1.1.10 功能入口清单
1. 日常假期管理
   - 假期额度维护
   - 请假单
2. 假期业务设置
   - 假期类型
   - 额度规则
   - 假期制度定义
   - 调休规则
3. 假期报表
   - 当前文档暂无已确认功能入口

#### 3.1.2 管理后台

##### 3.1.2.1 假期额度维护
- 范围：用于查询员工假期额度信息，并支持生成额度、批量延期、离职额度处理、导入导出、审核反审核、删除、查看后台事务以及查看和编辑额度记录。
- 子项：
  1. 假期额度列表
  2. 额度生成
  3. 批量延期
  4. 离职额度处理
  5. 假期额度导入
  6. 假期额度导出
  7. 审核与反审核
  8. 后台事务查看
  9. 假期额度详情

**功能概述**
- 用于查询员工的假期额度相关信息，包括假期类型、标准额度、已用额度、冻结额度、剩余额度等，并支持通过生成额度、导入、离职额度处理等方式形成或调整额度数据。模块支持新增、删除、编辑、查看，并提供审核、反审核、批量延期和后台事务查看能力。
- 年假类额度在数据层可区分为`去年结余法定年假`与`法定年假`，但在展示层统一归并为`法定年假`；展示时需可查看去年结余年假数值，扣减时先扣去年结余，再扣本年法定年假。
- 司龄假额度在数据层可区分为`结余司龄假`与`司龄假`，但在展示层统一归并为`司龄假`；展示时需可查看去年结余司龄假数值，扣减时先扣结余司龄假，再扣本年司龄假。

**角色范围**
| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看权限范围内额度维护结果，不执行后台操作 |
| 考勤员 | 是 | 否 | 是 | 是 | 负责额度生成、导入导出、审核反审核、延期处理、离职额度处理、查看和编辑 |
| 部门负责人 | 是 | 是 | 否 | 否 | 可查看本人管理范围内额度信息 |
| 普通员工 | 否 | 否 | 否 | 否 | 不开放管理后台入口 |

**功能范围**
1. 支持查看假期额度列表。
2. 支持查询员工额度相关信息。
3. 支持通过“生成额度”新增假期额度。
4. 支持批量延期处理假期额度结束日期。
5. 支持离职额度处理。
6. 支持新增导入、修改导入、初始化导入三种导入方式。
7. 支持导出全部和导出选中两种导出方式。
8. 支持审核额度记录。
9. 支持反审核额度记录。
10. 支持删除额度记录。
11. 支持查看后台事务执行情况。
12. 支持查看假期额度详情。
13. 支持编辑假期额度记录。

**页面模块说明**

1. 顶部操作区：用于执行生成额度、批量延期、离职额度处理、导入、导出及更多操作。
2. 查询筛选区：用于按组织、员工、假期类型、日期范围等条件筛选额度数据。
3. 列表区：用于展示员工假期额度明细及审核状态。
4. 生成额度区：用于选择员工范围、假期类型和基准日期并执行额度生成。
5. 批量延期区：用于选择延期方式并批量调整结束日期。
6. 离职额度处理区：用于按离职日期执行失效或折算处理。
7. 导入导出区：用于执行新增导入、修改导入、初始化导入及导出全部、导出选中。
8. 详情/编辑区：用于展示和维护额度记录详细信息。
9. 后台事务区：用于查看额度生成事务执行情况。

**列表字段定义**
| 字段名称 | 说明 |
| --- | --- |
| 勾选框 | 用于选择单条或多条额度记录 |
| 员工编码 | 员工唯一编码 |
| 姓名 | 员工姓名 |
| 假期类型 | 当前额度所属假期类型 |
| 周期开始日期 | 当前额度周期开始日期 |
| 周期结束日期 | 当前额度周期结束日期 |
| 假期单位 | 当前额度单位 |
| 标准额度 | 按规则生成的标准额度 |
| 增减额度 | 后续调整形成的增减额度 |
| 实际额度 | 当前实际可用额度口径 |
| 已用额度 | 已使用的额度值 |
| 冻结额度 | 已冻结或在途占用的额度值 |
| 剩余额度 | 当前剩余可用额度 |
| 审核状态 | 当前额度记录审核状态 |

**展示字段定义**
1. 员工范围：范围选择区或展示字段。
2. 假期类型：列表选择框或展示字段。
3. 基准日期：日期选择框或展示字段。
4. 批量延期方式：列表选择框，包含 3 种延期方式。
5. 离职日期：日期选择框或展示字段。
6. 离职额度处理方式：列表选择框或展示字段，包括失效处理、按规则折算。
7. 导入方式：列表选择框，包括新增导入、修改导入、初始化导入。
8. 导出方式：按钮或操作项，包括导出全部、导出选中。
9. 标准额度：数字输入框或展示字段。
10. 增减额度：数字输入框或展示字段。
11. 实际额度：数字输入框或展示字段。
12. 已用额度：数字展示字段。
13. 冻结额度：数字展示字段。
14. 剩余额度：数字展示字段。
15. 审核状态：状态展示字段。
16. 后台事务执行情况：列表展示区。
17. `法定年假`展示说明：统一展示本年法定年假额度，并补充显示去年结余法定年假数值。
18. `司龄假`展示说明：统一展示本年司龄假额度，并补充显示去年结余司龄假数值。

**交互逻辑**
1. 进入假期额度页面后，系统默认加载员工额度列表。
2. 用户可输入查询条件后执行筛选，系统返回符合条件的额度记录。
3. 用户点击“生成额度”后，系统进入额度生成界面。
4. 用户选择员工范围、假期类型、基准日期后点击“生成”，系统按规则生成假期额度。
5. 用户点击“批量延期”后，系统进入批量延期界面，并按所选延期方式处理额度结束日期。
6. 用户点击“离职额度处理”后，系统按离职日期执行失效或折算处理。
7. 用户点击“导入”后，可选择新增导入、修改导入或初始化导入方式完成导入。
8. 用户点击“导出”后，可按导出全部或导出选中方式导出数据。
9. 用户选择未审核额度记录后点击“审核”，系统将记录状态更新为已审核。
10. 用户选择已审核额度记录后点击“反审核”，系统将记录状态更新为未审核。
11. 额度生成和导入后的额度记录默认为已审核状态，需要修改或重算时应先反审核。
12. 用户点击“删除”后，系统删除选中的额度记录。
13. 用户点击“查看后台事务”后，系统展示额度生成事务执行情况。
14. 用户进入额度详情页后，可查看全部额度信息并进入编辑状态修改可维护字段。

**数据规则**

1. 假期额度记录用于存储员工在各假期类型下的额度结果。
2. 额度生成时需同时选择员工范围、假期类型和基准日期。
3. 系统按额度规则生成对应员工、对应假期类型的额度记录。
4. 批量延期用于调整假期额度的结束日期。
5. 离职额度处理可按离职日期使额度失效，或按离职日期及额度规则进行折算。
6. 离职额度处理功能需配置“离职额度处理”按钮权限后方可使用。
7. 新增导入适用于无额度记录的员工，导入结果为新增额度记录。
8. 修改导入适用于已有额度记录的员工，导入结果为标准额度 + 增减额度 = 实际额度。
9. 初始化导入适用于已有额度记录的员工，导入结果为实际额度 = 剩余额度 + 已用额度。
10. 导出选中仅导出列表中已勾选记录。
11. 导出全部导出当前过滤条件下的全部数据。
12. 审核后额度记录状态为已审核。
13. 反审核后额度记录状态为未审核。
14. 额度生成和导入后的额度记录默认为已审核状态。
15. 后台事务用于记录自动生成额度的执行情况。
16. `去年结余法定年假`与`法定年假`可拆分存储，但在展示层统一归并到`法定年假`标签下。
17. `法定年假`标签应可查看去年结余法定年假数值；实际扣减时优先扣减去年结余法定年假，再扣减本年法定年假。
18. `结余司龄假`与`司龄假`可拆分存储，但在展示层统一归并到`司龄假`标签下。
19. `司龄假`标签应可查看去年结余司龄假数值；实际扣减时优先扣减结余司龄假，再扣减本年司龄假。

**业务规则**
1. 系统应支持通过生成额度新增假期额度记录。
2. 系统应支持查看和编辑假期额度记录。
3. 系统应支持批量延期处理额度结束日期。
4. 系统应支持离职额度处理。
5. 系统应支持新增导入、修改导入、初始化导入三种导入方式。
6. 系统应支持导出全部和导出选中两种导出方式。
7. 系统应支持审核和反审核操作。
8. 额度记录修改或重算前，应先执行反审核。
9. 系统应支持删除额度记录。
10. 系统应支持查看后台事务执行结果。
11. 系统应支持在年假展示中合并呈现`去年结余法定年假`与`法定年假`，并保留去年结余值说明。
12. 系统应支持在司龄假展示中合并呈现`结余司龄假`与`司龄假`，并保留去年结余值说明。

**验收标准**
1. 页面可查询并展示员工假期额度相关信息，包括假期类型、标准额度、已用额度、冻结额度、剩余额度等。
2. 点击“生成额度”后，可进入额度生成界面并按规则生成额度记录。
3. 批量延期功能可按选择的延期方式批量调整额度结束日期。
4. 离职额度处理可按离职日期执行失效或折算处理。
5. 导入功能支持新增导入、修改导入、初始化导入三种方式。
6. 导出功能支持导出全部和导出选中两种方式。
7. 审核后额度记录状态变为已审核。
8. 反审核后额度记录状态变为未审核。
9. 生成和导入后的额度记录默认为已审核状态。
10. 页面支持删除额度记录。
11. 页面支持查看后台事务执行情况。
12. 页面支持查看和编辑额度详情。
13. `法定年假`额度展示时，可同时看到本年法定年假口径及去年结余法定年假数值说明。
14. `司龄假`额度展示时，可同时看到本年司龄假口径及去年结余司龄假数值说明。

##### 3.1.2.2 请假单
- 范围：用于在管理后台基于权限范围查看请假单记录，并执行`导出选中`、`导出全部`、`反审批`、`废弃`等公共处理操作。
- 子项：
  1. 请假单列表
  2. 请假单详情
  3. 公共处理动作

**功能概述**
- 用于在管理后台查看当前用户数据权限范围内的请假单记录，并执行导出选中、导出全部、反审批、废弃等公共处理操作。
- 除上述公共动作外，请假申请的创建、编辑、删除、额度校验、附件校验、半天时段、产假组成项、详情业务逻辑等均统一沿用员工自助端“我要请假”。

**角色范围**
| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看权限范围内请假单，不执行后台操作 |
| 考勤员 | 是 | 否 | 是 | 是 | 负责请假单的查看、导出、反审批和废弃处理 |
| 部门负责人 | 是 | 是 | 否 | 否 | 可查看本人管理范围内请假单，审批操作在审批中心完成 |
| 普通员工 | 否 | 否 | 否 | 否 | 不开放管理后台入口 |

**功能范围**
1. 支持导出选中。
2. 支持导出全部。
3. 支持反审批。
4. 支持废弃。

**页面模块说明**

1. 顶部操作区：仅保留导出选中、导出全部、反审批、废弃。
2. 列表区：用于展示权限范围内的请假单记录。
3. 详情区：用于展示请假单详细信息、审批状态和审批意见。

**列表字段定义**
| 字段名称 | 说明 |
| --- | --- |
| 勾选框 | 用于选择单条或多条请假单记录 |
| 单据编号 | 请假单唯一编号 |
| 员工编码 | 申请员工编码 |
| 姓名 | 申请员工姓名 |
| 组织 | 员工所属组织 |
| 假期类型 | 当前请假所属假期类型 |
| 开始时间 | 请假开始时间 |
| 结束时间 | 请假结束时间 |
| 请假长度 | 请假时长 |
| 单位 | 请假时长单位 |
| 单据状态 | 当前单据状态 |
| 审批人 | 当前或最近审批人 |

**展示字段定义**
1. 顶部操作区：导出选中、导出全部、反审批、废弃。
2. 审批状态：状态展示字段。
3. 审批意见：文本展示字段。

**交互逻辑**
1. 进入请假单列表页面后，系统默认展示当前用户数据权限范围内可查看的全部请假单。
2. 用户选择已审批通过的单据后，可点击“反审批”；仅当部门负责人已审批通过时方可触发。触发后单据退回呈批人并还原为未提交状态，支持批量反审批。
3. 用户选择审批中的请假单后，可通过顶部工具栏执行“废弃”；仅当部门负责人处于审批中时方可触发。触发后单据退回呈批人并还原为未提交状态，同时将冻结额度还原到剩余额度中。
4. 用户点击顶部工具栏“导出选中”时，系统导出当前勾选记录；点击“导出全部”时，系统导出当前过滤条件下的全部数据。

**数据规则**
1. 请假单列表展示当前用户数据权限范围内的全部可见请假单。
2. 废弃操作仅适用于部门负责人审批中的请假单，包括未审批和审批中状态。
3. 反审批或废弃后，单据均退回呈批人并还原为未提交状态；废弃场景下冻结额度同时恢复到剩余额度。
4. 组织、人员、导出范围等涉及数据范围控制的操作，统一遵循当前用户数据权限范围。
5. 除`导出选中`、`导出全部`、`反审批`、`废弃`外，后台本章节不再重复定义其他请假功能、规则和逻辑。

**业务规则**
1. 系统应支持导出选中、导出全部、反审批、废弃。
2. 顶部工具栏仅保留导出选中、导出全部、反审批、废弃；其他历史顶部操作不在本轮保留范围。
3. 除`导出选中`、`导出全部`、`反审批`、`废弃`外，请假业务逻辑统一沿用 `2.3.3 [我的假勤] 我要请假`，后台本章节不再重复定义其他请假功能与业务规则。

**验收标准**
1. 已审批通过且部门负责人已完成审批的单据可执行批量反审批；反审批后单据退回呈批人并变为未提交。
2. 部门负责人审批中的请假单可废弃；废弃后单据退回呈批人、状态恢复为未提交，且冻结额度恢复到剩余额度。
3. 支持导出全部和导出选中行。
4. 顶部工具栏仅展示导出选中、导出全部、反审批、废弃。

#### 3.1.3 系统设置

##### 3.1.3.1 假期类型
- 范围：定义请假场景可使用的固定假期类型字典。假期类型由后端写死并保存在数据库中，前后端统一按固定编码读取，不提供人工新增、删除、编辑、启停配置能力。
- 子项：
  1. 固定假期类型清单
  2. 编码与名称约定
  3. 使用规则

**功能概述**
- 本节用于说明系统内置的假期类型固定字典，作为请假单、额度规则、假期制度定义等模块的统一基础数据来源。
- 假期类型不通过后台页面维护，不面向业务人员开放创建、删除、编辑、启停等配置能力。
- 若后续假期类型需要调整，应由后端直接修改数据库字典及对应程序逻辑，并同步更新本文档。

**角色范围**
| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看固定假期类型字典，不提供人工维护入口 |
| 考勤员 | 是 | 是 | 否 | 否 | 可查看固定假期类型字典，不提供人工维护入口 |
| 部门负责人 | 否 | 否 | 否 | 否 | 不开放基础配置入口 |
| 普通员工 | 否 | 否 | 否 | 否 | 不开放基础配置入口 |

**固定数据清单**
| 编码 | 名称 | 说明 |
| --- | --- | --- |
| `JQLX0000010Y` | 法定年假 | 固定假期类型，由后端内置 |
| `JQLX0000011Y` | 司龄假 | 固定假期类型，由后端内置 |
| `JQLX0000020Y` | 事假 | 固定假期类型，由后端内置 |
| `JQLX0000030Y` | 病假 | 固定假期类型，由后端内置 |
| `JQLX0000040Y` | 调休假 | 固定假期类型，由后端内置 |
| `JQLX0000050Y` | 婚假 | 固定假期类型，由后端内置 |
| `JQLX0000060Y` | 产假 | 固定假期类型，由后端内置 |
| `JQLX0000070Y` | 陪产假 | 固定假期类型，由后端内置 |
| `JQLX0000090Y` | 工伤假 | 固定假期类型，由后端内置 |
| `JQLX0000100Y` | 哺乳假 | 固定假期类型，由后端内置 |
| `JQLX0000110Y` | 丧假 | 固定假期类型，由后端内置 |
| `JQLX0000112Y` | 丧假路途假 | 固定假期类型，由后端内置 |
| `JQLX0000120Y` | 孕检假 | 固定假期类型，由后端内置 |
| `JQLX0000130Y` | 流产假 | 固定假期类型，由后端内置 |
| `JQLX000013Y` | 护理假 | 固定假期类型，由后端内置 |
| `JQLX0000140Y` | 育儿假 | 固定假期类型，由后端内置 |
| `JQLX0000150Y` | 育儿假1 | 按当前数据库/截图名称保留，后续如后端统一命名需同步更新 |

**数据规则**
1. 假期类型数据作为请假单、额度规则、假期制度定义等模块的基础数据使用。
2. 假期类型为后端固定字典，初始化时直接写入数据库，不支持通过后台页面新增、删除、编辑或启停。
3. 前端展示、接口传输、数据库存储均应以固定编码为准，名称仅作为展示文案。
4. 假期类型调整属于程序与数据库字典变更，不属于业务人员后台配置行为。
5. 本节列示的清单以当前后端固定口径为准；后续若后端补充、合并或更名，应同步更新文档。

**业务规则**
1. 系统不得提供假期类型创建、删除、编辑、保存、启用、停用等人工维护能力。
2. 假期类型一经写入数据库，业务模块按固定编码引用，不允许在业务运行中由人员临时增删。
3. 假期制度定义、额度规则、请假单等模块仅可引用本节已定义的固定假期类型。
4. 若后端新增或调整假期类型，必须同步评估对额度规则、制度定义、请假校验和历史数据兼容的影响。
5. 若存在名称与业务口径不一致的情况，以后端最终字典和联调结果为准，并同步修订本文档。

**验收标准**
1. 文档中明确说明假期类型为后端固定字典，不提供人工配置能力。
2. 文档中列出当前固定假期类型的编码和名称清单。
3. 请假单、额度规则、假期制度定义等章节在引用假期类型时，均以本节固定清单为基础。
4. 本节不再保留创建、删除、编辑、状态控制、页面操作区、输入框、详情页等配置化描述。

##### 3.1.3.2 额度规则
- 范围：定义请假场景中各假期类型对应的固定额度规则清单。额度规则由后端写死并保存到数据库中，前后端统一按固定编码读取，不提供人工新增、删除、编辑、启停配置能力。
- 子项：
  1. 固定额度规则清单
  2. 公用规则表
  3. 特殊计算规则说明

**功能概述**
- 本节用于说明系统内置的固定额度规则清单，作为请假额度计算、请假页面剩余额度展示、假期制度定义等模块的统一基础数据来源。
- 额度规则不通过后台页面维护，不面向业务人员开放创建、删除、编辑、启停等配置能力。
- 当前阶段先固定规则列表，并将每条额度规则拆成“公用规则 + 特殊计算规则”两部分记录；具体参数后续逐条补充。
- 固定额度规则清单仅覆盖需要预先生成或固定控制额度的假期类型；`病假`、`产假`、`工伤假`等无预置额度假期不纳入本节固定额度规则清单。

**角色范围**
| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看固定额度规则清单，不提供人工维护入口 |
| 考勤员 | 是 | 是 | 否 | 否 | 可查看固定额度规则清单，不提供人工维护入口 |
| 部门负责人 | 否 | 否 | 否 | 否 | 不开放基础配置入口 |
| 普通员工 | 否 | 否 | 否 | 否 | 不开放基础配置入口 |

**固定额度规则清单**
| 编码 | 名称 | 假期类型 |
| --- | --- | --- |
| `001` | 法定年假 | 法定年假 |
| `002` | 司龄假 | 司龄假 |
| `003` | 事假 | 事假 |
| `004` | 婚假 | 婚假 |
| `005` | 丧假 | 丧假 |
| `006` | 丧假路途假 | 丧假路途假 |
| `007` | 孕检假 | 孕检假 |
| `008` | 育儿假 | 育儿假 |
| `009` | 陪产假 | 陪产假 |
| `010` | 育儿假1 | 育儿假1 |
| `011` | 护理假 | 护理假 |
| `012` | 流产假 | 流产假 |

**表1：公用规则表**
| 规则编码 | 名称 | 假期类型 | 值类型 | 固定值 | 计算标准 | 折算方式 | 额度起始年 | 起始日期计算基准 | 首次额度是否折算 | 首次折算方式 | 基准方式 | 首年生效日期 | 延期时长 | 延期时长单位 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `001` | 法定年假 | 法定年假 | 范围 | 不适用 | 社会工龄 | 年 | 入职首年计算额度 | 入集团日期优先 | 是 | 天 | 自然年 | 入职日期 | 2 | 月 | 特殊计算规则见下方文字说明 |
| `002` | 司龄假 | 司龄假 | 范围 | 不适用 | 司龄 | 年 | 入职满一年计算额度 | 入集团日期优先 | 不适用 | 不适用 | 自然年 | 入职满一年 | 2 | 月 | 特殊计算规则见下方文字说明 |
| `003` | 事假 | 事假 | 固定值 | 8 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 自然年 | 入职日期 | 0 | 年 | 固定值规则 |
| `004` | 婚假 | 婚假 | 固定值 | 10 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 自然年 | 入职日期 | 0 | 年 | 固定值规则 |
| `005` | 丧假 | 丧假 | 固定值 | 3 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 自然年 | 入职日期 | 0 | 年 | 固定值规则 |
| `006` | 丧假路途假 | 丧假路途假 | 固定值 | 2 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 自然年 | 入职日期 | 0 | 年 | 固定值规则 |
| `007` | 孕检假 | 孕检假 | 固定值 | 14 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 自然年 | 入职日期 | 0 | 年 | 固定值规则 |
| `008` | 育儿假 | 育儿假 | 固定值 | 5 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 自然年 | 入职后1个月 | 0 | 年 | 固定值规则 |
| `009` | 陪产假 | 陪产假 | 固定值 | 15 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 自然年 | 试用期满 | 0 | 年 | 固定值规则 |
| `010` | 育儿假1 | 育儿假1 | 固定值 | 5 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 自然年 | 入职后1个月 | 0 | 年 | 固定值规则 |
| `011` | 护理假 | 护理假 | 固定值 | 10 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 自然年 | 入职日期 | 0 | 年 | 固定值规则 |
| `012` | 流产假 | 流产假 | 固定值 | 15 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 不适用 | 自然年 | 入职日期 | 0 | 年 | 固定值规则 |

**法定年假特殊计算规则说明**
1. 本规则仅适用于`法定年假`额度计算。
2. 员工范围前置条件为：`参加工作年限 >= 1`。
3. 额度计算字段固定取`社会工龄`，不使用司龄口径。
4. 工龄分段及对应额度值如下：
   - `社会工龄 >= 0年 且 < 2年`，额度值为 `0天`
   - `社会工龄 >= 2年 且 < 11年`，额度值为 `5天`
   - `社会工龄 >= 11年 且 < 21年`，额度值为 `10天`
   - `社会工龄 >= 21年 且 < 100年`，额度值为 `15天`
5. 额度起始年按`入职首年计算额度`处理，表示员工入职首年即参与法定年假额度计算。
6. 起始日期计算基准按`入集团日期优先`处理；若入集团日期为空，则依次取入公司日期、入职日期。
7. 首次额度需要折算，首次折算方式固定为`天`。
8. 基准方式固定为`自然年`，即额度周期按每年 `1月1日-12月31日` 计算。
9. 首年生效日期固定取`入职日期`。
10. 延期时长固定为 `2个月`。

**法定年假案例**
1. 正常年度案例：
   员工甲当前社会工龄为 `12年`，已进入完整自然年度计算周期。根据分段规则，`社会工龄 >= 11年 且 < 21年`，对应法定年假额度为 `10天`。
2. 首年折算案例：
   员工乙于 `2026-07-01` 入职，当前社会工龄为 `12年`，法定年假基础额度仍按 `10天` 取值。由于该员工属于入职首年，且首次折算方式为`天`，系统需按 `2026-07-01` 至 `2026-12-31` 的在职天数占全年天数比例进行折算；最终折算结果和取整口径以后端实际实现为准。

**司龄假特殊计算规则说明**
1. 本规则仅适用于`司龄假`额度计算。
2. 额度计算字段固定取`司龄`，不使用社会工龄口径。
3. 工龄分段及对应额度值如下：
   - `司龄 >= 0年 且 < 6年`，额度值为 `0天`
   - `司龄 >= 6年 且 < 7年`，额度值为 `1天`
   - `司龄 >= 7年 且 < 8年`，额度值为 `2天`
   - `司龄 >= 8年 且 < 9年`，额度值为 `3天`
   - `司龄 >= 9年 且 < 10年`，额度值为 `4天`
   - `司龄 >= 10年 且 < 100年`，额度值为 `5天`
4. 额度起始年按`入职满一年计算额度`处理，表示员工未满一年时不参与司龄假额度计算，满一年后再进入对应年度额度计算。
5. 起始日期计算基准按`入集团日期优先`处理；若入集团日期为空，则依次取入公司日期、入职日期。
6. 本规则不涉及首次额度按天或按月折算，首年生效日期固定为`入职满一年`。
7. 基准方式固定为`自然年`，即额度周期按每年 `1月1日-12月31日` 计算。
8. 延期时长固定为 `2个月`。

**司龄假案例**
1. 正常年度案例：
   员工丙当前司龄为 `8年`，已进入完整自然年度计算周期。根据分段规则，`司龄 >= 8年 且 < 9年`，对应司龄假额度为 `3天`。
2. 满一年生效案例：
   员工丁于 `2025-07-01` 入职，到 `2026-06-30` 前司龄未满一年，不产生司龄假额度；自 `2026-07-01` 起满足“入职满一年计算额度”条件，再按当时司龄口径进入对应额度计算。若后端按自然年拆分当年剩余额度，具体折算或发放方式以后端实际实现为准。

**数据规则**
1. 额度规则数据作为请假页面剩余额度展示、额度生成、请假扣减和假期制度定义的基础规则来源。
2. 额度规则为后端固定字典，初始化时直接写入数据库，不支持通过后台页面新增、删除、编辑或启停。
3. 前端展示、接口传输、数据库存储均应以固定编码为准，名称和假期类型仅作为展示文案与业务映射说明。
4. 每条额度规则均拆分为两部分记录：上方公用规则使用表格记录，下方特殊计算规则使用文字说明，并可按需附案例。
5. 当前阶段先固定额度规则列表和公用规则表结构；存在分段计算或折算逻辑的规则，再按规则逐条补充特殊计算规则文字说明，当前已先补充`法定年假`和`司龄假`。
6. `法定年假`额度计算字段来源固定为`社会工龄`，`司龄假`额度计算字段来源固定为`司龄`。
7. `病假`、`产假`、`工伤假`属于无预置额度假期，不参与年度额度预分配，也不进入本节固定额度规则清单。
8. 本节列示的清单以当前后端固定口径为准；后续若后端补充、合并、更名或调整编码，应同步更新文档。

**业务规则**
1. 系统不得提供额度规则创建、删除、编辑、保存、启停等人工维护能力。
2. 请假单、假期额度维护、假期制度定义等模块仅可引用本节已定义的固定额度规则。
3. 后续补充额度规则时，应先补公用规则表，再补对应规则的特殊计算规则文字说明，并在必要时补充案例。
4. 若后端新增或调整额度规则，必须同步评估对请假扣减、额度生成、历史数据兼容和展示口径的影响。
5. `法定年假`和`司龄假`的具体计算内容后续补充时，应继续遵循当前已确认的 IPS 员工字段口径和展示扣减口径。
6. `病假`、`产假`、`工伤假`不参与固定额度预分配和剩余额度控制，申请时仅按单据审批流处理。
7. 若存在规则名称与业务口径不一致的情况，以后端最终字典和联调结果为准，并同步修订本文档。

**验收标准**
1. 文档中明确说明额度规则为后端固定清单，不提供人工配置能力。
2. 文档中列出当前固定额度规则的编码、名称、假期类型清单。
3. 文档中为额度规则新增“公用规则表”，并以文字形式记录特殊计算规则。
4. `001 法定年假`、`002 司龄假` 已补全公用规则参数，并补充对应的特殊计算规则说明；`003 事假`、`004 婚假`、`005 丧假`、`006 丧假路途假`、`007 孕检假`、`008 育儿假`、`009 陪产假` 仅保留固定值公用规则记录，不再单独补充下方计算规则说明。
5. 其他固定额度规则继续保留“待后续补充”的占位说明。
6. 文档中明确说明`病假`、`产假`、`工伤假`属于无预置额度假期，不纳入本节固定额度规则清单。
7. 本节不再保留创建、删除、编辑、页面模块说明、值类型配置、参数维护、规则详情维护等配置化描述。

##### 3.1.3.3 假期制度定义
- 范围：用于定义假期制度主数据及制度下各假期类型的规则明细。假期制度规则由后端写入数据库并统一读取，不提供人工页面新增、删除、编辑、启停、详情配置能力。
- 子项：
  1. 假期制度主表
  2. 假期制度明细表（字段待补充）

**功能概述**
- 本节用于说明假期制度的数据库承载模型，作为请假长度计算、假期优先级处理、额度控制、附件校验、产假组成项等业务规则的统一来源。
- 当前设计拆分为两张表：主表承载制度基础信息与全局参数；明细表承载制度下各假期类型的规则项。
- 系统初始化时应预置一个默认假期制度；前后端统一按数据库固定口径读取，不通过后台页面维护。

**角色范围**
| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 保留假期制度规则查看能力，不提供人工维护入口 |
| 考勤员 | 是 | 是 | 否 | 否 | 可查看假期制度数据库规则口径，不提供页面维护 |
| 部门负责人 | 否 | 否 | 否 | 否 | 不开放基础配置入口 |
| 普通员工 | 否 | 否 | 否 | 否 | 不开放基础配置入口 |

**功能范围**
1. 支持定义假期制度主表结构。
2. 支持定义假期制度明细表结构。
3. 支持预置默认假期制度及其常用假期类型规则。
4. 支持按组织维护多个假期制度定义，并在数据库中标识默认制度。
5. 支持假期制度作为请假单、额度规则、考勤档案、假期汇总表、调休明细表等模块的统一数据来源。
6. 当前已确认两张表的拆分方式；主文档保留 `法定年假` 样例，其他已确认假期类型默认值详见同级文档《假期制度定义_其他假期明细默认值.md》，其余假期类型详细字段待后续补充。
7. 不提供假期制度列表、新建、删除、详情、编辑等页面配置能力。

**主表字段定义**
| 字段名称 | 说明 | 默认值 |
| --- | --- | --- |
| 假期制度ID | 主键，假期制度唯一标识 | 数据库生成 |
| 编码 | 假期制度唯一标识编码 | `JQZD001` |
| 名称 | 假期制度名称 | `默认假期制度` |
| 组织 | 该制度所属组织 | `万慧达` |
| 备注 | 假期制度备注说明 | `系统预置` |
| 允许请/销假时修改请假长度 | 标识请假或销假场景是否允许修改请假长度 | `否` |
| 启用请假优先级控制 | 标识是否启用固定请假优先级规则 | `是` |
| 是否启动半天假 | 标识是否启用半天假口径 | `是` |
| 是否默认 | 标识该制度是否为当前组织默认制度 | `是` |
| 上午上班时间 | 半天假口径中的上午开始时间 | `09:00` |
| 上午下班时间 | 半天假口径中的上午结束时间 | `14:00` |
| 下午上班时间 | 半天假口径中的下午开始时间 | `14:00` |
| 下午下班时间 | 半天假口径中的下午结束时间 | `18:00` |
| 参数说明 | 主表级参数补充说明 | `待补充` |

**明细表字段定义**
| 字段名称 | 说明 | 当前状态 |
| --- | --- | --- |
| 假期制度明细ID | 主键，明细记录唯一标识 | 已确认 |
| 假期制度ID | 外键，关联假期制度主表 | 已确认 |
| 假期类型 | 引用 `3.1.3.1 假期类型` 的固定假期类型 | 已确认 |
| 规则字段集合 | 每种假期类型对应的规则参数明细 | 主文档保留 `法定年假` 样例；其他已确认假期类型详见《假期制度定义_其他假期明细默认值.md》；其他假期类型待补充 |
| 说明 | 规则补充说明 | 已确认可保留 |

**明细表默认值（已确认）**
1. `法定年假`

| 字段名称 | 默认值 | 说明 |
| --- | --- | --- |
| 请假单位 | `天` | 请假申请与展示的基础单位 |
| 启动周期 | `是` | 启用周期性规则 |
| 周期长度 | `1` | 周期长度数值 |
| 周期单位 | `年` | 周期长度单位 |
| 控制生成额度 | `是` | 启用额度生成控制 |
| 单位额度 | `1` | 每周期生成额度值 |
| 额度取值方式 | `向下取整` | 额度计算取值方式 |
| 控制请假单位时长 | `是` | 启用请假单位时长控制 |
| 单位时长 | `0.5` | 最小请假单位时长 |
| 请假单位时长取值方式 | `向上取整` | 请假时长换算取值方式 |
| 是否允许补请假 | `是` | 允许补请假 |
| 按时间段控制 | `是` | 启用按时间段控制补请假 |
| 按时间段补请假期限 | `6月` | 时间段控制下的补请假期限 |
| 按周期控制 | `否` | 不启用按周期控制 |
| 按周期补请假期限 | `空` | 按周期控制关闭时无默认值 |
| 是否允许销假 | `是` | 允许销假 |
| 销假期限 | `1` | 销假期限数值 |
| 销假期限单位 | `月` | 销假期限单位 |
| 是否控制假期额度 | `是` | 启用假期额度控制 |
| 额度规则 | `法定年假` | 引用 `3.1.3.2 额度规则` 中对应规则 |
| 是否允许修改额度 | `是` | 允许调整额度 |
| 允许请假次数 | `0` | `0` 表示不单独限制次数 |
| 包括公休日 | `否` | 请假时长不包含公休日 |
| 包括法定假日 | `否` | 请假时长不包含法定假日 |
| 是否需要附件 | `否` | 该假期类型默认不强制附件 |
| 请假优先级 | `10` | 用于参与制度内优先级判断 |
| 固定加班允许请假 | `否` | 固定加班场景下不允许直接请该假 |
| 额度分期发放方式 | `不分期` | 额度默认一次性发放 |
| 请假起始值（天） | `0` | 请假起始值默认不设门槛 |
| 说明 | `空` | 当前未补充额外说明 |

2. 其他已确认假期类型默认值详见同级文档：[假期制度定义_其他假期明细默认值.md](./默认指定清单/假期制度定义_其他假期明细默认值.md)。

**数据规则**

1. 假期制度定义数据作为请假长度计算、假期优先级处理、额度控制、附件校验等业务规则的基础数据使用。
2. 系统初始化时应预置一个默认假期制度。
3. 默认假期制度中应预置常用假期类型。
4. 同一组织下可配置多个假期制度定义，但仅允许一个默认制度。
5. 假期制度定义与假期类型之间存在主从关系，一个制度可引用多个假期类型。
6. 当启用请假优先级控制时，系统应按固定优先级`调休假 > 去年结余法定年假 > 结余司龄假 > 法定年假 > 司龄假 > 其他假期`参与请假计算，不由业务人员手工调整顺序。
7. `去年结余法定年假`与`法定年假`在展示层统一归并为`法定年假`标签，但扣减时需先扣去年结余法定年假。
8. `结余司龄假`与`司龄假`在展示层统一归并为`司龄假`标签，但扣减时需先扣结余司龄假。
9. 当员工申请`事假`且命中公司优先抵扣年休假规则时，制度层允许优先扣减年休假相关额度，但单据请假类型仍保持为`事假`。
10. 当启用半天假时，系统应按制度主表中的半天时段口径参与请假时长计算；当前固定口径为`上午 09:00-14:00`、`下午 14:00-18:00`。
11. 请假最小申请单位固定为半天；调休假达到 `4 小时及以上` 时按半天处理。
12. `病假`、`产假`、`工伤假`属于无预置额度假期，不参与年度额度预分配，也不引用固定额度规则；制度层仅控制其请假申请与审批生效口径。
13. `产假`在制度层采用分段记录型规则：`基础产假98天`为基础组成项，两个`30天`延长生育假按申请场景勾选参与，`难产`增加`15天`，多胞胎每多 `1` 个婴儿增加 `15天`。
14. 制度层应要求后端按产假组成项分别记录已申请、已审批和已使用状态，后续申请时仅允许继续申请剩余未使用组成项。
15. 制度层固定要求`病假`、`孕检假`在提交请假单时必须至少上传 1 份附件；该规则由后端写入数据库并统一校验，前台不提供人工配置。

**业务规则**
1. 假期制度定义不通过后台页面维护，统一由后端写入数据库并按固定口径提供给前后端读取。
2. 每个假期制度定义应以编码作为识别标识，并具备可识别名称。
3. 制度定义应归属到明确组织。
4. 假期制度主表负责承载制度级公共参数，明细表负责承载制度下各假期类型规则。
5. 主表中`是否默认`用于标识当前组织下默认生效的假期制度定义。
6. 制度层应通过`启用请假优先级控制`参数控制多种假期类型并存时的优先级处理逻辑。
7. 制度层应通过`是否启动半天假`及半天时段字段控制半天请假计算逻辑。
8. `法定年假`展示应合并本年法定年假与去年结余法定年假，`司龄假`展示应合并本年司龄假与结余司龄假；该口径由制度规则统一定义，不通过页面人工调整。
9. `病假`、`产假`、`工伤假`不做事先额度分配，制度层不校验其剩余额度；只要部门负责人审批通过即可按请假结果生效。
10. 假期制度一旦被考勤档案引用，不应直接物理删除；如后续需要调整，应以数据库变更和影响评估方式处理。
11. 若后端调整制度参数导致已发起但未完成的请假申请不符合新规则，系统应阻止继续提交，并提示申请人重新调整。
12. 若后端调整制度中涉及额度计算的参数变更（如额度规则引用、周期长度、单位额度等），应触发引用该制度且额度状态为`未使用`或`部分使用`的记录重算；已完全使用的额度记录是否重算待后续确认。

**验收标准**
1. 文档中不再保留假期制度定义的列表、新建、删除、详情、编辑等页面配置描述。
2. 文档明确假期制度定义改为两张表承载：主表 + 明细表。
3. 主表字段定义中可明确看到编码、名称、组织、备注、默认标识、半天假参数、请假优先级控制等制度级字段。
4. 明细表已明确为承载假期类型规则明细的第二张表；主文档保留 `法定年假` 样例，其他已确认假期类型已拆分至同级独立文档。
5. 文档明确假期制度规则由后端写入数据库并统一读取，前台不提供人工配置能力。
6. 文档明确保留请假优先级、半天假、无预置额度假期、产假组成项、附件必填等核心规则口径。
7. 文档明确假期制度仍作为请假单、额度规则、考勤档案、假期汇总表、调休明细表等模块的规则来源。

**待确认问题**
1. 除主文档样例及独立文档中已整理的假期类型外，第二张表中其他假期类型的完整字段清单、字段类型、是否按列展开而非按扩展字段存储，待你后续补充。
2. 主表中的`参数说明`字段是否保留为数据库字段，还是仅保留文档说明用途，待确认。
3. 已完全使用的额度记录在制度参数调整后是否参与重算，待确认。
##### 3.1.3.4 调休规则
- 范围：用于说明系统内置的调休假生成与失效规则。调休规则由后端写死并保存到数据库中，前后端统一按固定口径读取，不提供人工新增、删除、编辑、启停配置能力。
- 子项：
  1. 调休规则说明
  2. 固定规则清单

**功能概述**
- 本节用于说明系统固定的调休规则口径，作为加班转调休、调休假失效和请调休假时长计算的基础规则来源。
- 调休规则不通过后台页面维护，不面向业务人员开放创建、删除、编辑、启停等配置能力。
- 当前仅保留 3 条核心规则：`调休假仅来源于加班单`、`加班与调休按 1:1 转换`、`每次生成的调休时长自生成之日起 3 个月后失效`。

**角色范围**
| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 保留调休规则查看能力，不执行后台配置 |
| 考勤员 | 是 | 是 | 否 | 否 | 可查看固定调休规则，不提供人工维护入口 |
| 部门负责人 | 否 | 否 | 否 | 否 | 不开放基础配置入口 |
| 普通员工 | 否 | 否 | 否 | 否 | 不开放基础配置入口 |

**功能范围**
1. 支持查看固定调休规则说明。
2. 支持查看调休来源、转换比例和失效期限。
3. 支持查看调休规则详情。

**页面模块说明**
1. 规则概述区：用于展示调休规则的适用范围和总体说明。
2. 固定规则清单区：用于展示调休来源、转换比例、失效期限等固定规则。
3. 详情展示区：用于只读展示调休规则全部字段信息。

**列表字段定义**
| 字段名称 | 说明 |
| --- | --- |
| 规则项 | 调休规则名称 |
| 规则内容 | 调休规则具体说明 |

**展示字段定义**
1. 转调休来源：固定展示字段，固定为`加班单`。
2. 转换比例：固定展示字段，固定为`1:1`。
3. 失效期限：固定展示字段，固定为`自每次生成之日起3个月后失效`。
4. 说明：文本展示字段，用于说明调休生成与失效逻辑。

**交互逻辑**
1. 进入调休规则页面后，系统默认展示当前固定规则说明。
2. 用户查看详情时，系统以只读方式展示全部字段信息。
3. 页面不提供创建、删除、编辑、保存、启停等人工操作入口。

**数据规则**
1. 调休假仅来源于已生效的加班单；只有加班单补偿方式选择为`调休`时，才可生成调休时长。
2. 加班时长与生成的调休时长按`1:1`固定转换。
3. 每次加班单生成的调休时长应单独记录生成时间和失效时间。
4. 每次生成的调休时长自生成之日起满`3个月`后失效，不可继续用于请调休假。
5. 后端需按每次生成记录独立管理调休时长，不得将不同批次调休时长合并后统一失效。

**业务规则**
1. 系统应支持查看固定调休规则说明和详情。
2. 调休假不得来源于日考勤结果、节假日上班事实或其他非加班单来源。
3. 调休时长转换比例固定为`1:1`，不支持按工作日、公休日、法定节假日分别配置不同换算比率。
4. 调休时长有效期固定为`3个月`，不支持人工调整为其他期限。
5. 系统不得提供调休规则创建、删除、编辑、保存、启停等人工维护能力。

**验收标准**
1. 文档中明确说明调休规则由后端固定写入数据库，不提供人工配置能力。
2. 页面可只读展示固定调休规则，并明确`转调休来源=加班单`。
3. 文档中明确说明加班时长与调休时长固定按`1:1`转换。
4. 文档中明确说明每次生成的调休时长自生成之日起`3个月`后失效。
5. 文档中不再保留创建、删除、编辑、搜索、分页、周期类型、优先级配置、换算方式配置等页面化描述。

### 3.2 考勤管理

#### 3.2.1 考勤管理入口页

##### 3.2.1.1 功能概述
用于作为“考勤管理”模块的统一入口页，集中展示当前文档已确认的管理后台功能和系统设置功能，帮助用户快速进入对应业务页面。该页面仅承担功能导航和分组展示作用，不直接承载单据处理、计算执行、配置维护等具体业务操作。

##### 3.2.1.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看入口页与系统设置类入口，不执行后台操作 |
| 考勤员 | 是 | 是 | 否 | 否 | 主要使用角色，可进入考勤管理相关后台功能 |
| 部门负责人 | 否 | 否 | 否 | 否 | 当前文档未定义其进入考勤管理统一入口页 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台考勤管理入口页 |

##### 3.2.1.3 功能范围
1. 提供考勤管理模块统一入口页。
2. 按功能分组展示当前已确认的考勤管理能力。
3. 支持点击功能卡片进入对应业务页面。
4. 支持按角色权限控制功能入口是否展示。
5. 支持展示功能名称和功能说明。

##### 3.2.1.4 页面模块说明
1. 页面标题区：用于展示“考勤管理”页面标题。
2. 日常考勤分组区：用于展示考勤档案、考勤单据-出差单管理、考勤单据-加班单管理、考勤单据-补签卡管理、出勤记录、打卡记录、考勤计算、审批中心相关入口。
3. 排班管理分组区：用于展示排班管理-排班列表、排班管理-排班向导、排班管理-考勤组入口。
4. 考勤业务设置分组区：用于展示考勤项目、取卡规则、班次设置、考勤制度、轮班规则入口。
5. 系统设置分组区：用于展示考勤数据设置、数据权限设置、业务字段配置映射、审批工作流入口。
6. 功能卡片区：用于展示功能名称和简要说明，并支持点击跳转。

##### 3.2.1.5 展示字段定义
1. 页面标题区：页面标题。
2. 分组区：分组名称。
3. 功能卡片区：功能名称、功能说明。

##### 3.2.1.6 交互逻辑
1. 用户点击左侧导航“考勤管理”后，进入考勤管理入口页。
2. 系统按当前用户权限加载其可见的功能分组和功能卡片。
3. 用户点击某一功能卡片后，系统跳转到对应功能页面。
4. 若用户无某功能权限，则该功能卡片不展示。
5. 用户从具体功能页面返回后，可继续通过入口页进入其他功能。

##### 3.2.1.7 数据规则
1. 入口页本身不沉淀业务单据数据，仅维护功能导航关系。
2. 页面展示的功能卡片按角色权限动态过滤。
3. 每个功能卡片需配置名称、说明文案和跳转目标。
4. 跳转目标应与文档中已定义的功能模块一一对应。
5. 入口页分组仅用于导航展示，不改变原有模块归属关系。

##### 3.2.1.8 业务规则
1. 考勤管理入口页仅作为导航页，不直接承载新增、审批、导入、导出、计算、配置等业务操作。
2. 入口页仅展示当前文档已确认的功能，不因页面样式参考图新增功能。
3. 同一功能在入口页中仅展示一次，不重复出现在多个分组下。
4. 功能名称应与正文对应模块名称保持一致。
5. 若某功能在文档中仅明确到一级模块，则入口页展示一级模块；若已明确到子项且适合作为独立入口，则可展示子项入口。

##### 3.2.1.9 验收标准
1. 点击左侧“考勤管理”后可进入统一入口页。
2. 页面可按分组展示当前已确认的考勤管理功能。
3. 点击各功能卡片后可正确跳转到对应功能页面。
4. 无权限的功能卡片不展示。
5. 入口页不直接执行具体业务处理。

##### 3.2.1.10 功能入口清单
1. 日常考勤
   - 考勤档案
   - 考勤单据-出差单管理
   - 考勤单据-加班单管理
   - 考勤单据-补签卡管理
   - 出勤记录
   - 打卡记录
   - 考勤计算
   - 审批中心
2. 排班管理
   - 排班管理-排班列表
   - 排班管理-排班向导
   - 排班管理-考勤组
3. 考勤业务设置
   - 考勤项目
   - 取卡规则
   - 班次设置
   - 考勤制度
   - 轮班规则
4. 系统设置
   - 考勤数据设置
   - 数据权限设置
   - 业务字段配置映射
   - 审批工作流

##### 3.2.1.11 待确认问题
暂无。

#### 3.2.2 管理后台

##### 3.2.2.1 考勤档案

###### 3.2.2.1.1 功能概述
用于维护员工考勤档案，作为员工参与排班、打卡考勤、考勤计算以及假期制度分配的统一基础档案模块。模块支持考勤档案创建、导入、启用/禁用、删除、批量赋值、匹配打卡记录、档案历史查看，以及未建档案等处理能力。员工考勤档案编辑支持`纠正档案信息`和`变更档案信息`两种处理方式：纠正用于修正最新档案错误，不产生新的档案历史记录；变更用于从指定生效日期开始调整档案信息，并产生新的档案历史记录。员工考勤档案禁用后，系统需删除该员工禁用日期当天及以后的排班记录，并将该员工排除出后续自动排班对象范围。职位变化、组织变化当前仅保留概念说明，后续如需扩展再单独设计辅助处理能力。新建考勤档案前，需先完成考勤制度、假期制度、班次等基础配置。

###### 3.2.2.1.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看列表与结果，不执行后台维护 |
| 考勤员 | 是 | 否 | 是 | 是 | 主要使用角色，负责档案维护、批量处理、导入导出和历史查看 |
| 部门负责人 | 是 | 是 | 否 | 否 | 可查看本人管理范围内考勤档案 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台考勤档案页面 |

###### 3.2.2.1.3 功能范围
1. 支持查看考勤档案列表。
2. 支持查看未建档案辅助列表。
3. 支持单个创建考勤档案。
4. 支持通过导入模板导入考勤档案，并支持选择“纠正档案信息”或“变更档案信息”。(未开发)
5. 支持启用、禁用考勤档案。
6. 支持禁用考勤档案时删除该员工禁用日期当天及以后的排班记录。
7. 支持禁用考勤档案后将该员工排除出后续自动排班对象范围。
8. 支持删除符合条件的考勤档案。
9. 支持查看考勤档案详情和档案历史。
10. 支持执行匹配打卡记录。(未开发)
11. 支持未建档案人员批量新建。
12. 支持对考勤制度、假期制度、默认班次、打卡考勤、是否自动排班执行批量赋值。
13. 支持编辑考勤档案时选择`纠正档案信息`或`变更档案信息`。
14. 支持选择`变更档案信息`时填写生效日期，并从生效日期开始影响打卡考勤、默认班次、是否自动排班。
15. 支持导出全部和导出选中行。(未开发)

###### 3.2.2.1.4 页面模块说明
1. 页面标题：`考勤档案列表`
2. 按钮：`创建`、`启用`、`批量赋值`
3. 下拉按钮：`更多`，用于承载禁用、删除、导入、导出、匹配打卡记录等扩展操作。
4. 输入框：精确搜索框。
5. 标签页：`考勤档案`、`未建档案`
6. 列表：`考勤档案列表`
7. 列表：`未建档案列表`
8. 表单页：`考勤档案创建/编辑页`
9. 弹窗：`员工选择`
10. 弹窗：`考勤职位`
11. 侧栏或弹窗：`档案历史`
12. 确认弹窗：`禁用确认`，用于提示禁用后将删除员工禁用日期当天及以后的排班记录。
13. 单选项：`纠正档案信息`、`变更档案信息`，用于选择本次编辑的处理方式。
14. 日期选择框：`生效日期`，仅在选择`变更档案信息`时展示。

###### 3.2.2.1.5 列表字段定义
1. 考勤档案列表字段：员工编码、姓名、考勤编号、打卡考勤、行政组织、行政职位、假期制度、考勤制度、默认班次、用工关系状态、状态、是否自动排班。
2. 未建档案列表字段：员工编码、姓名、组织、职位、用工关系状态、入集团日期。
3. 考勤职位弹窗列表字段：序号、职位编码、职位名称、职务、所属组织、上级职位、是否负责人职位。

###### 3.2.2.1.6 展示字段定义
1. 考勤档案创建/编辑页字段：员工编码、员工姓名、考勤组织、考勤职位、考勤编号、打卡考勤、考勤制度、假期制度、默认班次、考勤状态、是否自动排班、处理方式、生效日期。
2. 批量赋值弹窗字段：组织、员工、考勤制度、假期制度、默认班次、打卡考勤、是否自动排班、处理方式。
3. 档案历史展示区：历史记录列表、历史详情入口。
4. 精确搜索区：精确搜索勾选项、关键字输入框、搜索按钮。

###### 3.2.2.1.7 交互逻辑
1. 用户进入考勤档案页面后，系统默认展示 `考勤档案` 标签页和当前权限范围内的档案记录。
2. 用户点击 `创建` 后，系统进入考勤档案创建页；用户选择员工后，系统自动带出该员工所在组织默认的考勤制度、假期制度和默认班次，并允许继续修改。
3. 用户维护考勤编号、打卡考勤、考勤制度、假期制度、默认班次、是否自动排班等字段后点击 `保存`，系统完成档案创建。
4. 当员工已设置默认班次且 `是否自动排班` 为“是”时，系统将该员工标记为自动排班对象，供排班向导按自动排班规则生成年度排班结果。
5. 用户编辑考勤档案时，系统展示`纠正档案信息`和`变更档案信息`两个单选项。
6. 用户选择`纠正档案信息`时，系统不展示生效日期；用户保存后直接修正最新档案信息，不新增档案历史记录。
7. 用户选择`变更档案信息`时，系统展示并要求填写`生效日期`；用户保存后生成新的档案历史记录，打卡考勤、默认班次、是否自动排班从生效日期开始按新值生效。
8. 考勤制度、假期制度在考勤档案编辑时不可修改；页面可展示当前值，但不得作为可编辑项提交。
9. 用户点击 `更多-导入` 后，系统先提供导入模板下载入口；用户上传模板时，需选择“纠正档案信息”或“变更档案信息”后再执行导入。
10. 用户点击档案详情中的 `档案历史` 后，系统展示该员工的档案历史记录，并支持新增、修改、删除历史。
11. 用户选择启用状态档案后，可通过 `更多-禁用` 执行禁用；系统弹出禁用确认提示，明确告知禁用后将删除该员工禁用日期当天及以后的排班记录，用户确认后系统执行禁用。
12. 禁用成功后，系统将员工考勤档案状态更新为禁用，删除该员工禁用日期当天及以后的排班记录，并写入档案历史。
13. 员工考勤档案禁用后，系统不得在后续自动排班任务或排班向导自动排班逻辑中继续为该员工生成排班。
14. 选择禁用状态档案后，可通过工具栏 `启用` 恢复启用；重新启用后是否补生成禁用期间或未来排班，不在禁用操作中自动处理，应按后续启用逻辑或排班向导另行处理。
15. 用户选择档案后点击 `删除`，系统先校验是否存在考勤业务数据；无业务数据时允许删除，有业务数据时阻止删除。
16. 用户点击 `更多-匹配打卡记录` 后，系统将已采集但未成功关联员工的打卡记录，按考勤编号重新尝试关联。
17. 用户切换到 `未建档案` 标签页后，可对选中员工点击 `新建`，或点击 `全部新建` 对当前查询结果批量创建考勤档案。
18. 未建档案批量新建时，系统默认带出员工所在组织的考勤制度、假期制度、默认班次，并允许修改考勤编号、打卡考勤、考勤制度、假期制度、默认班次、是否自动排班。
19. 用户点击 `批量赋值` 后，系统按组织和员工范围筛选处理对象，并允许选择“纠正档案信息”或“变更档案信息”。
20. 用户点击 `更多-导出选中` 时，系统导出当前勾选记录；点击 `更多-导出全部` 时，系统导出当前过滤条件下的全部数据。

###### 3.2.2.1.8 数据规则
1. 创建考勤档案前，需先配置考勤制度、假期制度、班次。
2. 考勤编号对应员工在考勤机上的唯一编码；若与考勤机编码不一致，打卡记录同步后将无法正确关联员工。
3. 创建或批量新建考勤档案时，考勤制度、假期制度、默认班次默认取员工所在组织的默认值，并允许按权限修改；员工假期制度分配统一在考勤档案中完成。
4. `打卡考勤` 为“是”时，员工需要排班并按打卡记录参与考勤计算。
5. `打卡考勤` 为“否”时，员工不需要排班，也不需要按打卡记录计算考勤；考勤计算时取默认班次的标准工时作为出勤时长，请假、出差、加班等时长取对应单据申请时长。
6. `是否自动排班` 为“是”且已设置默认班次时，员工进入自动排班对象范围；排班向导可按排班开始日期至后端固定的排班年度截止日自动生成排班结果。
7. 考勤档案编辑时，`考勤制度`、`假期制度`不可修改；如后续需要调整，应另行设计专项流程。
8. `纠正档案信息`用于修正不准确的最新档案信息，不需要填写生效日期，不新增档案历史记录。
9. `变更档案信息`用于从指定生效日期开始调整档案信息，必须填写生效日期，并新增档案历史记录。
10. `变更档案信息`下受生效日期控制的字段限定为：`打卡考勤`、`默认班次`、`是否自动排班`。
11. 排班向导、自动排班任务、员工日排班解析在读取员工默认班次、打卡考勤、是否自动排班时，必须按排班日期解析对应生效版本，不得只读取最新主表值。
12. 若排班日期早于变更生效日期，使用变更前档案配置；若排班日期大于等于变更生效日期，使用变更后档案配置。
13. 示例：当前为 5 月，员工原默认班次为 9 点班次；用户选择`变更档案信息`，将默认班次改为 10 点班次且生效日期为 7 月，则 6 月排班仍按 9 点班次生成，7 月及以后按 10 点班次生成。
14. `attendance_archive_history`作为`变更档案信息`的生效版本来源，需要记录或可解析生效日期、变更前后快照、处理方式、操作人和操作时间。
15. 禁用考勤档案时，禁用日期取系统执行禁用操作的业务日期；例如 `2026年05月15日` 执行禁用，则删除 `2026年05月15日` 当天及以后该员工的排班记录。
16. 禁用考勤档案只删除禁用日期当天及以后的排班记录，不删除禁用日期之前的历史排班记录。
17. 禁用状态考勤档案不得进入自动排班对象范围；自动排班任务和排班向导自动排班逻辑筛选员工时，必须排除禁用状态档案。
19. 禁用不同于删除；禁用不执行删除考勤档案的业务数据校验逻辑，删除仍需单独按删除规则校验。
19. 禁用解除后也要生成一份考勤档案历史。
20. 导入考勤档案时，处理方式支持“纠正档案信息”和“变更档案信息”。
21. 删除考勤档案前，系统必须校验是否存在考勤业务数据；存在业务数据时不允许删除。
22. 批量赋值仅允许维护考勤制度、假期制度、默认班次、打卡考勤、是否自动排班五个字段。
23. 导出选中仅导出勾选记录，导出全部导出当前过滤条件下的全部数据。

###### 3.2.2.1.8.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 考勤档案 | 员工、行政组织、行政职位、用工关系状态 | `IPS 员工基础数据` |
| 考勤档案 | 考勤制度 | `3.2.3.1.4 考勤制度` |
| 考勤档案 | 假期制度 | `3.1.3.3 假期制度定义` |
| 考勤档案 | 默认班次、标准工时 | `3.2.3.1.3 班次设置` |
| 考勤档案 | 打卡记录匹配结果 | 打卡记录业务数据 |
| 考勤档案 | 档案历史 | 考勤档案历史数据 |
| 考勤档案 | 变更档案信息生效版本 | `attendance_archive_history` |
| 考勤档案 | 禁用后需删除的未来排班记录 | `attendance_schedule_day` |

###### 3.2.2.1.9 业务规则
1. 考勤档案是员工参与排班、打卡考勤、考勤计算和假期制度分配的统一基础档案。
2. 员工未建立有效考勤档案时，不应直接进入常规打卡考勤链路。
3. 考勤编号必须与考勤机侧编码保持一致，否则会导致打卡记录无法正确归属。
4. 打卡考勤关闭的员工，不进入打卡考勤和排班要求控制，但仍可按单据时长参与请假、出差、加班等结果计算。
5. 未建档案批量新建、批量赋值，均应遵循当前用户数据权限范围。
6. 删除考勤档案前必须校验考勤业务数据，已发生考勤业务数据的档案不得删除。
7. 档案历史应保留员工考勤档案变更痕迹，用于追溯档案演变过程。
8. 员工设置默认班次并开启 `是否自动排班` 后，应作为排班向导自动排班的前置条件之一；未满足前置条件的员工不进入自动排班范围。
9. 员工考勤档案禁用后，应立即从自动排班对象范围中移除，后续自动排班不得继续为该员工生成排班。
10. 员工考勤档案禁用后，系统应删除该员工禁用日期当天及以后的排班记录；禁用日期之前的历史排班记录保留。
11. 员工考勤档案重新启用后，系统不在启用动作中自动补生成禁用期间或未来排班；如需生成排班，应通过后续自动排班任务、排班向导或其他排班维护能力处理。
12. `纠正档案信息`和`变更档案信息`是考勤档案编辑的两种不同业务语义，前者修正错误，后者记录从生效日期开始的业务变化。
13. 使用`变更档案信息`修改打卡考勤、默认班次、是否自动排班时，下游排班与考勤计算不得简单使用最新字段值，应按业务日期取对应生效版本。
14. 因生效日期产生的未来档案变更，应影响生效日期当天及之后的排班生成，不影响生效日期之前已存在或待生成的排班口径。

###### 3.2.2.1.10 验收标准
1. 用户可进入考勤档案列表页面并查看 `考勤档案`、`未建档案` 两个标签页。
2. 用户可创建单个考勤档案，并在创建时看到自动带出的考勤制度、假期制度和默认班次。
3. 页面可维护考勤编号、打卡考勤、考勤制度、假期制度、默认班次、是否自动排班等核心字段。
4. 编辑考勤档案时，页面应提供`纠正档案信息`和`变更档案信息`两个处理方式。
5. 选择`纠正档案信息`时，页面不展示生效日期，保存后不新增档案历史记录。
6. 选择`变更档案信息`时，页面展示并必填生效日期，保存后新增档案历史记录。
7. 考勤制度、假期制度在考勤档案编辑中不可修改。
8. 打卡考勤、默认班次、是否自动排班应受生效日期控制；未来生效变更不得影响生效日期之前的排班生成。
9. 当前 5 月将默认班次从 9 点班次变更为 10 点班次且生效日期为 7 月时，6 月排班仍应按 9 点班次生成，7 月及以后按 10 点班次生成。
10. 导入功能支持选择“纠正档案信息”或“变更档案信息”。
11. 用户可查看档案历史，并可在历史记录中执行新增、修改、删除。
12. 启用状态档案可禁用，禁用状态档案可启用。
13. 禁用启用状态档案时，系统应提示用户禁用后会删除该员工禁用日期当天及以后的排班记录。
14. 禁用成功后，员工考勤档案状态变为禁用，且该员工禁用日期当天及以后的排班记录被删除。
15. 禁用成功后，该员工不再进入后续自动排班对象范围。
16. 存在考勤业务数据的档案删除时，系统应阻止删除。
17. 未建档案列表支持 `新建` 和 `全部新建` 两种批量创建方式。
18. 页面支持匹配打卡记录。
19. 页面支持导出全部和导出选中两种导出方式。
20. 员工已设置默认班次且 `是否自动排班` 为“是”时，系统可将其纳入排班向导自动排班对象范围；禁用状态员工除外。

###### 3.2.2.1.11 待确认问题
暂无。

##### 3.2.2.2 考勤单据-出差单管理

###### 3.2.2.2.1 功能概述
用于查看和联动处理从 IPS 出差中获取的员工出差单据，支持按主要字段查询、导出及出差确认相关操作。出差申请的创建、提交、删除等维护动作由 IPS 侧处理，考勤后台不重复建设完整出差单管理能力。

###### 3.2.2.2.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看列表与结果，不执行后台处理 |
| 考勤员 | 是 | 否 | 是 | 是 | 主要使用角色，负责出差确认与导出处理 |
| 部门负责人 | 是 | 是 | 否 | 否 | 可查看本人管理范围内单据，审批操作在审批中心完成 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台管理页面 |

###### 3.2.2.2.3 功能范围
1. 支持出差单列表查询与查看。
2. 支持按主要字段进行高级搜索。
3. 支持导出选中和导出全部。
4. 支持出差确认。
5. 支持查看和维护出差确认列表。

###### 3.2.2.2.4 页面模块说明
1. 工具栏区：仅保留导出选中、导出全部。
2. 高级搜索区：用于按出差单主要字段组合筛选数据。
3. 结果列表区：用于展示出差单据列表。
4. 分页区：用于切换页码和查看总记录数。
5. 出差确认相关入口：用于进入出差确认或出差确认列表。

###### 3.2.2.2.5 列表字段定义
1. 单据编号：出差单唯一编号。
2. 员工编码：员工唯一编号。
3. 姓名：员工姓名。
4. 组织：员工所属组织。
5. 职位：员工职位。
6. 出差开始时间：出差起始时间。
7. 出差结束时间：出差结束时间。
8. 出差天数：本次出差天数。
9. 出差原因：出差申请原因。
10. 目的地：出差目的地。
11. 单据状态：当前单据处理状态。
12. 审批人：当前或最终审批人。
13. 已出差确认：是否已完成出差确认。

###### 3.2.2.2.6 展示字段定义
1. 工具栏区：导出选中、导出全部。
2. 高级搜索区：单据编号、员工编码、姓名、组织、职位、申请日期、出差开始时间、出差结束时间、出差天数、出差原因、目的地、单据状态、已出差确认。
3. 分页区：当前页、总页数、总记录数。

###### 3.2.2.2.7 交互逻辑
1. 用户进入出差单列表后，可查看当前筛选条件下的出差单记录。
2. 系统从 IPS 出差中获取当前用户权限范围内的出差单据，并按查询条件返回符合条件的出差单列表。
3. 用户在高级搜索区按主要字段输入或选择查询条件后，系统返回符合条件的出差单列表。
4. 用户点击顶部工具栏“导出选中”或“导出全部”后，系统按对应口径导出数据。
5. 用户如需处理出差确认，可进入出差确认编辑界面或出差确认列表继续处理。

###### 3.2.2.2.8 数据规则
1. 出差单列表按当前查询条件过滤展示。
2. 高级搜索应支持按主要业务字段组合查询。
3. 出差单数据来源于 IPS 出差。
4. 导出选中针对当前勾选记录导出。
5. 导出全部针对当前过滤结果集导出。
6. 出差确认与原出差单建立关联关系。
7. 出差单审批通过后，系统对出差覆盖时段按已打卡处理，不再要求员工额外发起补卡或补签申请。
8. 组织、人员、导出范围等涉及数据范围控制的操作，统一遵循当前用户数据权限范围。

###### 3.2.2.2.9 业务规则
1. 出差申请的创建、提交、删除等维护动作在 IPS 出差中完成，考勤后台仅获取并展示结果数据。
2. 出差单顶部工具栏仅保留导出选中、导出全部。
3. 出差单数据获取、查询、导出及相关联动操作统一遵循当前用户数据权限范围。
4. 出差结束后可进行出差确认。
5. 出差确认列表用于维护出差确认单，不替代原出差单列表。
6. 合伙人的出差单在ips不需要审核默认通过，并标注 合伙人自动通过
7. 出差单审批通过后，对应出差覆盖时段应视同已完成打卡，不得再要求员工针对该时段额外提交补卡或补签。

###### 3.2.2.2.10 验收标准
1. 用户可进入出差单列表并查看记录。
2. 用户可按主要字段执行高级搜索。
3. 页面可展示从 IPS 出差中获取的出差单据数据。
4. 出差单查询、导出及查看范围遵循当前用户数据权限范围。
5. 顶部工具栏仅展示导出选中、导出全部。
6. 用户可执行导出选中和导出全部操作。
7. 用户可进行出差确认，并可查看和维护出差确认列表。
8. 出差单审批通过后，对应出差覆盖时段不再要求员工额外提交补卡或补签申请。

##### 3.2.2.3 考勤单据-加班单管理

###### 3.2.2.3.1 功能概述
用于在管理后台查看权限范围内的员工加班单据，并执行导出、废弃等处理操作。加班单创建入口统一由 `2.3.5 [我的假勤] 我要加班` 承接，本模块以后台查询和单据处理为主。

###### 3.2.2.3.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看列表与结果，不执行后台处理 |
| 考勤员 | 是 | 否 | 是 | 是 | 主要使用角色，负责单据维护与处理 |
| 部门负责人 | 是 | 是 | 否 | 否 | 可查看本人管理范围内单据，审批操作在审批中心完成 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台管理页面 |

###### 3.2.2.3.3 功能范围
1. 支持加班单列表查询与查看。
2. 支持按主要字段进行高级搜索。
3. 支持导出选中和导出全部。
4. 支持废弃操作。

###### 3.2.2.3.4 页面模块说明
1. 工具栏区：仅保留导出选中、导出全部、废弃。
2. 高级搜索区：用于按加班单主要字段组合筛选数据。
3. 结果列表区：用于展示加班单据列表。
4. 分页区：用于切换页码和查看总记录数。

###### 3.2.2.3.5 列表字段定义
1. 单据编号：加班单唯一编号。
2. 员工编码：员工唯一编号。
3. 姓名：员工姓名。
4. 组织：员工所属组织。
5. 职位：员工职位。
6. 加班日期：加班所属日期。
7. 加班开始时间：加班起始时间。
8. 加班结束时间：加班结束时间。
9. 休息时长(分)：加班期间休息分钟数。
10. 申请加班小时数：本次申请加班时长。
11. 加班原因：加班申请原因。
12. 补偿方式：加班补偿方式。
13. 加班类型：加班分类。
14. 单据状态：当前单据处理状态。
15. 审批人：当前或最终审批人。
16. 来源：单据来源。

###### 3.2.2.3.6 展示字段定义
1. 工具栏区：导出选中、导出全部、废弃。
2. 高级搜索区：单据编号、员工编码、姓名、组织、职位、加班日期、加班开始时间、加班结束时间、申请加班小时数、加班原因、补偿方式、加班类型、单据状态、来源。
3. 分页区：当前页、总页数、总记录数。

###### 3.2.2.3.7 交互逻辑
1. 用户进入加班单列表后，可查看当前筛选条件下的加班单记录。
2. 用户在高级搜索区按主要字段输入或选择查询条件后，系统返回符合条件的加班单列表。
3. 用户点击顶部工具栏“导出选中”或“导出全部”后，系统按对应口径导出数据。
4. 用户在列表中选择符合条件的单据后，可通过顶部工具栏执行“废弃”。

###### 3.2.2.3.8 数据规则
1. 加班单列表按当前查询条件过滤展示。
2. 高级搜索应支持按主要业务字段组合查询。
3. 导出全部针对当前过滤结果集导出。
4. 导出选中针对当前勾选记录导出。
5. 加班单审批通过后，系统对加班覆盖时段按已打卡处理，不再要求员工额外发起补卡或补签申请。
6. 组织、人员、导出范围等涉及数据范围控制的操作，统一遵循当前用户数据权限范围。

###### 3.2.2.3.9 业务规则
1. 仅部门负责人审批中的加班单可废弃；废弃后单据退回呈批人并恢复为未提交状态。
2. 加班单查询、导出及处理范围统一遵循当前用户数据权限范围。
3. 顶部工具栏仅保留导出选中、导出全部、废弃；其他历史顶部操作不在本轮保留范围。
4. 调休时长仅来源于已生效的加班单；当补偿方式为调休时，加班时长按 1:1 转化为调休时长，不存在仅凭节假日上班或其他出勤事实直接生成调休时长的情况。
5. 加班单审批通过后，对应加班覆盖时段应视同已完成打卡，不得再要求员工针对该时段额外提交补卡或补签。

###### 3.2.2.3.10 验收标准
1. 用户可进入加班单列表并查看记录。
2. 用户可按主要字段执行高级搜索。
3. 仅部门负责人审批中的单据可废弃；废弃后单据退回呈批人并恢复为未提交状态。
4. 用户可执行导出选中和导出全部操作。
5. 顶部工具栏仅展示导出选中、导出全部、废弃。
6. 当补偿方式为调休时，系统仅可基于已生效加班单按 1:1 生成对应调休时长；不会仅依据节假日上班事实直接生成调休时长。
7. 加班单审批通过后，对应加班覆盖时段不再要求员工额外提交补卡或补签申请。

###### 3.2.2.3.11 待确认问题
暂无。

##### 3.2.2.4 考勤单据-补签卡管理

###### 3.2.2.4.1 功能概述
用于在管理后台查看权限范围内的员工补签卡单据，并执行导出、反审批、废弃等处理操作。补签卡创建入口统一由 `2.3.6 [我的假勤] 我要补签` 承接，本模块以后台查询和单据处理为主。

###### 3.2.2.4.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看列表与结果，不执行后台处理 |
| 考勤员 | 是 | 否 | 是 | 是 | 主要使用角色，负责单据维护与处理 |
| 部门负责人 | 是 | 是 | 否 | 否 | 可查看本人管理范围内单据，审批操作在审批中心完成 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台管理页面 |

###### 3.2.2.4.3 功能范围
1. 支持补签卡列表查询与查看。
2. 支持按主要字段进行高级搜索。
3. 支持导出选中和导出全部。
4. 支持反审批和废弃操作。
5. 支持缺卡检查。

###### 3.2.2.4.4 页面模块说明
1. 工具栏区：仅保留缺卡检查、导出选中、导出全部、反审批、废弃。
2. 高级搜索区：用于按补签卡主要字段组合筛选数据。
3. 结果列表区：用于展示补签卡单据列表。
4. 分页区：用于切换页码和查看总记录数。
5. 缺卡检查入口：用于进入独立的缺卡检查页，按条件查找缺卡时间点并执行后续处理。

###### 3.2.2.4.5 列表字段定义
1. 单据编号：补签卡单唯一编号。
2. 组织：员工所属组织。
3. 员工编码：员工唯一编号。
4. 姓名：员工姓名。
5. 考勤日期：补签所属日期时间，采用日期时间选择器录入，格式为 `yyyy-MM-dd HH:mm`。
6. 补签卡原因编码：补签原因基础数据编码。
7. 补签卡时间点：补签时间点，精确到分钟；分钟固定按 `00/15/30/45` 四档选择。
8. 补签卡原因：补签原因。
9. 单据状态：当前单据处理状态。
10. 办理人编码：办理人员编号。
11. 办理人姓名：办理人员姓名。
12. 审批人：当前或最终审批人。
13. 备注：补充说明。

###### 3.2.2.4.6 展示字段定义
1. 工具栏区：缺卡检查、导出选中、导出全部、反审批、废弃。
2. 高级搜索区：单据编号、组织、员工编码、姓名、考勤日期、补签卡原因编码、补签卡时间点、补签卡原因、单据状态、办理人编码、办理人姓名、审批人、备注；其中补签时间按 `yyyy-MM-dd HH:mm` 展示。
3. 分页区：当前页、总页数、总记录数。

###### 3.2.2.4.7 交互逻辑
1. 用户进入补签卡列表后，可查看当前筛选条件下的补签卡记录。
2. 用户在高级搜索区按主要字段输入或选择查询条件后，系统返回符合条件的补签卡列表。
3. 用户点击顶部工具栏“缺卡检查”后，系统进入独立的缺卡检查页；用户可按组织、姓名、开始日期、结束日期查询缺卡时间点，并支持对查询结果批量生成补签卡。
4. 用户点击顶部工具栏“导出选中”或“导出全部”后，系统按对应口径导出数据。
5. 用户点击顶部工具栏“反审批”后，系统仅在部门负责人已审批通过时允许触发；触发后补签卡单据退回呈批人、恢复为未提交状态，并删除对应打卡记录表中的补签记录。
6. 用户在列表中选择符合条件的单据后，可通过顶部工具栏执行“废弃”。

###### 3.2.2.4.8 数据规则
1. 补签卡列表按当前查询条件过滤展示。
2. 高级搜索应支持按主要业务字段组合查询。
3. 导出全部针对当前过滤结果集导出。
4. 导出选中针对当前勾选记录导出。
5. 缺卡检查结果来自考勤计算明细中的缺卡时间点。
6. 补签卡原因来源于补签卡原因基础数据。
7. 当前已确认的补签卡原因示例包括：忘记打卡、外出公干、参加公司团建、体育活动。
8. 补签卡原因基础数据支持配置“允许提前补签”标记。
9. 补签卡时间采用日期时间选择器录入，格式为 `yyyy-MM-dd HH:mm`，分钟固定按 `00/15/30/45` 四档选择。
10. 已审批通过的出差单或加班单覆盖时段，不应进入缺卡检查结果，也不要求通过补签卡补录。
11. 组织、人员、导出范围等涉及数据范围控制的操作，统一遵循当前用户数据权限范围。

###### 3.2.2.4.8.1 缺卡检查页说明
1. 缺卡检查以独立页面承载，默认从补签卡管理页顶部工具栏进入。
2. 缺卡检查页查询条件包括：组织、姓名、开始日期、结束日期。
3. 组织字段的可选范围应参考当前用户负责组织的数据权限范围；当选择上级组织时，按组织树覆盖其下属范围。
4. 姓名字段用于在当前负责组织权限范围内进一步筛选员工；未输入姓名时，按组织与日期范围返回结果。
5. 缺卡检查页按钮包括：查询、批量补签、取消。
6. 缺卡检查结果列表至少包括：勾选框、序号、员工编码、姓名、考勤日期、排班打卡点、缺卡时间点。
7. 用户点击“查询”后，系统按当前条件返回缺卡结果；点击“批量补签”后，系统基于当前勾选结果批量生成补签卡；点击“取消”后，系统退出当前缺卡检查页。
8. 若从考勤计算页进入缺卡检查，则应默认带入当前已选组织、开始日期、结束日期；若考勤计算页当前已输入姓名，则姓名一并带入，用户可在缺卡检查页继续调整条件后再查询。
9. 缺卡检查页本身不单独维护缺卡判定算法，缺卡识别结果仍来源于考勤计算明细。

###### 3.2.2.4.9 业务规则
1. 仅部门负责人审批中的补签卡可废弃；废弃后单据退回呈批人并恢复为未提交状态。
2. 仅部门负责人已审批通过的补签卡可反审批；反审批后单据退回呈批人并恢复为未提交状态。
3. 补签卡反审批后，对应打卡记录应从打卡记录表中删除。
4. 缺卡检查批量补签的记录行数上限为 100 行。
5. 考勤明细中的迟到分钟、早退分钟、旷工小时数均为 0 时，当天不检查缺卡。
6. 缺卡点若已被有效请假单或出差单覆盖，则不计为缺卡。
7. 缺卡点若已被审批通过的加班单覆盖，则同样不计为缺卡。
8. 顶部工具栏仅保留缺卡检查、导出选中、导出全部、反审批、废弃；其他历史顶部操作不在本轮保留范围。
9. 缺卡检查作为共享能力由补签卡管理承载页面定义，考勤计算页仅复用其入口与查询条件回填能力。
10. 补签卡查询、导出及处理范围统一遵循当前用户数据权限范围。

###### 3.2.2.4.10 验收标准
1. 用户可进入补签卡列表并查看记录。
2. 用户可按主要字段执行高级搜索。
3. 用户可执行缺卡检查并批量生成补签卡。
4. 仅部门负责人审批中的单据可废弃；废弃后单据退回呈批人并恢复为未提交状态。
5. 仅部门负责人已审批通过的单据可反审批，且反审批后单据退回呈批人、对应打卡记录被删除。
6. 用户可执行导出选中和导出全部操作。
7. 缺卡检查批量补签最多支持 100 行。
8. 顶部工具栏仅展示缺卡检查、导出选中、导出全部、反审批、废弃。
9. 用户进入缺卡检查页后，可看到组织、姓名、开始日期、结束日期四个查询条件，以及查询、批量补签、取消三个按钮。
10. 缺卡检查结果列表可展示勾选框、序号、员工编码、姓名、考勤日期、排班打卡点、缺卡时间点。
11. 组织查询范围遵循当前用户负责组织的数据权限范围。
12. 从考勤计算页进入缺卡检查时，系统可自动带入当前组织、开始日期、结束日期；若当前页已输入姓名，则姓名同步带入。
13. 补签卡时间应支持日期时间选择，并精确到分钟；分钟固定按 `00/15/30/45` 四档选择。

###### 3.2.2.4.11 待确认问题
暂无。

##### 3.2.2.5 出勤记录
###### 3.2.2.5.1 功能概述
用于按组织、姓名、日期范围等条件查询员工出勤记录，集中展示员工打卡、补签卡、请假、出差、加班等出勤相关信息，并支持导出结果与导入打卡记录。该功能以综合查询为主，支持通过上传 Excel 文件向后端新增原始打卡记录，用于补充出勤业务信息，但不参与排班和考勤计算结果的生成，不用于直接维护考勤计算结果。

###### 3.2.2.5.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 否 | 是 | 否 | 可查看权限范围内出勤记录，并可导入打卡记录 |
| 考勤员 | 是 | 否 | 是 | 否 | 可查看负责部门范围内出勤记录、导入打卡记录并用于后台业务核对 |
| 部门负责人 | 是 | 是 | 否 | 否 | 仅查看本人权限范围内数据 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台出勤记录页面 |

###### 3.2.2.5.3 功能范围
1. 支持按组织、姓名、开始日期、结束日期查询出勤记录。
2. 查询方式当前仅保留“全部”。
3. 支持按用户权限范围选择组织。
4. 支持列表展示员工逐日的打卡、补签卡、请假、出差、加班等出勤信息。
5. 支持预留 `打卡来源` 字段，用于标识设备打卡、补签卡、出差自动打卡、加班自动打卡等来源口径。
6. 支持通过“导入打卡记录”按钮上传 Excel 文件，向后端新增打卡记录。
7. 支持下载打卡记录导入模板。
8. 支持展示导入结果，包括成功条数、失败条数和失败原因明细。
9. 支持勾选记录后导出选中行。
10. 支持按查询结果导出全部数据。
11. 支持分页查看查询结果。
12. 支持横向滚动查看完整字段。

###### 3.2.2.5.4 页面模块说明
1. 工具栏区：用于执行查询、导出、导入打卡记录操作。
2. 查询条件区：用于输入组织、姓名、开始日期、结束日期、查询方式等条件；查询方式当前仅保留“全部”。
3. 组织选择弹窗：用于在当前用户可见的组织范围内选择组织。
4. 导入弹窗：用于下载导入模板、上传 Excel 文件、执行导入并查看导入结果。
5. 结果列表区：用于展示员工出勤记录。
6. 分页区：用于切换页码和查看总记录数。

###### 3.2.2.5.5 列表字段定义

| 字段名称 | 说明 |
| --- | --- |
| 选择框 | 用于勾选记录，支持导出选中行 |
| 考勤日期 | 出勤所属日期 |
| 员工编码 | 员工唯一编号 |
| 姓名 | 员工姓名 |
| 组织 | 员工所属组织 |
| 职位 | 员工职位 |
| 打卡记录 | 员工当日打卡信息 |
| 打卡来源 | 预留字段，用于标识设备打卡、补签卡、出差自动打卡、加班自动打卡等来源；是否在页面显示待客户确认 |
| 补卡记录 | 员工当日补签卡信息 |
| 请假时间 | 员工当日请假时间段 |
| 请假类型 | 员工当日请假类型 |
| 请假时长 | 员工当日请假时长 |
| 请假单状态 | 关联请假单状态 |
| 出差时间 | 员工当日出差时间段 |
| 出差天数 | 员工当日出差天数 |
| 出差单状态 | 关联出差单状态 |
| 加班时间 | 员工当日加班时间段 |
| 加班时长 | 员工当日加班时长 |
| 加班单状态 | 关联加班单状态 |

###### 3.2.2.5.6 展示字段定义
1. 工具栏区：查询按钮、导出按钮、导入打卡记录按钮。
2. 查询条件区：组织、姓名、开始日期、结束日期、查询方式（当前仅保留“全部”）。
3. 导入弹窗：模板下载入口、Excel 文件上传入口、导入按钮、导入结果反馈区。
4. 分页区：当前页、总页数、总记录数。

###### 3.2.2.5.7 交互逻辑
1. 用户进入“出勤记录”页面后，系统默认将开始日期和结束日期带为当天，并展示默认结果列表或空列表。
2. 用户选择组织、输入姓名、调整开始日期和结束日期；查询方式当前固定为“全部”。
3. 用户点击组织选择按钮后，系统弹出组织选择弹窗；弹窗中仅展示当前用户权限范围内可见的组织。
4. 用户在组织弹窗中选择组织后，系统将所选组织回填至查询条件区。
5. 用户点击“查询”后，系统按查询条件返回符合条件的员工出勤记录。
6. 系统在列表中按“员工 + 考勤日期”的粒度逐日展示打卡、补签卡、请假、出差、加班等信息；若后续启用 `打卡来源` 展示，则应同步展示对应来源标识。
7. 用户点击“导入打卡记录”按钮后，系统打开导入弹窗，并提供模板下载入口。
8. 用户下载模板、按模板填写后上传 Excel 文件。
9. 系统对上传文件执行格式校验、必填字段校验和数据行级校验；校验失败时返回失败原因明细。
10. 校验通过后，系统将打卡记录写入后端，并反馈成功条数、失败条数和处理结果。
11. 用户完成导入后，可重新执行查询查看最新出勤记录结果。
12. 用户点击“导出”按钮后，系统以下拉方式展示“导出选中”和“导出全部”两个选项。
13. 用户勾选部分记录后点击“导出选中”，系统导出当前勾选记录。
14. 用户点击“导出全部”后，系统导出当前过滤条件下的全部结果。
15. 用户可通过分页控件切换结果页。
16. 用户可通过横向滚动查看超出可视区域的列表字段。

###### 3.2.2.5.8 数据规则
1. 查询结果中的组织范围受当前用户数据权限控制。
2. 组织字段不是自由录入，而是从用户权限范围内的组织中选择。
3. 页面默认查询日期为当天。
4. 查询方式当前仅保留“全部”，后续是否开放“行政组织”“考勤地点”待确认。
5. 页面展示的是员工打卡、补签卡、请假、出差、加班等业务信息的综合结果。
6. 出勤记录与排班和考勤计算结果无直接关系，不作为考勤计算结果来源。
7. 导入仅支持 Excel 文件，模板由系统统一提供。
8. 导入数据写入原始打卡记录后端数据，不直接修改补签卡、请假、出差、加班等单据数据。
9. 同一员工同一打卡时间的重复导入记录不覆盖原有数据，应按失败明细返回。
10. 导入成功后的打卡记录可用于后续出勤记录查询展示。
11. 导出全部针对当前过滤后的结果集。
12. 导出选中仅针对当前勾选记录。
13. 列表按查询条件过滤后分页展示。
14. 结果记录粒度为“员工 + 考勤日期”，用于展示单个员工在单日的综合出勤信息。
15. `打卡来源` 为预留字段；若后续启用展示，应至少支持区分 `设备打卡`、`补签卡`、`出差自动打卡`、`加班自动打卡` 四种来源口径。

###### 3.2.2.5.8.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 出勤记录 | 组织可选范围 | `3.2.3.2.2 数据权限设置` |
| 出勤记录 | 员工基础信息 | `IPS 员工基础数据` |
| 出勤记录 | 打卡记录 | 打卡记录业务数据、用户上传的 Excel 导入数据 |
| 出勤记录 | 打卡来源 | 打卡记录业务数据、补签卡管理业务数据、出差单业务数据、加班单业务数据 |
| 出勤记录 | 补卡记录 | 补签卡管理业务数据 |
| 出勤记录 | 请假信息 | 请假单业务数据 |
| 出勤记录 | 出差信息 | 出差单业务数据 |
| 出勤记录 | 加班信息 | 加班单业务数据 |

###### 3.2.2.5.9 业务规则
1. 用户仅可查看本人权限范围内组织和人员的出勤记录。
2. 组织查询范围应随用户数据权限动态变化，不允许查看超权限组织数据。
3. 查询条件中的开始日期不得晚于结束日期。
4. 查询方式当前仅支持“全部”。
5. 出勤记录页面提供查询、导出和导入打卡记录能力，不提供新增出勤记录、修改出勤结果、删除、审批等操作。
6. 导入打卡记录按钮当前仅对系统管理员、考勤员显示。
7. 导入操作应遵循当前用户数据权限，不得向超权限范围的组织或人员写入打卡记录。
8. 导出操作应遵循当前用户数据权限，不得导出超权限范围数据。
9. 出勤记录仅用于查询员工出勤业务信息，不用于替代考勤计算结果查看页面。
10. 列表字段较多时应支持横向滚动查看，避免截断关键字段。
11. 部门负责人可见该页面，但仅可查看其权限范围内的数据。
12. 若后续启用 `打卡来源` 展示，审批通过的出差单、加班单对应时段应标识为 `出差自动打卡`、`加班自动打卡`，无需生成具体设备打卡时间记录。

###### 3.2.2.5.10 验收标准
1. 用户可进入出勤记录页面并看到查询、导出、导入打卡记录入口。
2. 页面默认将开始日期和结束日期带为当天。
3. 用户可按组织、姓名、开始日期、结束日期查询出勤记录，查询方式当前为“全部”。
4. 组织弹窗中仅展示当前用户权限范围内的组织。
5. 有权限用户点击“导入打卡记录”后，可打开导入弹窗并下载模板。
6. 用户上传符合模板要求的 Excel 文件后，系统可完成导入并返回成功/失败结果汇总。
7. 当导入文件存在异常数据时，系统可返回明确失败原因明细。
8. 成功导入的打卡记录可在后续查询中展示。
9. 列表可展示打卡、补签卡、请假、出差、加班等相关字段。
10. 点击导出按钮后，可看到“导出选中”和“导出全部”两个选项。
11. 勾选记录后可导出选中行。
12. 点击导出全部后可按当前过滤结果导出全部数据。
13. 页面支持分页和横向滚动查看结果。
14. 文档已预留 `打卡来源` 字段，并明确其可用于区分设备打卡、补签卡、出差自动打卡、加班自动打卡。

###### 3.2.2.5.11 待确认问题
1. 查询方式当前需求仅保留“全部”；“行政组织 / 考勤地点”后续是否启用，以及各自的实际查询口径与数据范围差异，待后续确认。
2. 若后续启用“考勤地点”查询方式，当前筛选条件中的“组织”字段是否应切换为“考勤地点”，待确认。
3. 打卡记录字段当前截图展示为时间串，后续是否需要进一步规范展示格式（如上下班分行、逗号分隔、时段拆分）待确认。
4. `打卡来源` 字段当前仅作预留；是否在出勤记录页面正式显示，待客户确认。
##### 3.2.2.6 考勤计算
###### 3.2.2.6.1 功能概述
用于按考勤周期对员工考勤数据进行统一计算，并按状态查看计算结果。系统根据排班、打卡、申请单自动生成考勤结果和异常信息，供考勤员查看结果、发起计算并处理异常。该功能当前按简约版流程整理，前台仅保留 `未计算`、`待计算`、`已计算` 三个标签；其中 `已计算` 支持 `汇总模式` 与 `明细模式` 两种视图：`汇总模式` 为周期矩阵视图（一个员工一行，考勤周期有多少天就展开多少个日期列；例如考勤周期 `202604（2026-03-21~2026-04-20）` 展开 31 列日期，若李四在 `3/23` 列显示 `第一段上班时间：2026-03-23 09:20:00` 即代表该日项目值）；`明细模式` 为单日/单条项目明细视图（按员工+日期+项目值逐条展示），其字段来源统一按 `3.2.3.1.1 考勤项目` 固定字段清单。`已计算` 明细模式上方保留 `缺卡检查`、`计算全部`、`计算选中行`、`导出全部`、`导出选中` 按钮，以及按状态复选框筛选结果数据的能力。

###### 3.2.2.6.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看计算结果与状态流转，不执行后台计算 |
| 考勤员 | 是 | 否 | 是 | 否 | 主要使用角色，负责发起计算、查看结果与处理异常 |
| 部门负责人 | 是 | 是 | 否 | 否 | 可查看本人权限范围内结果，不执行后台计算操作 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台考勤计算页面 |

###### 3.2.2.6.3 功能范围
1. 支持按考勤制度、组织、考勤周期、姓名/编码查询考勤计算数据。
2. 支持按 `未计算`、`待计算`、`已计算` 页签查看不同状态的数据。
3. 支持发起考勤计算，并根据排班、打卡、申请单生成考勤结果。
4. 支持在 `已计算` 中切换 `汇总模式` 与 `明细模式` 查看计算结果。
5. `已计算` 汇总模式按“周期矩阵”展示：一个员工一行，考勤周期有多少天就展开多少个日期列（例如周期 `202604（2026-03-21~2026-04-20）` 展开 31 列）。
6. 支持双击 `已计算` 汇总模式中的某个日期单元格，查看该员工该日的单日考勤详情。
7. `已计算` 明细模式按“单日/单条项目明细”展示，并支持按 `旷工`、`迟到`、`早退`、`请假`、`加班`、`出差` 复选框筛选计算结果。
8. 支持在 `已计算` 明细模式中执行 `缺卡检查`、`计算全部`、`计算选中行`、`导出全部`、`导出选中`。
9. 支持查看员工单日考勤记录详情。
10. 支持流程单据检查入口。
11. 审核、反审核、汇总、转薪资等状态可由后端预留，当前前台不展示对应标签，也不提供相关操作。

###### 3.2.2.6.4 页面模块说明
1. 查询条件区：用于选择考勤制度、组织、考勤周期、姓名/员工编码等查询条件。
2. 状态页签区：用于在 `未计算`、`待计算`、`已计算` 之间切换。
3. 工具栏区：用于执行查询、开始计算、流程单据检查等必要操作。
4. `已计算` 视图切换区：用于在 `汇总模式` 与 `明细模式` 之间切换。
5. `已计算` 汇总模式列表区：用于按周期矩阵展示结果（一个员工一行、按周期天数展开日期列，日期单元格承载当日项目值）。
6. `已计算` 明细模式操作区：用于在单日/单条项目明细视图中执行 `缺卡检查`、`计算全部`、`计算选中行`、`导出全部`、`导出选中`，并展示状态筛选复选框。
7. 结果列表区：用于展示当前页签下的考勤结果数据。
8. 单日详情弹窗：用于查看某员工某天的考勤记录详情。

###### 3.2.2.6.5 列表字段定义
1. 状态列表通用字段：员工编码、姓名、组织名称、职位、考勤组/班次、状态。
2. `未计算` / `待计算` 页签列表字段：员工编码、姓名、考勤编号、打卡考勤、考勤组织、考勤职位、用工关系状态、状态。
3. `已计算` 页签包含 `汇总模式` 与 `明细模式` 两种列表视图。
4. `已计算` 汇总模式列表字段：左侧固定展示员工编码、姓名、组织名称等员工基础信息，右侧按当前考勤周期展开日期列（周期 31 天则展开 31 列）；每个日期单元格展示当天项目值（例如 `第一段上班时间：2026-03-23 09:20:00`）。
5. `已计算` 明细模式列表字段：按单日/单条项目明细展示，记录粒度为“员工+考勤日期+考勤项目”，字段来源统一按 `3.2.3.1.1 考勤项目` 固定字段清单。
6. `已计算` 明细模式状态筛选项：旷工、迟到、早退、请假、加班、出差。
7. 流程单据检查列表字段：单据编号、员工编码、姓名、组织、职位、申请日期、业务时间范围、单据状态、审批人，以及对应业务类型关键字段。

###### 3.2.2.6.6 展示字段定义
1. 查询条件区：考勤制度、组织、考勤周期、姓名/编码。
2. 状态页签区：未计算、待计算、已计算。
3. 工具栏区：查询、开始计算、流程单据检查。
4. `已计算` 视图切换区：汇总模式、明细模式。
5. `已计算` 汇总模式展示字段：周期矩阵展示；列表左侧展示员工基础信息，右侧按日期展开结果列（周期多少天即多少列），每个日期单元格展示当天项目值。
6. `已计算` 明细模式展示字段：单日/单条项目明细展示，字段来源统一按 `3.2.3.1.1 考勤项目` 固定字段清单。
7. `已计算` 明细模式操作区：缺卡检查、计算全部、计算选中行、导出全部、导出选中；状态筛选复选框为旷工、迟到、早退、请假、加班、出差。
8. 单日详情弹窗：当前状态、默认班次、实际出勤时间、旷工情况、迟到情况、早退情况、请假信息、出差信息、加班信息、打卡记录入口、维护更多信息。

###### 3.2.2.6.7 交互逻辑
1. 用户进入考勤计算页面后，先选择考勤制度、组织、考勤周期，并可按需输入姓名/员工编码。
2. 用户点击“查询”后，系统按当前页签和筛选条件返回对应状态的数据。
3. 用户点击“开始计算”后，系统根据排班、打卡、申请单生成或刷新考勤结果。
4. 计算完成后，系统将有计算结果的数据展示在 `已计算` 页签。
5. 当打卡变化、排班变化，或出现新的考勤相关单据（如请假单、补签卡、加班单、出差单）后，系统应将受影响的 `已计算` 记录放入 `待计算` 页签，等待重新计算。
6. 用户进入 `已计算` 后，系统默认进入汇总模式，并按周期矩阵展示（一个员工一行、按考勤周期天数展开日期列）。
7. 用户双击汇总模式中的某个日期单元格后，系统弹出该员工该日的单日详情窗口，展示该员工该日的考勤详情。
8. 单日详情弹窗中的 `维护更多信息` 区提供项目下拉框，项目列表统一来源于 `3.2.3.1.1 考勤项目` 固定字段清单；用户选择某个项目后，系统展示该员工该日对应考勤项目的结果值。
9. 用户切换到 `明细模式` 后，系统按单日/单条项目明细展示结果（记录粒度为“员工+考勤日期+考勤项目”），并在列表上方展示 `缺卡检查`、`计算全部`、`计算选中行`、`导出全部`、`导出选中` 按钮，以及 `旷工`、`迟到`、`早退`、`请假`、`加班`、`出差` 复选框筛选项。
10. 用户勾选一个或多个状态复选框后，系统仅展示符合所选状态的计算明细数据。
11. 用户点击 `计算全部` 后，系统按当前查询条件对结果集执行重新计算；点击 `计算选中行` 后，系统仅对当前勾选记录执行重新计算。
12. 用户点击 `导出全部` 后，系统将当前筛选结果导出为 Excel；点击 `导出选中` 后，系统将当前勾选记录导出为 Excel。
13. 用户点击 `缺卡检查` 后，系统跳转到补签卡管理中的独立缺卡检查页，并默认带入当前组织、开始日期、结束日期；若当前页已输入姓名，则姓名一并带入。具体缺卡检查逻辑沿用补签卡章节定义。
14. 用户点击“流程单据检查”后，系统进入流程单据检查页面，展示待核查的考勤相关单据；具体检查口径见待确认问题。
15. 审核、反审核、汇总、转薪资等后端预留状态不在前台单独展示，相关记录当前统一在 `已计算` 标签中查看。

###### 3.2.2.6.8 数据规则
1. 考勤计算的基础数据来源于排班、打卡和已生效的申请单数据。
2. 页面查询范围中的组织和人员应遵循当前用户数据权限范围。
3. `未计算` 展示该周期尚未形成考勤明细的员工数据。
4. `待计算` 展示已计算过，但因打卡变化、排班变化，或出现新的考勤相关单据而需要重算的员工数据。
5. `已计算` 展示已形成考勤明细的员工数据；后端若预留审核、汇总、转薪资等内部状态，前台当前统一归入 `已计算` 标签查看。
6. `已计算` 包含 `汇总模式` 与 `明细模式` 两种视图；汇总模式为周期矩阵（一个员工一行，周期有多少天就展开多少个日期列），明细模式为单日/单条项目明细（记录粒度为“员工+考勤日期+考勤项目”），字段来源统一按 `3.2.3.1.1 考勤项目` 固定字段清单。
7. 汇总模式中的每日日期单元格承载当天项目值（例如 `第一段上班时间：2026-03-23 09:20:00`），支持双击后进入对应员工对应日期的单日详情。
8. `已计算` 明细模式中的状态筛选数据来源于考勤计算结果明细，支持按 `旷工`、`迟到`、`早退`、`请假`、`加班`、`出差` 六类状态进行组合筛选。
9. `计算全部` 针对当前查询结果集执行重新计算，`计算选中行` 仅针对当前勾选记录执行重新计算。
10. `导出全部` 导出当前筛选结果，`导出选中` 导出当前勾选记录，导出文件格式为 Excel。
11. `缺卡检查` 按钮在考勤计算页仅保留入口能力，具体缺卡识别、查询条件、批量补签逻辑沿用 `3.2.2.4 考勤单据-补签卡管理`；从考勤计算页进入时需默认带入当前组织、开始日期、结束日期，姓名在已输入时一并带入。
12. 审核、反审核、汇总、转薪资等状态可由后端预留，当前前台不展示对应标签，也不提供相关操作。

###### 3.2.2.6.8.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 考勤计算 | 组织、姓名/编码可选范围 | `3.2.3.2.2 数据权限设置`、`IPS 员工基础数据` |
| 考勤计算 | 考勤制度、考勤周期 | `3.2.3.1.4 考勤制度`、`3.2.3.2.1 考勤数据设置` |
| 考勤计算 | 考勤编号、打卡考勤、默认班次、考勤组织/职位 | `3.2.2.1 考勤档案` |
| 考勤计算 | 排班信息 | `3.2.2.7 排班管理-排班列表`、`3.2.2.8 排班管理-排班向导` |
| 考勤计算 | 打卡信息 | 打卡记录业务数据 |
| 考勤计算 | 申请单信息 | 请假单、补签卡、加班单，以及后续接入的出差相关单据 |
| 考勤计算 | `已计算` 汇总模式日期列及单元格摘要 | 当前查询考勤周期、考勤计算结果 |
| 考勤计算 | `已计算` 明细模式列表字段 | `3.2.3.1.1 考勤项目` |
| 考勤计算 | 单日详情弹窗 `维护更多信息` 项目列表及项目值 | `3.2.3.1.1 考勤项目` |
| 考勤计算 | `已计算` 明细模式状态筛选 | 考勤计算结果明细 |
| 考勤计算 | 缺卡检查入口 | `3.2.2.4 考勤单据-补签卡管理` |

###### 3.2.2.6.8.2 页签含义与流转表

| 页签 | 含义 | 进入条件 | 可执行操作 | 成功后去哪里 | 会不会被退回 |
| --- | --- | --- | --- | --- | --- |
| `未计算` | 该周期还没形成考勤结果 | 该员工该周期没有生成考勤明细，或没有形成有效结果 | 开始计算 | 生成考勤明细后进入 `已计算` | 会。算过后如果结果被清空或未形成有效明细，仍可留在这里 |
| `待计算` | 之前算过，但结果可能失效，需要重算 | 已经有考勤明细，但打卡变化、排班变化，或出现新的考勤相关单据，导致结果可能变化 | 开始计算 | 重算成功后回到 `已计算` | 会。`已计算` 以及后端预留的内部状态，在出现新单据或基础数据变化后都可能重新落到这里 |
| `已计算` | 结果已算出，前台统一查看已计算结果 | 该员工该周期已经形成考勤明细 | 查询、缺卡检查、计算全部、计算选中行、导出全部、导出选中、流程单据检查 | 保持在 `已计算` | 会。出现新的考勤相关单据，或打卡、排班变化时可进入 `待计算` |

###### 3.2.2.6.8.3 典型退回逻辑

| 当前页签 | 触发原因 | 退回到哪里 |
| --- | --- | --- |
| `已计算` | 打卡变化、排班变化，或出现新的考勤相关单据 | `待计算` |

###### 3.2.2.6.9 业务规则
1. 前台页面仅保留 `未计算`、`待计算`、`已计算` 三个标签，不展示 `已审核`、`已汇总`、`已转薪资` 等额外标签。
2. “开始计算”统一作为计算入口，用于覆盖 `未计算` 的首次计算和 `待计算` 的重新计算。
3. `已计算` 固定提供 `汇总模式` 与 `明细模式` 两种视图；汇总模式固定为周期矩阵（一个员工一行，周期有多少天就展开多少个日期列），明细模式固定为单日/单条项目明细（记录粒度为“员工+考勤日期+考勤项目”），字段统一来源于 `3.2.3.1.1 考勤项目` 固定字段清单。
4. 汇总模式支持双击某个日期单元格，进入该员工该日的单日详情。
5. `已计算` 明细模式上方保留 `缺卡检查`、`计算全部`、`计算选中行`、`导出全部`、`导出选中` 按钮。
6. `已计算` 明细模式中的状态筛选复选框固定为 `旷工`、`迟到`、`早退`、`请假`、`加班`、`出差` 六项。
7. `计算全部` 和 `计算选中行` 均属于重算入口，不替代主页面的“开始计算”能力。
8. `导出全部` 导出当前筛选结果，`导出选中` 导出当前勾选记录，导出格式为 Excel。
9. `缺卡检查` 在考勤计算页只保留入口，具体缺卡识别、查询条件与批量补签逻辑复用补签卡章节定义，并默认带入当前组织、日期范围及已输入姓名。
10. 审核、反审核、汇总、转薪资等状态可由后端预留，当前前台不提供对应按钮和处理动作。
11. 若打卡变化、排班变化，或出现新的考勤相关单据，受影响记录应进入 `待计算`。
12. 审批通过的出差单、加班单在其覆盖时段内应按已打卡处理，不再要求额外补卡或补签，也不再将对应时段识别为缺卡。
13. 半天请假在考勤计算中按公司固定时段定义覆盖对应结果：`上午`按 `09:00-14:00`，`下午`按 `11:45-18:00`。
14. 最小请假申请单位为半天；调休假达到 `4 小时及以上` 时，在考勤计算中按半天口径覆盖对应结果。

###### 3.2.2.6.10 验收标准
1. 用户可按考勤制度、组织、考勤周期、姓名/编码查询考勤计算数据。
2. 页面可展示 `未计算`、`待计算`、`已计算` 三个状态页签。
3. 用户可在 `未计算` 和 `待计算` 页签发起“开始计算”。
4. 系统可根据排班、打卡、申请单生成考勤结果。
5. 用户进入 `已计算` 后，可在 `汇总模式` 与 `明细模式` 之间切换查看结果。
6. `已计算` 汇总模式可按周期矩阵展示考勤结果（一个员工一行，周期多少天即展开多少个日期列；如 `202604（2026-03-21~2026-04-20）` 展开 31 列）。
7. 用户双击汇总模式中的日期单元格后，可查看该员工该日的考勤详情。
8. 用户进入 `已计算` 明细模式后，可看到单日/单条项目明细记录及 `旷工`、`迟到`、`早退`、`请假`、`加班`、`出差` 复选框筛选项。
9. 用户可在 `已计算` 明细模式执行 `缺卡检查`、`计算全部`、`计算选中行`、`导出全部`、`导出选中`。
10. `导出全部` 可将当前筛选结果导出为 Excel，`导出选中` 可将当前勾选记录导出为 Excel。
11. 用户可通过“流程单据检查”进入对应检查页面查看待核查单据。
12. 页面不展示审核、反审核、汇总、转薪资相关按钮和独立标签。
13. 后端如保留审核、汇总、转薪资等内部状态，前台当前统一不单独展示。
14. 审批通过的出差单、加班单覆盖时段，不会再被系统识别为需要补卡或补签的缺卡时段。
15. 当请假单按半天提交时，系统可按 `上午 09:00-14:00 / 下午 11:45-18:00` 的固定口径覆盖对应考勤结果。
16. 当调休假申请达到 `4 小时及以上` 时，系统按半天口径参与请假计算与结果覆盖。

###### 3.2.2.6.11 待确认问题
1. `已计算` 明细模式中的状态筛选当前固定为 `旷工`、`迟到`、`早退`、`请假`、`加班`、`出差` 六项；后续是否扩展其他筛选项待确认。

##### 3.2.2.7 排班管理-排班列表
###### 3.2.2.7.1 功能概述
用于按组织、员工、日期范围查询员工排班信息，并支持以 `横向显示`、`纵向显示`、`未排班列表` 三种方式查看结果。页面同时提供调班、排班向导、排班时间修订，以及通过“更多”入口执行导出操作。该功能以排班查询和排班维护入口为主，操作逻辑参考金蝶排班列表。

###### 3.2.2.7.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看排班结果与维护入口，不执行后台维护 |
| 考勤员 | 是 | 否 | 是 | 是 | 主要使用角色，负责查询和维护排班 |
| 部门负责人 | 是 | 是 | 否 | 否 | 仅查看本人权限范围内排班数据 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台排班列表页面 |

###### 3.2.2.7.3 功能范围
1. 支持按组织、员工编码、姓名、开始日期、结束日期查询排班信息。
2. 支持 `横向显示`、`纵向显示`、`未排班列表` 三种结果展示方式。
3. 支持查看单日排班详情。
4. 支持编辑单日排班信息并保存。
5. 支持发起调班。
6. 支持进入排班向导。
7. 支持批量执行排班时间修订。
8. 支持通过“更多”执行s导出选中、导出全部。
9. 支持调班单分页和详情查询。

###### 3.2.2.7.4 页面模块说明
1. 页面标题：`排班列表`
2. 按钮：`查询`、`调班`、`排班向导`、`排班时间修订`
3. 下拉按钮：`更多`
4. 组织选择框：用于选择查询组织
5. 员工编码输入框：用于按员工编码查询
6. 姓名输入框：用于按姓名查询
7. 开始日期选择框：用于设置查询开始日期
8. 结束日期选择框：用于设置查询结束日期
9. 标签页：`横向显示`、`纵向显示`、`未排班列表`
10. 列表：`横向显示列表`
11. 列表：`纵向显示列表`
12. 列表：`未排班列表`
13. 详情页：`排班设置` 查看页
14. 表单页：`排班设置` 编辑页

###### 3.2.2.7.5 列表字段定义
1. 横向显示列表字段：员工编码、姓名、考勤组织、考勤日期、日期类型、班次名称、上班时间、下班时间、考勤编号、考勤制度、取卡规则。
2. 纵向显示左侧固定字段：员工编码、姓名、考勤组织。
3. 纵向显示日期字段：按所选开始日期至结束日期逐天展开，每天展示对应班次结果。
4. 未排班列表字段：员工编码、姓名、考勤组织、员工关系状态。

###### 3.2.2.7.6 展示字段定义
1. 排班设置查看页基本信息：员工编码、姓名、考勤组织、考勤日期、日期类型、考勤档案编号、考勤制度、标准工时、取卡规则、班次类型、加班补偿方式。
2. 排班设置查看页班次信息：段次、出勤类型、参考日期、上班时间、上班是否打卡、浮动调整值、下班时间、下班是否打卡、休息开始、休息结束、段内休息分钟数。
3. 排班设置编辑页基本信息：日期类型、班次名称、取卡规则、班次类型、加班补偿方式。
4. 排班设置编辑页班次信息：出勤类型、参考日期、上班时间、上班是否打卡、浮动调整值、下班时间、下班是否打卡、休息开始、休息结束、段内休息分钟数。

###### 3.2.2.7.7 交互逻辑
1. 用户进入排班列表页面后，系统默认带出开始日期为今天往前推 7 天，结束日期为今天。
2. 用户选择组织，并可按需输入员工编码、姓名后点击“查询”。
3. 用户点击 `横向显示` 标签页后，系统按“员工 + 日期”粒度展示排班记录，一人一天一条记录。
4. 用户点击 `纵向显示` 标签页后，系统按“员工”粒度展示结果；左侧固定展示员工编码、姓名、考勤组织，右侧按日期范围逐天展开排班结果，并支持横向拖动查看。
5. 用户点击 `未排班列表` 标签页后，系统展示符合未排班条件的员工列表。
6. 用户点击某条排班记录后，可进入 `排班设置` 查看页查看该日排班信息。
7. 用户点击 `编辑` 后，进入 `排班设置` 编辑页；编辑完成后点击 `保存`，系统更新该条排班数据；点击 `取消` 则放弃本次修改。
8. 用户点击 `调班` 后，系统对所选排班记录发起调班处理，并生成调班历史。
9. 用户点击 `排班向导` 后，跳转到排班向导页面执行批量排班。
10. 用户点击 `排班时间修订` 后，按组织、姓名、日期批量调整排班上下班参数。
11. 用户点击 `更多` 后，可执行导出选中、导出全部。
12. 点击调班单按钮进入调班单页面

###### 3.2.2.7.8 数据规则
1. 查询范围中的组织和员工应遵循当前用户数据权限范围。
2. `横向显示` 和 `纵向显示` 默认查询范围均为今天往前推 7 天。
3. 横向显示按“员工 + 日期”返回结果，一人一天一条记录。
4. 纵向显示按“员工”返回结果，一人一条记录；左侧为员工信息，右侧为日期范围内逐天展开的排班结果。
5. 纵向显示的日期列表应支持横向滚动查看。
6. 未排班列表展示在所选日期范围内未生成排班记录的员工。
7. 排班设置查看页和编辑页的数据来源于当前排班记录。
8. 导出选中仅导出当前勾选记录；导出全部导出当前过滤结果集。

###### 3.2.2.7.8.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 排班列表 | 组织、员工范围 | `3.2.3.2.2 数据权限设置`、`IPS 员工基础数据` |
| 排班列表 | 考勤编号、考勤组织、考勤职位、默认班次 | `3.2.2.1 考勤档案` |
| 排班列表 | 班次名称、班次类型、上下班时间 | `3.2.3.1.3 班次设置` |
| 排班列表 | 考勤制度 | `3.2.3.1.4 考勤制度` |
| 排班列表 | 取卡规则 | `3.2.3.1.2 取卡规则` |
| 排班列表 | 日期类型、工作日/休息日信息 | 工作日历、排班结果数据 |
| 排班列表 | 加班补偿方式 | 排班结果数据或班次规则配置 |

###### 3.2.2.7.9 业务规则
1. 排班列表支持横向显示和纵向显示两种主展示方式，并支持未排班列表查看未生成排班数据的员工。
2. 横向显示用于查看单人单日排班明细，纵向显示用于查看员工在日期范围内的整体排班分布。
3. 调班适用于不同班次的两个排班进行调换，并可按规则生成休息日班次。
4. 排班时间修订用于批量修改排班上的上下班等参数。
5. `更多` 下拉当前仅保留导出能力，不承载其他维护入口。
6. 列表和相关操作应遵循当前用户数据权限范围。

###### 3.2.2.7.10 验收标准
1. 用户进入页面后，`横向显示` 和 `纵向显示` 默认查询范围均为今天往前推 7 天。
2. 用户可按组织、员工编码、姓名、开始日期、结束日期查询排班信息。
3. 页面可切换 `横向显示`、`纵向显示`、`未排班列表` 三种查看方式。
4. 横向显示可按“员工 + 日期”展示排班记录。
5. 纵向显示左侧可展示员工编码、姓名、考勤组织，右侧可按日期展示排班结果并支持横向拖动。
6. 用户可查看单日排班详情。
7. 用户可进入排班编辑页并保存修改。
8. 用户可发起调班并保留调班历史。
9. 用户可通过 `更多` 执行导出选中、导出全部。
10. 查询与操作均受用户数据权限控制。

###### 3.2.2.7.11 排班时间修订（批量）
1. 来源说明：依据操作手册“排班时间修订：按组织、姓名、日期批量进行排班上下班等参数的修改”，并结合本节交互逻辑 `3.2.2.7.7` 第 10 条落地。
2. 入口：用户在 `排班列表` 页面点击 `排班时间修订` 按钮后，系统打开“排班时间修订”弹窗。
3. 弹窗基础字段：开始日期、结束日期、考勤组织、员工、班次名称。
4. 必填与校验规则：开始日期、结束日期、班次名称为必填；结束日期不能小于开始日期；组织和员工筛选范围受数据权限控制。
5. 修订参数范围：按段次维护并批量修改排班参数，至少包括出勤类型、参考日期、上班时间、上班是否打卡、上班浮动调整值、下班时间、下班是否打卡、下班浮动调整值、休息开始、休息结束、段内休息(分)。
6. 生效范围：仅对“组织 + 员工 + 日期范围”命中的已存在排班记录执行修订；未生成排班记录的日期不在本次修订中自动创建。
7. 执行结果反馈：用户点击 `修正` 后，系统返回成功条数、失败条数及失败原因明细；点击 `取消` 则关闭弹窗且不保存变更。
8. 下游联动规则：排班修订成功后，受影响员工在考勤计算中应进入 `待计算`；涉及请假影响时，可通过“排班修改请假重算”按最新排班结果执行重算（仅未结束请假参与重算）。
9. 操作示例（正常场景）：用户选择组织“组织-星云一部”、员工“员工甲、员工乙”、日期 `2026-04-02` 至 `2026-04-10`、班次“九点班次”，将上班时间从 `09:00` 修订为 `09:30` 并点击 `修正`，系统批量更新命中记录并返回成功结果。
10. 操作示例（校验失败场景）：用户输入开始日期 `2026-04-02`、结束日期 `2026-04-01`，点击 `修正` 后系统提示“结束日期不能小于开始日期”，并阻止本次提交。

###### 3.2.2.7.12 待确认问题
1. `未排班列表` 的判定口径待确认：是指员工在所选日期范围内完全没有排班记录，还是日期范围内存在任意未排班日期即进入未排班列表。

##### 3.2.2.8 排班管理-排班向导
###### 3.2.2.8.1 功能概述
用于通过向导方式为员工批量生成排班结果。初版排班向导聚焦“设置时间、选择员工、选择轮班规则或使用默认班次、生成并保存排班结果”的主流程，不提供复杂手工排班交互。对于已设置默认班次且 `是否自动排班` 为“是”的员工，排班向导支持按排班开始日期自动生成至后端固定排班年度截止日的排班结果。

###### 3.2.2.8.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看页面与排班结果，不执行批量排班 |
| 考勤员 | 是 | 否 | 是 | 是 | 主要使用角色，负责批量排班 |
| 部门负责人 | 否 | 否 | 否 | 否 | 当前不进入后台排班向导页面 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台排班向导页面 |

###### 3.2.2.8.3 功能范围
1. 支持设置排班开始日期和结束日期。
2. 支持通过员工选择弹窗筛选并选择待排班员工。
3. 支持通过轮班规则弹窗选择轮班规则，并按时间范围批量生成排班结果。
4. 支持对已设置默认班次且 `是否自动排班` 为“是”的员工，按默认班次自动生成起始日至后端固定排班年度截止日的排班结果。
5. 支持保存排班结果。
6. 保存后的排班结果可在排班列表中查询。

###### 3.2.2.8.4 页面模块说明
1. 页面标题：`排班向导`。
2. 开始日期选择框：用于设置排班开始日期。
3. 结束日期选择框：用于设置排班结束日期；自动排班场景下默认带出后端固定的排班年度截止日。
4. 按钮：`选择员工`、`轮班规则`、`保存`。
5. 列表：已选员工列表。
6. 弹窗：`选择员工`。
7. 弹窗：`轮班规则`。
8. 弹窗：`轮班名称`。
9. 提示信息：用于提示当前自动排班的年度截止日由后端固定，不支持用户在前台配置。

###### 3.2.2.8.5 列表字段定义
1. 选择员工弹窗列表字段：员工编码、姓名、组织、职位。
2. 已选员工列表字段：员工编码、姓名。
3. 轮班名称弹窗列表字段：序号、编码、名称、备注。

###### 3.2.2.8.6 展示字段定义
1. 选择员工弹窗筛选项：考勤制度、组织、考勤组、姓名。
2. 轮班规则弹窗字段：轮班规则、轮班规则开始于、开始日期、结束日期、节假日处理。
3. 自动排班提示信息：用于提示当前自动排班的年度截止日由后端固定，不支持用户在前台配置。

###### 3.2.2.8.7 交互逻辑
1. 用户进入排班向导页面后，先设置开始日期和结束日期。
2. 系统校验排班时间范围；自动排班场景下，结束日期不得超过后端固定的排班年度截止日。
3. 用户点击 `选择员工` 后，系统弹出员工选择弹窗；用户可按考勤制度、组织、考勤组、姓名筛选员工并勾选，点击 `确定` 后将已选员工带回主页面。
4. 主页面展示已选员工列表，支持移除单个已选员工。
5. 当已选员工已设置默认班次且 `是否自动排班` 为“是”时，系统可按默认班次直接生成排班开始日期至排班年度截止日的排班结果，考勤员无需逐日维护。
6. 用户点击 `轮班规则` 后，系统弹出轮班规则弹窗；用户填写轮班规则、轮班规则开始于、开始日期、结束日期、节假日处理后点击 `确定`，系统按规则生成排班结果。
7. 用户点击 `保存` 后，系统保存当前排班结果；保存完成后可在排班列表查看结果。

###### 3.2.2.8.8 数据规则
1. 排班向导的开始日期和结束日期为必填项。
2. 自动排班场景下，结束日期默认带出后端固定的排班年度截止日，且不得超过该截止日；该截止日不支持前台人工配置。
3. 员工选择范围应遵循当前用户数据权限范围。
4. 选择员工弹窗中的筛选条件包括考勤制度、组织、考勤组、姓名。
5. 已选员工列表展示当前纳入本次排班的员工集合。
6. 使用轮班规则生成排班时，应按所选员工、时间范围、轮班规则和节假日处理规则生成排班结果。
7. 员工已设置默认班次且 `是否自动排班` 为“是”时，可按默认班次自动生成排班开始日期至排班年度截止日的排班结果。
8. 保存后生成的排班结果应可在排班列表中查询。

###### 3.2.2.8.8.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 排班向导 | 组织、员工、考勤组可选范围 | `3.2.3.2.2 数据权限设置`、`IPS 员工基础数据` |
| 排班向导 | 考勤制度 | `3.2.3.1.4 考勤制度` |
| 排班向导 | 轮班规则 | `3.2.3.1.5 轮班规则` |
| 排班向导 | 班次结果 | `3.2.3.1.3 班次设置` |
| 排班向导 | 节假日处理 | 轮班规则配置或排班规则配置 |

###### 3.2.2.8.9 业务规则
1. 排班向导按“设置时间、选择员工、排班设置、保存”的主流程完成排班。
2. 自动排班按排班开始日期至后端固定的排班年度截止日执行，截止日由后端固定，不支持用户前台配置。
3. 使用轮班规则排班时，先选择轮班规则，再批量应用到排班范围。
4. 使用轮班规则排班时，可按时间段执行排班。
5. 保存后排班结果应落入排班结果数据，并可在排班列表查询。
6. 考勤员为员工设置默认班次并开启 `是否自动排班` 后，员工即可进入排班向导自动排班范围，无需逐日手工排班。

###### 3.2.2.8.10 验收标准
1. 用户可设置开始日期和结束日期；自动排班场景下，结束日期默认带出后端固定的排班年度截止日，且不得超过该日期。
2. 用户可通过选择员工弹窗筛选并选择员工。
3. 页面可展示已选员工列表，并支持移除单个已选员工。
4. 用户可通过轮班规则弹窗选择规则并生成排班结果。
5. 用户点击 `保存` 后可保存排班结果。
6. 保存后的排班结果可在排班列表查询。
7. 员工已设置默认班次且 `是否自动排班` 为“是”时，系统可自动生成排班开始日期至排班年度截止日的排班结果。

###### 3.2.2.8.11 待确认问题
1. `节假日处理` 的可选枚举值当前仅看到 `顺延`，其余可选项待确认。
##### 3.2.2.9 排班管理-考勤组
###### 3.2.2.9.1 功能概述
用于维护考勤组信息，并将员工归入对应考勤组，便于后续排班操作。考勤组允许将不同行政组织的人员放入同一个组中，以支持跨组织统一排班。当前支持考勤组创建、删除及成员查看，导入能力暂作为待确认问题保留。

###### 3.2.2.9.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看考勤组列表与结果，不执行后台维护 |
| 考勤员 | 是 | 否 | 是 | 是 | 主要使用角色，负责创建、删除和维护考勤组 |
| 部门负责人 | 否 | 否 | 否 | 否 | 当前不进入后台考勤组页面 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台考勤组页面 |

###### 3.2.2.9.3 功能范围
1. 支持查看考勤组列表。
2. 支持创建考勤组。
3. 支持删除考勤组。
4. 支持查看考勤组详情及成员列表。
5. 支持在创建或维护时选择加入考勤组的员工。
6. 支持将不同行政组织的人员放入同一考勤组。

###### 3.2.2.9.4 页面模块说明
1. 页面标题：`考勤组列表`
2. 按钮：`创建`、`删除`
3. 输入框：精确搜索框
4. 列表：考勤组列表
5. 详情页：考勤组详情页
6. 表单页：考勤组创建页
7. 输入框：编码输入框
8. 输入框：名称输入框
9. 输入框：备注输入框
10. 组织选择框：用于选择考勤组所属组织
11. 弹窗：`组织`
12. 输入框：成员搜索框
13. 列表：考勤组成员列表

###### 3.2.2.9.5 列表字段定义
1. 考勤组列表字段：编码、名称、组织、备注。
2. 组织弹窗列表字段：序号、组织编码、组织名称、组织长名称。
3. 考勤组成员列表字段：员工编码、姓名、考勤职位、考勤组织、调出时间、调出组织。

###### 3.2.2.9.6 展示字段定义
1. 考勤组创建页字段：编码、名称、组织、备注。
2. 考勤组详情页基本信息：编码、名称、组织、备注。
3. 考勤组详情页成员区：成员搜索框、成员列表。

###### 3.2.2.9.7 交互逻辑
1. 用户进入 `考勤组列表` 页面后，可查看现有考勤组列表。
2. 用户点击 `创建` 后，进入考勤组创建页。
3. 用户在创建页输入编码、名称、备注，并通过组织选择框选择所属组织。
4. 用户点击组织选择框后，系统弹出 `组织` 弹窗；弹窗中仅展示当前用户权限范围内可选组织，用户选择组织后回填到创建页。（组织统一默认万惠达）
5. 若当前用户仅有一个组织权限，系统可直接将该组织自动带入创建页。（组织统一默认万惠达）
6. 用户在创建过程中选择需要加入考勤组的员工后，点击 `保存`，系统完成考勤组创建。
7. 用户在列表中选中某个考勤组后，可查看该考勤组详情和成员列表。
8. 用户在详情页可通过成员搜索框按员工编码或姓名筛选考勤组成员。
9. 用户在列表中选中考勤组后点击 `删除`，系统先校验该考勤组是否已被排班引用；未被引用时允许删除，已被引用时提示不可删除。

###### 3.2.2.9.8 数据规则
1. 考勤组包含编码、名称、组织、备注等基础信息。
2. 一个考勤组可包含多个员工。
3. 同一考勤组允许包含来自不同行政组织的员工。
4. 组织选择范围应遵循当前用户数据权限范围，用户仅可选择权限范围内的组织。
5. 成员选择范围应遵循当前用户数据权限范围。
6. 若用户仅有一个组织权限，创建页可默认带出该组织。
7. 考勤组详情页成员列表展示当前属于该组的员工数据。
8. 删除前应校验考勤组是否已被排班引用。

###### 3.2.2.9.8.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 考勤组 | 组织可选范围 | `3.2.3.2.2 数据权限设置`、`IPS 员工基础数据` |
| 考勤组 | 考勤组基础信息 | 考勤组主数据 |
| 考勤组 | 成员信息 | 员工基础信息、考勤组成员关系数据 |

###### 3.2.2.9.9 业务规则
1. 考勤组用于服务排班操作，不以行政组织边界作为唯一限制。
2. 不同行政组织的员工可以归入同一个考勤组。
3. 创建考勤组时，需维护考勤组基础信息并选择员工。
4. 创建考勤组时，所属组织只能从当前用户权限范围内选择。
5. 若当前用户仅有一个组织权限，可自动带入该组织。
6. 删除仅针对选中的考勤组执行，且删除前需校验是否已被排班引用。
7. 已被排班引用的考勤组不允许删除，并应给出提示。
8. 列表与成员数据查询应遵循当前用户数据权限范围。

###### 3.2.2.9.10 验收标准
1. 用户可进入考勤组列表页面并查看考勤组列表。
2. 页面可展示编码、名称、组织、备注字段。
3. 用户可创建考勤组并保存。
4. 用户可在创建时选择所属组织。
5. 用户可在创建或维护过程中将员工加入考勤组。
6. 系统支持将不同行政组织的员工加入同一个考勤组。
7. 用户创建考勤组时，仅可选择权限范围内的组织。
8. 若用户仅有一个组织权限，系统可自动带入该组织。
9. 用户可删除未被排班引用的考勤组。
10. 用户可查看考勤组详情及成员列表。
11. 用户可在成员列表中按员工编码或姓名搜索成员。

###### 3.2.2.9.11 待确认问题
1. 导入能力当前暂不纳入已确认范围，后续是否保留导入入口及其模板规则待确认。

##### 3.2.2.10 审批中心
###### 3.2.2.10.1 功能概述
用于承接考勤相关审批任务的统一处理入口。加班单、补签卡、出差单、请假单提交后会生成对应审批任务并进入审批中心，按审批流程进入正常待办流转。审批中心按顶部标签布局提供 `全部`、`待审批`、`我的申请` 三类任务视图，并在待审批任务详情中提供 `通过`、`退回` 处理，同时在待审批页提供“任务权限转交（转签）”按钮，以弹窗方式配置并提交转交参数。除 IPS 已实现的转签能力外，审批中心还支持考勤员在详情页对某一张审批中的单据执行`调整审核人`，可对当前审批步骤的审核人列表进行增删改并支持多人审核，当前按`或签`口径处理；该能力仅作用于当前单据的当前审批步骤，不影响其他单据和后续新任务。

###### 3.2.2.10.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看权限范围内审批任务，不处理审批 |
| 考勤员 | 是 | 否 | 是 | 是 | 可查看并处理权限范围内审批任务，执行反审批、废弃、详情页调整审核人等后台操作 |
| 部门负责人 | 是 | 否 | 是 | 是 | 可查看并处理分配给本人的审批任务 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台审批中心 |

###### 3.2.2.10.3 功能范围
1. 支持承接加班单、补签卡、出差单、请假单四类审批任务。
2. 支持标签页切换：`全部`、`待审批`、`我的申请`。
3. 支持按当前用户权限范围查询任务。
4. 支持搜索框关键字查询与筛选按钮高级筛选。
5. 支持查看任务关联单据和详情页任务流程列表。
6. 支持双击任务进入审批详情。
7. 支持对待审批任务执行 `通过`、`退回`。
8. 支持在 `待审批` 标签页发起“任务权限转交（转签）”，按“流程类型+当前节点”批量转交该步骤下任务。
9. 支持查看任务到达时间、任务运行时长等基础字段。
10. 支持导出当前筛选结果或选中任务。
11. 支持考勤员在审批中的任务详情中发起“调整审核人”，对当前单据当前审批步骤的审核人列表进行增加、删除或修改。

###### 3.2.2.10.4 页面模块说明
1. 标签页：`全部`、`待审批`、`我的申请`，用于切换任务范围。
2. 输入框：搜索输入框，用于关键字搜索任务。
3. 按钮：`筛选`，用于打开高级筛选条件。
4. 按钮：`导出`，用于导出任务数据。
5. 列表：审批任务列表，用于展示任务记录。
6. 详情页：审批任务详情页，用于展示任务关联单据详情。
7. 列表：任务流程列表（详情页内），用于展示流程类型、处理人、处理意见、处理状态、日期。
8. 按钮：`通过`（仅待审批任务详情页展示），用于审批通过当前任务。
9. 按钮：`退回`（仅待审批任务详情页展示），用于退回当前任务。
10. 按钮：`任务权限转交`（仅 `待审批` 标签页展示），用于打开转签弹窗并批量转交当前步骤任务。
11. 弹窗：`任务权限转交`弹窗（仿 IPS 交互样式），用于选择业务种类、转交人及转交参数后提交。
12. 按钮：`调整审核人`（仅审批中的任务详情页展示，且仅考勤员可见），用于维护当前单据当前审批步骤的审核人列表。
13. 弹窗：`调整审核人`弹窗，用于增加、删除或修改审核人并填写调整说明后提交。

###### 3.2.2.10.5 列表字段定义
1. 流程主题：任务主题信息，通常包含单据类型与单据编号。
2. 状态：任务当前处理状态。
3. 当前处理人：当前审批节点处理人。
4. 任务到达时间：任务到达当前处理人的时间。
5. 任务运行时间：任务已运行时长。

###### 3.2.2.10.6 展示字段定义
1. 顶部操作区：全部、待审批、我的申请、搜索输入框、筛选、导出。
2. 标签页：全部、待审批、我的申请。
3. 查询控件：搜索输入框、筛选按钮（支持列表字段高级搜索与名称模糊搜索）。
4. 任务列表：流程主题、状态、当前处理人、任务到达时间、任务运行时间。
5. 任务详情：展示对应单据的基础信息与流程处理信息；待审批详情额外展示通过、退回按钮。
6. 任务流程列表：流程类型、处理人、处理意见、处理状态、日期。
7. 待审批页操作按钮：任务权限转交。
8. 任务权限转交弹窗字段布局（弹窗）：
   - 标题：任务权限转交。
   - 提示文案：可以将选中类型的待审批任务转交给其他人处理。
   - 业务种类：下拉框（流程类型 + 当前节点，例如“请假流程-任务呈批”）。
   - 是否关注：单选（继续关注 / 暂不关注）。
   - 是否可收回：单选（可收回 / 不可收回）。
   - 指定客户：输入框（沿用 IPS 字段，作为关键字筛选项）。
   - 选择转交人：下拉选择框。
   - 备注：多行文本框。
   - 底部按钮：取消、确定转交。
9. 任务详情操作按钮：调整审核人。
10. 调整审核人弹窗字段布局（弹窗）：
   - 标题：调整审核人。
   - 当前单据信息：单据编号、流程类型、当前步骤，只读展示。
   - 当前审核人列表：列表展示，支持查看当前步骤已配置审核人。
   - 审核人维护区：支持新增、删除或修改审核人，允许配置多个审核人。
   - 审批方式：只读展示为`或签`。
   - 调整说明：多行文本框。
   - 底部按钮：取消、确定调整。

###### 3.2.2.10.7 交互逻辑
1. 申请人提交加班单、补签卡、出差单、请假单后，系统生成对应审批任务并写入审批中心，按审批流程进入待办流转。
2. 用户进入审批中心后，系统默认加载用户权限范围内的任务数据。
3. 用户点击 `全部` 标签页时，系统展示用户权限范围内全部任务（包含已处理任务）。
4. 用户点击 `待审批` 标签页时，系统仅展示当前处理人为本人的任务。
5. 用户点击 `我的申请` 标签页时，系统仅展示“本人发起且流程仍在处理中”的任务。
6. 用户在搜索输入框输入关键字后执行搜索，系统返回匹配任务；名称类字段按模糊匹配处理。
7. 用户点击 `筛选` 打开高级筛选面板，按列表字段组合筛选任务。
8. 用户双击任务记录进入详情页，系统按任务类型展示对应详情信息与任务流程列表。
9. 当任务来源于 `待审批` 标签页时，详情页展示 `通过`、`退回` 按钮。
10. 用户执行通过或退回后，系统更新任务状态并刷新相关标签页数据。
11. 用户在 `待审批` 标签页点击 `任务权限转交` 后，系统弹出“任务权限转交”弹窗。
12. 用户在弹窗中选择“业务种类（流程类型+当前节点）”、是否关注、是否可收回、指定客户、转交人并填写备注后，点击确定转交。
13. 系统调用 IPS 既有“任务权限转交”接口完成转签；本系统仅做参数透传与结果回显。
14. 转签成功后，系统刷新 `待审批`、`全部`、`我的申请` 标签页数据，并记录操作日志。
15. 用户点击 `导出` 后，系统导出当前筛选结果或选中记录。
16. 当审批结果为`退回`时，申请人应在原单据上修改后再次提交，不生成新单据。
17. 转签示例：当前流程为“呈批”，当前处理人为“张亚静”，任务处于待审批；张亚静在 `待审批` 页点击“任务权限转交”，选择“业务合同审批-任务呈批”（或考勤对应流程节点）后，可将该步骤下任务批量转交给目标处理人。
18. 考勤员打开审批中的任务详情时，可点击`调整审核人`按钮。
19. 考勤员在弹窗中对当前步骤审核人列表执行新增、删除或修改，并填写调整说明后，点击`确定调整`。
20. 系统仅更新当前单据当前审批步骤的审核人列表，支持多个审核人，并按`或签`口径生效；不影响其他单据，也不影响后续同流程步骤新出现的任务。
21. 调整成功后，系统刷新当前任务详情、流程记录及审批中心列表，并记录调整前审核人列表、调整后审核人列表、操作人、操作时间和调整说明。

###### 3.2.2.10.8 数据规则
1. 审批中心任务来源于四类审批单据：加班单、补签卡、出差单、请假单。
2. 每次提交申请至少生成一条对应审批任务记录。
3. 生成的任务按审批流程进入待办流转。
4. `全部` 标签页查询范围为当前用户权限范围内全部任务，包含已处理任务。
5. `待审批` 标签页查询范围为当前处理人为当前用户的任务。
6. `我的申请` 标签页查询范围为申请人为当前用户且状态为“处理中”的任务。
7. 任务列表查询、详情查看、审批处理统一遵循当前用户数据权限范围。
8. 任务状态变化后应同步影响三个标签页的可见结果集。
9. 任务运行时间按“当前时间 - 任务到达时间”实时或准实时计算展示。
10. 查询支持按列表字段进行高级搜索；名称类字段采用模糊匹配，其余字段按字段类型执行精确匹配或区间匹配。
11. 导出支持“导出当前筛选结果”与“导出选中记录”两种口径。
12. 审批退回后，原业务单据状态应转为`已驳回`，申请人需在原单据上修改并重新提交流程。
13. 任务权限转交仅允许对当前处理人为操作人的`待审批`任务生效。
14. 任务权限转交范围按“流程类型 + 当前节点”聚合，可对该步骤下任务执行批量转交。
15. 转签处理调用 IPS 既有接口，本系统不重建转签引擎，仅维护按钮、弹窗、参数与回显。
16. 转签参数应完整留痕：业务种类、转交人、是否关注、是否可收回、备注、操作人、操作时间、结果状态。
17. 转签生效后，当前已存在且满足“流程类型 + 当前节点 + 原处理人”条件的任务，以及后续新到达的同条件任务，处理人均替换为转交目标人，直至触发收回。
18. 调整审核人仅允许由考勤员对审批中的单据发起。
19. 调整审核人仅作用于当前单据的当前审批步骤，不影响同流程的其他单据，也不影响未来新生成的任务。
20. 调整审核人支持维护多个审核人，当前审批方式固定按`或签`处理。
21. 调整审核人参数应完整留痕：单据编号、流程类型、当前步骤、调整前审核人列表、调整后审核人列表、调整说明、操作人、操作时间、结果状态。

###### 3.2.2.10.8.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 审批中心 | 审批任务来源单据 | `3.2.2.2 考勤单据-出差单管理`、`3.2.2.3 考勤单据-加班单管理`、`3.2.2.4 考勤单据-补签卡管理`、`3.1.2.2 请假单` |
| 审批中心 | 流程状态、当前处理人 | `3.2.3.3 审批工作流` |
| 审批中心 | 组织/人员可见范围 | `3.2.3.2.2 数据权限设置` |

###### 3.2.2.10.9 业务规则
1. 四类审批单据提交后必须进入审批中心形成审批任务。
2. 审批中心仅处理任务流转，不替代原单据业务页面的数据维护能力。
3. 待审批任务仅允许当前处理人执行审批操作。
4. 审批结果仅支持通过、退回两种结果；另支持“任务权限转交”作为处理人变更能力，不改变审批结果类型。
5. 通过或退回后，任务不再属于当前处理人的待审批集合。
6. 我的申请仅保留本人发起且未结束的任务，任务结束后自动移出。
7. 审批中心不提供“新建审批”能力，任务统一由业务单据提交后自动生成。
8. 审批中心审批通过/退回时必填处理意见。
9. 审批退回后必须走“原单修改再提交”路径，不允许通过新建单据替代原单据重提。
10. 转签能力复用 IPS 现有能力，本系统只需对接 IPS 接口并提供弹窗化操作入口；转签属于“流程类型 + 当前节点”维度的处理人替换，影响当前已存在任务和后续同条件任务，直至收回。
11. 调整审核人与转签不是同一能力；前者仅针对某一张单据当前审批步骤调整审核人列表，后者针对指定流程步骤批量替换当前及后续任务处理人。
12. 调整审核人仅允许考勤员发起，且仅适用于审批中的单据。
13. 调整审核人支持多个审核人，当前审批方式固定按`或签`处理。

###### 3.2.2.10.10 验收标准
1. 四类审批单据提交后，审批中心可生成并展示对应任务。
2. 页面可切换 `全部`、`待审批`、`我的申请` 三个标签页。
3. `全部` 标签页可展示权限范围内任务并包含已处理记录。
4. `待审批` 标签页仅展示处理人为当前用户的任务。
5. `我的申请` 标签页仅展示本人发起且处理中任务。
6. 列表可展示流程主题、状态、当前处理人、任务到达时间、任务运行时间。
7. 双击任务后可进入详情页并查看对应单据与流程信息。
8. 待审批任务详情可执行通过、退回，并在操作后更新任务状态和标签页结果。
9. 查询区域可按列表字段执行高级搜索，并支持名称类字段模糊搜索。
10. 页面支持按当前查询条件导出任务数据。
11. 待审批任务执行退回后，申请人可在原单据上修改并再次提交；系统不应要求新建单据。
12. `待审批` 标签页可见“任务权限转交”按钮；点击后可弹出“任务权限转交”弹窗。
13. 弹窗可展示并填写业务种类、是否关注、是否可收回、指定客户、选择转交人、备注等字段，布局与 IPS 转签弹窗一致。
14. 点击“确定转交”后，系统可成功调用 IPS 接口完成转签，并刷新任务列表与日志记录。
15. 考勤员在审批中的任务详情中可见“调整审核人”按钮；非考勤员不可见。
16. 点击“调整审核人”后，系统可弹出审核人调整弹窗，并展示当前单据编号、流程类型、当前步骤、当前审核人列表等信息。
17. 提交调整后，系统仅更新当前单据当前审批步骤的审核人列表，支持多个审核人并按`或签`处理，不影响其他单据，也不影响后续同流程步骤新出现的任务。
18. 转签成功后，当前已存在且满足条件的任务与后续同条件任务均替换为新处理人，直至收回。

###### 3.2.2.10.11 待确认问题
1. 通过/退回时是否必须填写处理意见，当前未明确。
2. 请假单与审批中心的章节衔接已明确，后续如新增其他假期类单据再单独补充。

###### 3.2.2.10.12 文档更新记录
| 日期 | 变更 ID | 类型 | 变更内容 | 说明 |
| --- | --- | --- | --- | --- |
| 2026-04-01 | `CR-20260401-04` | 对话补充 | 新增审批中心“任务权限转交（转签）”按钮与弹窗UI需求 | 转签能力复用 IPS 现有能力，仅需调整 IPS 接口对接并在审批中心提供弹窗入口。 |
| 2026-04-07 | `CR-20260407-05` | 对话补充 | 明确转签定义并新增详情页调整审核人需求 | 转签为流程步骤级替换，影响当前及后续同条件任务；详情页调整审核人仅作用于当前单据当前审批步骤，支持多个审核人并按或签处理。 |

##### 3.2.2.11 打卡记录

###### 3.2.2.11.1 功能概述
用于按组织、姓名、开始日期、结束日期查询员工打卡记录，并支持通过 Excel 文件导入打卡记录。该模块仅保留查询与导入能力，不提供反作废、作废、审批、导出等操作。

###### 3.2.2.11.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 否 | 是 | 否 | 可查询并导入权限范围内打卡记录 |
| 考勤员 | 是 | 否 | 是 | 否 | 可查询并导入负责范围内打卡记录 |
| 部门负责人 | 是 | 是 | 否 | 否 | 仅查询权限范围内打卡记录 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台打卡记录页面 |

###### 3.2.2.11.3 功能范围
1. 支持按组织、姓名、开始日期、结束日期执行查询。
2. 支持点击“导入”上传 Excel 文件写入后端打卡记录。
3. 支持导入模板下载与导入结果反馈。
4. 工具栏仅保留“查询”“导入”两个按钮。

###### 3.2.2.11.4 页面模块说明
1. 工具栏区：仅包含查询按钮与导入按钮。
2. 查询条件区：组织、姓名、开始日期、结束日期。
3. 结果列表区：展示符合条件的打卡记录。
4. 导入弹窗：上传 Excel、下载模板、展示导入结果。

###### 3.2.2.11.5 列表字段定义
| 字段 | 说明 |
| --- | --- |
| 员工编码 | 员工唯一编码 |
| 姓名 | 员工姓名 |
| 组织名称 | 员工所属组织 |
| 考勤编号 | 员工考勤机编号 |
| 打卡日期 | 打卡日期 |
| 打卡时间 | 打卡时间 |
| 打卡来源 | 打卡数据来源 |
| 打卡位置 | 打卡位置描述 |
| 描述 | 记录补充说明 |
| 状态 | 打卡记录状态 |

###### 3.2.2.11.6 展示字段定义
1. 工具栏区：查询、导入。
2. 查询条件区：组织、姓名、开始日期、结束日期。
3. 列表区：员工编码、姓名、组织名称、考勤编号、打卡日期、打卡时间、打卡来源、打卡位置、描述、状态。

###### 3.2.2.11.7 交互逻辑
1. 用户进入“打卡记录”页面后，系统默认带出当月起止日期。
2. 用户在查询条件区选择组织、输入姓名、选择开始日期和结束日期。
3. 用户点击“查询”后，系统按条件返回打卡记录列表。
4. 用户点击“导入”后，系统打开导入弹窗并提供模板下载。
5. 用户上传 Excel 后系统执行格式校验与数据校验。
6. 校验通过后写入后端打卡记录，并返回成功/失败明细。
7. 导入完成后用户可重新查询查看结果。

###### 3.2.2.11.8 数据规则
1. 打卡记录查询遵循数据权限，仅返回当前用户可见组织与人员数据。
2. 查询条件中的开始日期不得晚于结束日期。
3. 导入文件格式限定为 Excel（`.xls`、`.xlsx`）。
4. 导入数据需至少包含员工标识、打卡日期、打卡时间等关键字段。
5. 导入校验失败的数据不得写入后端，并返回失败原因。

###### 3.2.2.11.9 业务规则
1. 打卡记录页面仅提供查询与导入能力，不提供反作废、作废、审批、导出操作。
2. 查询条件固定为组织、开始日期、结束日期、姓名，不新增其他筛选项。
3. 导入操作必须受当前用户数据权限控制，不得跨权限写入数据。
4. 系统应保留导入处理结果（成功条数、失败条数、失败原因）供用户查看。

###### 3.2.2.11.10 验收标准
1. 页面工具栏仅展示“查询”“导入”两个按钮。
2. 查询条件区仅包含组织、开始日期、结束日期、姓名四项。
3. 用户设置条件后可查询到符合条件的打卡记录。
4. 用户点击导入可打开导入弹窗并下载模板。
5. 用户上传符合模板的 Excel 后可成功导入，导入结果可查看成功/失败明细。
6. 用户无权限时不可查询或导入超权限组织与人员的数据。

###### 3.2.2.11.11 待确认问题
1. 导入模板字段口径（必填列、时间格式、重复记录处理策略）是否沿用现网模板，待客户确认。
2. 默认日期范围当前暂按“当月”处理，是否改为“当天”待客户确认。


#### 3.2.3 系统设置

##### 3.2.3.1 考勤业务设置
- 范围：汇总考勤项目、取卡规则、班次设置、考勤制度、轮班规则等考勤业务配置。
- 子项：
  1. 考勤项目
  2. 取卡规则
  3. 班次设置
  4. 考勤制度
  5. 轮班规则

###### 3.2.3.1.1 考勤项目
- 范围：用于说明系统固定的考勤计算结果字段清单。考勤项目由后端写死并保存到数据库中，前后端统一按固定口径读取，不提供人工创建、编辑、删除、启停、分类维护或公式绑定配置能力。
- 子项：
  1. 固定字段说明
  2. 固定字段清单

**3.2.3.1.1.1 功能概述**
- 本节用于定义考勤计算结果的固定输出字段，作为 `3.2.2.6 考勤计算` 汇总模式列表、结果导出和下游统计引用的字段来源。
- 当前文档中的“考勤项目”不再按可配置项目主数据处理，而是按“考勤计算结果字段”统一维护；考勤计算的目标即是产出本节定义的字段内容。
- 考勤项目字段由后端固定写入数据库，前台仅查看字段定义，不开放人工新增、删除、编辑、自定义项目和项目分类维护能力。

**3.2.3.1.1.2 角色范围**

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 保留考勤项目查看能力，不执行后台维护 |
| 考勤员 | 是 | 是 | 否 | 否 | 可查看固定考勤项目字段清单，不提供人工维护能力 |
| 部门负责人 | 否 | 否 | 否 | 否 | 不进入该页面 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台配置页面 |

**3.2.3.1.1.3 数据规则**
1. 考勤项目由后端直接写入数据库，作为考勤计算结果的固定输出字段清单。
2. 本节字段与 `3.2.2.6 考勤计算` 强关联；考勤计算的汇总模式列表字段应统一来源于本节定义的字段中。
3. 当前字段范围按已见截图先收集已确认的考勤计算结果字段；后续若新增明细模式字段、展示字段或导出字段，应同步更新本文档。
4. 本节字段不再区分系统内置项目与用户自定义项目，也不提供人工新增、删除、禁用、排序、分类和公式绑定能力。
5. 当前已确认字段当前先作为考勤计算汇总模式列表展示字段和导出字段来源；明细模式字段后续单独补充。
6. 字段名称、单位和展示格式统一按考勤计算当前已见口径维护。
7. 加班相关术语固定定义为：`OT1` 表示工作日加班，`OT2` 表示公休日加班，`OT3` 表示法定节假日加班；三者均为加班单审批通过后，系统依据加班日期所属日历类型自动判定的结果字段，不作为前台人工录入项。

**表1：固定考勤计算结果字段清单**

| 字段名称 | 当前展示口径/示例 | 说明 | 备注 |
| --- | --- | --- | --- |
| 考勤日期 | `2026-03-23` | 考勤结果所属日期 | 来源于考勤计算结果日期，按考勤周期、组织、员工和当日计算口径生成 |
| 员工编码 | `10072` | 员工唯一编码 | 来源于人员主数据/考勤档案 |
| 姓名 | `陈红` | 员工姓名 | 来源于人员主数据/考勤档案 |
| 组织名称 | `万慧达_曜斗科技` | 员工当前考勤组织名称 | 来源于考勤档案中的组织信息和 IPS 组织数据 |
| 职位 | `技术工程师` | 员工当前职位名称 | 来源于考勤档案中的职位信息和 IPS 员工基础数据 |
| 班次名称 | `九点班次` | 当日排班对应班次名称 | 来源于排班结果引用的固定班次清单 |
| 上下班时间 | `09:00-18:00` | 当日排班上下班时间范围 | 来源于排班结果和班次详情中的上下班时间 |
| 考勤组织 | `万慧达...` | 员工所属考勤计算组织 | 来源于考勤档案中的考勤组织字段；当前按明细模式列表展示字段收集 |
| 状态 | `已计算` | 当前考勤记录状态 | 来源于考勤计算页签状态流转结果 |
| 第一段上班时间 | `09:20` | 第一段有效上班打卡/结果时间 | 来源于排班结果、打卡记录和取卡规则计算出的第一段上班有效时间 |
| 第一段下班时间 | `18:46` | 第一段有效下班打卡/结果时间 | 来源于排班结果、打卡记录和取卡规则计算出的第一段下班有效时间 |
| 迟到次数 | `1` | 当日迟到次数 | 来源于排班结果、打卡记录与 `3.2.3.1.4 考勤制度` 中异常页签固定配置共同计算出的异常结果 |
| 迟到分钟 | `20` | 当日迟到分钟数 | 来源于排班上班时间、有效上班打卡时间以及异常页签中的迟到允许值规则计算结果 |
| 早退次数 | `0` | 当日早退次数 | 来源于排班结果、打卡记录与异常页签固定配置共同计算出的异常结果 |
| 补卡次数 | `0` | 当日补卡/补签次数 | 来源于当日已生效补签卡单据及其对应的补签结果统计 |
| 出差时长(天) | `0.00` | 当日出差时长，单位为天 | 来源于已生效出差单据，并结合出差页签固定配置按考勤计算口径汇总 |
| 调休OT1小时数 | `0.00` | 工作日调休加班小时数 | 来源于已生效加班单、加班页签固定配置及固定公式明细汇总表中对应加班公式计算结果 |
| 调休OT2小时数 | `0.00` | 公休日调休加班小时数 | 来源于已生效加班单、加班页签固定配置及固定公式明细汇总表中对应加班公式计算结果 |
| 病假时长(天) | `0.00` | 当日病假时长，单位为天 | 来源于已生效请假单中的病假申请时段，并按请假页签固定配置折算汇总 |
| 年假时长(天) | `0.00` | 当日年假时长，单位为天 | 来源于已生效请假单中的年假申请时段，并按请假页签固定配置和假期规则折算汇总 |
| 事假时长(天) | `0.00` | 当日事假时长，单位为天 | 来源于已生效请假单中的事假申请时段，并按请假页签固定配置和假期规则折算汇总 |
| 调休假时长(小时) | `0.00` | 当日调休假时长，单位为小时 | 来源于已生效请假单中的调休假申请时段，并结合调休规则按小时口径汇总 |
| 婚假时长(天) | `0.00` | 当日婚假时长，单位为天 | 来源于已生效请假单中的婚假申请时段，并按请假页签固定配置折算汇总 |
| 加班时长 | `0.00` | 当日加班时长 | 来源于已生效加班单、打卡记录及加班页签固定配置共同计算；当前截图未体现单位，暂按原字段名保留 |
| 旷工天数 | `0.00` | 当日旷工天数 | 来源于排班结果、打卡记录、请假/出差/补签等单据和异常页签固定配置综合判定后的异常计算结果 |

**3.2.3.1.1.4 业务规则**
1. 考勤项目仅作为考勤计算结果的固定输出字段清单存在，不提供人工创建、删除、编辑、禁用、自定义项目或项目分类维护能力。
2. `3.2.2.6 考勤计算` 的汇总模式列表、导出字段和相关下游统计，应统一以本节固定字段清单为准。
3. 字段值由考勤计算根据排班、打卡、请假、出差、加班、补签等基础数据计算后生成，不在本节单独维护。
4. 若后端新增考勤计算明细字段，或调整字段名称、单位、展示顺序，应同步更新本节及考勤计算相关章节。
5. 当前仅按已见截图收集字段；后续补充更多明细字段时，应继续追加到本节固定字段清单中。

###### 3.2.3.1.2 取卡规则
- 范围：用于说明系统内置的固定取卡规则。取卡规则由后端写死并保存到数据库中，前后端统一按固定口径读取，不提供人工新增、删除、编辑、启停配置能力。
- 子项：
  1. 固定取卡规则说明
  2. 固定规则清单

**3.2.3.1.2.1 功能概述**
- 本节用于说明系统固定的取卡规则口径，作为班次识别有效上班卡、下班卡及加班取卡的基础规则来源。
- 取卡规则不通过后台页面维护，不面向业务人员开放创建、删除、编辑、启停等配置能力。
- 当前先固定 `001 默认取卡规则`，其余规则如后端后续补充，再同步更新文档。

**3.2.3.1.2.2 角色范围**

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 保留取卡规则查看能力，不执行后台配置 |
| 考勤员 | 是 | 是 | 否 | 否 | 可查看固定取卡规则，不提供人工维护入口 |
| 部门负责人 | 否 | 否 | 否 | 否 | 不进入该配置页面 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台配置页面 |

**3.2.3.1.2.3 数据规则**
1. 取卡规则由后端直接写入数据库，当前先固定 `001 默认取卡规则`。
2. `001 默认取卡规则` 的基础参数固定为：编码 `001`、名称 `默认取卡规则`、上班取卡提前(H) `4`、下班取卡延后(H) `12`、最短取卡间隔(分钟) `0`、适用段次 `一段`、是否默认 `是`、跨天允许重复取卡 `是`、免卡点实际打卡控制 `免卡点直接填充`。
3. 上班第一次取卡固定为：取卡范围开始时数 `4`、取卡范围结束时数 `4`、取卡方式 `该段最早卡`。
4. 下班第一次取卡固定为：取卡范围开始时数 `4`、取卡范围结束时数 `12`、取卡方式 `该段最晚卡`。
5. 上班第二次取卡和下班第二次取卡当前未启用，后端需保留字段但当前规则不参与计算。
6. 加班取卡方式固定为 `按班次范围取卡`。
7. 当前固定规则按 `一段` 口径生效；若后端后续补充二段、三段规则，应同步更新文档。

**表1：固定取卡规则清单**

| 规则编码 | 名称 | 上班取卡提前(H) | 下班取卡延后(H) | 最短取卡间隔(分钟) | 适用段次 | 是否默认 | 跨天允许重复取卡 | 免卡点实际打卡控制 | 上班第一次取卡 | 上班第二次取卡 | 下班第一次取卡 | 下班第二次取卡 | 加班取卡方式 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `001` | 默认取卡规则 | `4` | `12` | `0` | `一段` | `是` | `是` | `免卡点直接填充` | 开始 `4` / 结束 `4` / `该段最早卡` | 当前未启用 | 开始 `4` / 结束 `12` / `该段最晚卡` | 当前未启用 | `按班次范围取卡` | 固定规则，由后端写入数据库 |

**表2：规则解释说明表**

| 规则项 | 当前值 | 规则含义 | 示例 |
| --- | --- | --- | --- |
| 上班取卡提前(H) | `4` | 系统从班次上班时间点往前 4 小时开始识别有效上班卡。 | 班次上班时间为 `09:00` 时，系统最早从 `05:00` 开始识别上班卡。 |
| 下班取卡延后(H) | `12` | 系统从班次下班时间点往后 12 小时内继续识别有效下班卡。 | 班次下班时间为 `18:00` 时，系统最晚识别到次日 `06:00` 的下班卡。 |
| 最短取卡间隔(分钟) | `0` | 当前不限制两次打卡之间的最小间隔，系统不因间隔过短而过滤记录。 | 员工 `08:30` 和 `08:31` 连续打卡时，两条记录都可进入取卡判定范围。 |
| 适用段次 | `一段` | 当前固定规则仅适用于一段班次，不覆盖二段班、三段班。 | `09:00-18:00` 的单段班次适用本规则。 |
| 是否默认 | `是` | 当前规则作为系统默认取卡规则，被班次和考勤计算优先引用。 | 新班次未指定其他规则时，默认引用 `001 默认取卡规则`。 |
| 跨天允许重复取卡 | `是` | 允许跨天场景继续识别打卡记录，不因日期变化直接截断。 | 员工加班到次日 `01:20` 打卡时，仍可作为前一工作日的有效下班卡。 |
| 免卡点实际打卡控制 | `免卡点直接填充` | 对免打卡点位，系统按规则直接补足结果，不再要求实际存在设备打卡。 | 某班次下班点设置为免打卡时，系统可直接按规则补足该点结果。 |
| 上班第一次取卡 | 开始 `4` / 结束 `4` / `该段最早卡` | 在上班时间前 4 小时到上班时间后 4 小时的识别范围内，取最早的一笔作为上班卡。 | 班次 `09:00`，员工在 `06:10`、`08:55` 打卡时，系统取 `06:10` 作为上班卡。 |
| 上班第二次取卡 | 当前未启用 | 后端保留该字段，但当前规则不进行第二次上班取卡判定。 | 当前不会再额外识别一笔“上班第二次取卡”。 |
| 下班第一次取卡 | 开始 `4` / 结束 `12` / `该段最晚卡` | 在下班时间前 4 小时到下班时间后 12 小时的识别范围内，取最晚的一笔作为下班卡。 | 班次 `18:00`，员工在 `18:06`、`21:30` 打卡时，系统取 `21:30` 作为下班卡。 |
| 下班第二次取卡 | 当前未启用 | 后端保留该字段，但当前规则不进行第二次下班取卡判定。 | 当前不会再额外识别一笔“下班第二次取卡”。 |
| 加班取卡方式 | `按班次范围取卡` | 加班场景沿用班次对应的取卡范围口径，不单独定义另一套打卡识别规则。 | 加班审批通过后，系统仍按班次范围识别该次加班相关打卡记录。 |

**3.2.3.1.2.4 业务规则**

1. 系统应支持查看固定取卡规则说明和详情。
2. 取卡规则不得提供人工创建、删除、编辑、保存、启停能力。
3. 当前固定规则按截图确认值直接落库使用，不再开放页面维护。
4. 班次设置、排班列表、考勤计算等模块引用取卡规则时，统一以本节固定规则为准。
5. 若后端新增或调整取卡规则，必须同步评估对班次、排班和考勤计算结果的影响，并更新本文档。

###### 3.2.3.1.3 班次设置
- 范围：用于说明系统内置的固定班次主数据。班次设置由后端写死并保存到数据库中，前后端统一按固定口径读取，不提供人工创建、复制、导入、删除、编辑、启停等配置能力。
- 子项：
  1. 固定班次列表说明
  2. 固定班次清单

**3.2.3.1.3.1 功能概述**
- 本节用于说明系统固定的班次主数据口径，作为排班、考勤档案和考勤计算引用的班次基础来源。
- 班次设置不通过后台页面维护，不面向业务人员开放创建、复制、导入、删除、编辑、启停等能力。
- 当前先固定截图中的 `001-016` 班次列表；班次内部详情字段后续补充时，再同步更新文档。

**3.2.3.1.3.2 角色范围**

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 保留班次设置查看能力，不执行后台配置 |
| 考勤员 | 是 | 是 | 否 | 否 | 可查看固定班次列表，不提供人工维护入口 |
| 部门负责人 | 否 | 否 | 否 | 否 | 不进入该配置页面 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台配置页面 |

**3.2.3.1.3.3 数据规则**
1. 班次设置由后端直接写入数据库，当前先固定截图中的 `001-016` 班次列表。
2. 当前固定班次列表中的 `加班补偿方式` 统一为 `调休`。
3. 当前固定班次列表中的 `组织` 统一为 `万慧达`。
4. 当前固定班次列表中的 `适用段次` 统一为 `一段`。
5. 当前固定班次列表中的 `取卡规则` 统一关联 `默认取卡规则`。
6. 当前固定班次列表中仅 `001 九点班次` 标记为默认班次，其余班次 `是否默认` 均为 `否`。
7. 当前已补充 `001-016` 的固定班次详情。
8. 若截图未给出班次类型、浮动值、休息时间等字段，则当前按“未配置/空值”记录，不自行推断。

**表1：固定班次清单**

| 班次编码 | 班次名称 | 加班补偿方式 | 组织     | 标准工时 | 适用段次 | 取卡规则       | 是否默认 | 备注                       |
| -------- | -------- | ------------ | -------- | -------- | -------- | -------------- | -------- | -------------------------- |
| `001`    | 九点班次 | `调休`       | `万慧达` | `7.75`   | `一段`   | `默认取卡规则` | `是`     | 固定班次，由后端写入数据库 |

其他已确认固定班次清单默认值详见同级文档：[固定班次清单和详情列表.md](./默认指定清单/固定班次清单和详情列表.md)。

**表2：固定班次详情表（当前先补 `001-016`）**

| 班次编码 | 班次名称 | 班次类型 | 标准工时 | 取卡规则       | 默认班次 | 段次     | 出勤类型   | 上班参考日期 | 上班时间 | 上班是否打卡 | 上班浮动值(分) | 下班参考日期 | 下班时间 | 下班是否打卡 | 下班浮动值(分) | 休息开始 | 休息结束 | 段内休息(分) | 备注                           |
| -------- | -------- | -------- | -------- | -------------- | -------- | -------- | ---------- | ------------ | -------- | ------------ | -------------- | ------------ | -------- | ------------ | -------------- | -------- | -------- | ------------ | ------------------------------ |
| `001`    | 九点班次 | `早班`   | `7.75`   | `默认取卡规则` | `是`     | `第一段` | `正常出勤` | `当天`       | `09:00`  | `是`         | 未配置         | `当天`       | `18:00`  | `是`         | 未配置         | `11:45`  | `13:00`  | `75.00`      | 固定班次详情，由后端写入数据库 |

其他已确认固定班次详情表默认值详见同级文档：[固定班次清单和详情列表.md](./默认指定清单/固定班次清单和详情列表.md)。

**3.2.3.1.3.4 业务规则**

1. 系统应支持查看固定班次列表说明和清单。
2. 班次设置不得提供人工创建、复制、导入、删除、编辑、保存、启停能力。
3. 当前固定班次列表按已确认截图值直接落库使用，不再开放页面维护。
4. 当前 `001-016` 的班次详情按截图确认值直接落库使用；后续若补充或调整班次详情，需同步更新本文档。
5. 排班向导、排班列表、考勤档案、考勤计算等模块引用班次时，统一以本节固定班次清单和已确认详情为准。
6. 班次内部详情字段、班次分段和更细规则后续补充时，需同步更新本文档和相关引用章节。

###### 3.2.3.1.4 考勤制度
- 范围：用于说明系统内置的固定考勤制度主数据。考勤制度由后端写死并保存到数据库中，前后端统一按固定口径读取，不提供人工创建、编辑、删除、保存、启停或公式配置能力。
- 子项：
  1. 固定制度基础信息
  2. 固定页签配置

**3.2.3.1.4.1 功能概述**
- 本节用于说明系统固定的考勤制度主数据口径，作为考勤档案、考勤计算、异常判断和相关业务规则引用的制度来源。
- 考勤制度不通过后台页面维护，不面向业务人员开放创建、编辑、删除、保存、启停、页签配置和公式配置等能力。
- 当前先固定截图中的 `001 默认考勤制度`；其他制度或其他页签的固定配置后续补充时，再同步更新文档。

**3.2.3.1.4.2 角色范围**

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 保留考勤制度查看能力，不执行后台维护 |
| 考勤员 | 是 | 是 | 否 | 否 | 可查看固定考勤制度，不提供人工维护能力 |
| 部门负责人 | 否 | 否 | 否 | 否 | 不进入后台考勤制度配置页面 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台考勤制度配置页面 |

**3.2.3.1.4.3 数据规则**
1. 考勤制度由后端直接写入数据库，当前先固定截图中的 `001 默认考勤制度`。
2. 当前固定制度基础信息包括：编码、名称、工作日历、考勤周期、组织、是否默认、备注。
3. 当前已确认存在的制度页签包括：`异常`、`请假`、`出差`、`加班`、`取卡`、`其他`。
4. 当前已补充 `异常`、`请假`、`出差`、`加班`、`取卡`、`其他` 六个页签的固定配置。
5. 页面中的 `计算器` 字段按系统预置实现类固定值读取，前台不支持手工编辑。
6. 当前截图中 `备注` 为空，当前按空值记录。

**表1：固定考勤制度基础信息表**

| 编码 | 名称 | 工作日历 | 考勤周期 | 组织 | 是否默认 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| `001` | 默认考勤制度 | `2020默认工作日历` | `2025年考勤周期` | `万慧达` | `是` | 空值 |

**表2：异常页签固定配置表**

**表3：请假页签固定配置表**

**表4：出差页签固定配置表**

**表5：加班页签固定配置表**

**表6：取卡页签固定配置表**

| 配置项 | 当前固定值 | 备注 |
| --- | --- | --- |
| 计算器 | `com.kingdee.shr.ats.calculate.implement.FetchCardPro` | 固定取卡计算实现类，用于按制度口径识别有效上班卡、下班卡和相关取卡结果 |

**表7：其他页签固定配置表**

**表8：固定公式变量与勾选项汇总表**

> 说明：本表用于记录当前考勤制度中已识别的页签变量、勾选项与已见公式引用关系。相关公式由后端写死并保存到数据库中，仅作为固定规则定义说明，不表示当前系统开放人工公式配置能力。

**表9：固定公式明细汇总表**

> 说明：本表用于记录当前已识别的考勤制度公式明细。公式内容由后端写死并保存到数据库中，前台不提供新增、编辑、删除或调整执行顺序能力。
> 术语说明：`OT1` 表示工作日加班，`OT2` 表示公休日加班，`OT3` 表示法定节假日加班；三者均为加班单审批通过后，系统依据加班日期所属日历类型自动判定的结果字段，不作为前台人工录入项。下方公式中的 `加班类型名称` 为系统自动判定后的结果值。



表1到表9详情内容请见：[默认考勤制度配置信息.md](./默认指定清单/默认考勤制度配置信息.md)。



**3.2.3.1.4.4 业务规则**
1. 系统应支持查看固定考勤制度基础信息和已确认的固定页签配置。
2. 考勤制度不得提供人工创建、编辑、删除、保存、启停、页签配置和公式配置能力。
3. 当前固定考勤制度按已确认截图值直接落库使用，不再开放页面维护。
4. 当前六个页签的固定配置按截图确认值直接落库使用；后续若有其他固定制度或页签值调整，需同步更新本文档。
5. 考勤档案、考勤计算、排班和异常判断引用考勤制度时，统一以本节固定考勤制度和已确认页签配置为准。
6. 计算器字段为后端预置实现类标识，只读引用，不支持业务人员人工修改。
7. 考勤制度相关公式由后端写死并保存到数据库中，前台仅作只读说明，不提供人工配置入口。

###### 3.2.3.1.5 轮班规则
- 范围：用于说明系统内置的固定轮班规则主数据。轮班规则由后端写死并保存到数据库中，前后端统一按固定口径读取，不提供人工创建、删除、编辑、保存、启停等配置能力。
- 子项：
  1. 固定轮班规则列表说明
  2. 固定轮班规则清单

**3.2.3.1.5.1 功能概述**
- 本节用于说明系统固定的轮班规则主数据口径，作为排班向导和自动排班引用的轮班模板来源。
- 轮班规则不通过后台页面维护，不面向业务人员开放创建、删除、编辑、保存、启停等能力。
- 当前先固定截图中的 `001-010` 轮班规则列表；规则详情后续补充时，再同步更新文档。

**3.2.3.1.5.2 角色范围**

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 保留轮班规则查看能力，不执行后台配置 |
| 考勤员 | 是 | 是 | 否 | 否 | 可查看固定轮班规则列表，不提供人工维护入口 |
| 部门负责人 | 否 | 否 | 否 | 否 | 不进入后台轮班规则配置页面 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台轮班规则配置页面 |

**3.2.3.1.5.3 数据规则**
1. 轮班规则由后端直接写入数据库，当前先固定截图中的 `001-010` 轮班规则列表。
2. 当前固定轮班规则列表中的 `组织` 统一为 `万慧达`。
3. 当前截图中 `备注` 列均为空，当前按空值记录。
4. 当前已补充 `001-010` 的固定轮班规则详情。
5. 若截图未给出备注或休息日对应班次信息，则当前按空值记录，不自行推断。

**表1：固定轮班规则清单**

| 编码  | 名称                 | 组织     | 备注 |
| ----- | -------------------- | -------- | ---- |
| `006` | 哺乳假轮班规则-9点半 | `万慧达` | 空值 |

其他已确认固定轮班规则详情表默认值详见同级文档：[默认轮班规则值.md](./默认指定清单/默认轮班规则值.md)。

**表2：固定轮班规则详情表（当前先补 `001-010`）**

| 规则编码 | 规则名称             | 组织     | 备注 | 序号 | 日期类型 | 班次编码 | 班次名称       | 上下班时间    |
| -------- | -------------------- | -------- | ---- | ---- | -------- | -------- | -------------- | ------------- |
| `006`    | 哺乳假轮班规则-9点半 | `万慧达` | 空值 | `1`  | `工作日` | `009`    | `哺乳假-9点半` | `09:30-17:00` |
| `006`    | 哺乳假轮班规则-9点半 | `万慧达` | 空值 | `2`  | `工作日` | `009`    | `哺乳假-9点半` | `09:30-17:00` |
| `006`    | 哺乳假轮班规则-9点半 | `万慧达` | 空值 | `3`  | `工作日` | `009`    | `哺乳假-9点半` | `09:30-17:00` |
| `006`    | 哺乳假轮班规则-9点半 | `万慧达` | 空值 | `4`  | `工作日` | `009`    | `哺乳假-9点半` | `09:30-17:00` |
| `006`    | 哺乳假轮班规则-9点半 | `万慧达` | 空值 | `5`  | `工作日` | `009`    | `哺乳假-9点半` | `09:30-17:00` |
| `006`    | 哺乳假轮班规则-9点半 | `万慧达` | 空值 | `6`  | `休息日` | 空值     | 空值           | 空值          |
| `006`    | 哺乳假轮班规则-9点半 | `万慧达` | 空值 | `7`  | `休息日` | 空值     | 空值           | 空值          |

其他已确认固定轮班规则详情表默认值详见同级文档：[默认轮班规则值.md](./默认指定清单/默认轮班规则值.md)。



**3.2.3.1.5.4 业务规则**

1. 系统应支持查看固定轮班规则列表说明和清单。
2. 轮班规则不得提供人工创建、删除、编辑、保存、启停能力。
3. 当前固定轮班规则列表按已确认截图值直接落库使用，不再开放页面维护。
4. 当前 `001-010` 的轮班规则详情按截图确认值直接落库使用。
5. 排班向导和自动排班引用轮班规则时，统一以本节固定轮班规则清单和已确认详情为准。
6. 轮班规则详情、周期内容和更细规则后续补充时，需同步更新本文档和相关引用章节。

##### 3.2.3.2 基础数据与系统设置
- 范围：说明本系统对 IPS 现有基础能力的复用方式，并维护本系统自有的考勤基础数据。
- 子项：
  1. 考勤数据设置
  2. 使用 IPS 中的权限控制
  3. 业务字段配置映射说明


###### 3.2.3.2.1 考勤数据设置
- 范围：统一维护考勤业务使用的基础属性数据，采用固定基础数据类型下的属性项增删改查模式，不复刻金蝶灵活复杂的基础数据配置能力。
- 子项：
  1. 基础数据类型列表
  2. 属性项维护
  3. 属性项查看

**3.2.3.2.1.1 功能概述**
- 用于统一维护考勤业务的基础属性数据，作为请假、出差、加班、补签、班次、日历、考勤状态等功能的基础数据来源。
- 本模块仅维护固定基础数据类型下的属性项，支持属性项的新增、编辑、删除、查看，不支持用户自定义新增新的基础数据类型。
- 本模块不承载复杂规则引擎、公式配置、计算器配置或动态扩展能力，重点是把考勤业务所需的基础属性维护起来并供其他功能引用。

**3.2.3.2.1.2 角色范围**

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 保留考勤基础数据查看能力，不执行后台维护 |
| 考勤员 | 是 | 否 | 是 | 是 | 负责维护考勤基础数据类型下的属性项 |
| 部门负责人 | 否 | 否 | 否 | 否 | 不进入该配置页面 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台配置页面 |

**3.2.3.2.1.3 功能范围**
1. 支持查看固定的考勤基础数据类型列表。
2. 支持通过搜索快速定位目标基础数据类型。
3. 支持查看某一基础数据类型下的属性项。
4. 支持新增属性项。
5. 支持编辑属性项。
6. 支持删除属性项。
7. 支持统一维护以下固定基础数据类型：加班原因、加班类型、加班补偿方式、出差类型、假期类型库、补签卡原因、考勤设备、班次类型、日历模板、法定假日、工作日历、考勤周期、考勤状态。

**3.2.3.2.1.4 页面模块说明**
1. 页面标题：`考勤基础数据`
2. 按钮：`创建`、`删除`、`查看`
3. 勾选项：`精确搜索`
4. 输入框：搜索输入框
5. 列表：基础数据类型列表
6. 维护页：用于查看和维护所选基础数据类型下的属性项

**3.2.3.2.1.5 列表字段定义**

| 字段名称 | 说明 |
| --- | --- |
| 选择框 | 用于勾选某条基础数据类型记录 |
| 编码 | 基础数据类型编码 |
| 名称 | 基础数据类型名称 |

**表：固定基础数据类型清单**

| 编码 | 名称 |
| --- | --- |
| ATS001 | 加班原因 |
| ATS002 | 加班类型 |
| ATS003 | 加班补偿方式 |
| ATS004 | 出差类型 |
| ATS005 | 假期类型库 |
| ATS006 | 补签卡原因 |
| ATS007 | 考勤设备 |
| ATS008 | 班次类型 |
| ATS009 | 日历模板 |
| ATS010 | 法定假日 |
| ATS011 | 工作日历 |
| ATS012 | 考勤周期 |
| ATS013 | 考勤状态 |

**3.2.3.2.1.6 交互逻辑**
1. 管理员进入“考勤基础数据”页面后，系统展示固定的基础数据类型列表。
2. 管理员可通过搜索框或精确搜索定位目标基础数据类型。
3. 管理员选中某一基础数据类型后点击“查看”，进入该类型的属性项维护页面。
4. 管理员在属性项维护页面可新增属性项，录入并保存基础属性数据。
5. 管理员在属性项维护页面可编辑已有属性项，保存后更新该类型下的数据内容。
6. 管理员在属性项维护页面可删除未使用或允许删除的属性项。
7. 保存后的属性项可作为相关业务页面的可选值或基础配置来源被引用。

**3.2.3.2.1.7 数据规则**
1. 考勤基础数据采用“固定类型 + 类型下属性项”的数据结构。
2. 基础数据类型为系统预置，不支持用户自行新增、删除或重定义基础数据类型。
3. 每条属性项归属于一个基础数据类型。
4. 不同业务页面引用对应基础数据类型下的属性项作为可选值或基础配置来源。
5. 本节当前仅定义统一 CRUD 维护能力，不展开各类型下属性项的复杂字段结构和高级规则。

**3.2.3.2.1.8 业务规则**
1. 本模块的目标是维护考勤基础属性，不建设金蝶式的灵活基础数据平台。
2. 系统仅支持在既定基础数据类型下维护属性项，不支持新增自定义基础数据类型。
3. 加班原因、加班类型、加班补偿方式、出差类型、假期类型库、补签卡原因、考勤设备、班次类型、日历模板、法定假日、工作日历、考勤周期、考勤状态应统一纳入本模块维护范围。
4. 各基础数据类型下的属性项均采用统一的新增、编辑、删除、查看维护方式。
5. 若某属性项已被业务单据、班次、制度或其他配置引用，删除前应进行引用校验。
6. 本模块不负责考勤计算、复杂联动判断、公式维护和高级规则配置。

**3.2.3.2.1.9 验收标准**
1. 管理员可进入“考勤基础数据”页面并查看固定基础数据类型列表。
2. 页面可展示编码、名称字段，并支持搜索定位基础数据类型。
3. 管理员可进入某一基础数据类型的属性项维护页面。
4. 管理员可对属性项执行新增、编辑、删除、查看操作。
5. 系统不提供新增自定义基础数据类型的能力。
6. 上述 13 类基础数据类型均纳入本模块维护范围。
7. 已保存的属性项可被相关业务页面作为基础数据引用。

**3.2.3.2.1.10 待确认问题**
1. 各基础数据类型下的属性项字段结构当前未逐项展开，后续若需要按类型细化，再单独补充。
2. `查看` 操作是进入独立详情页，还是直接进入该类型的属性项维护页，当前待界面方案最终确认。

###### 3.2.3.2.2 数据权限设置
- 范围：使用 IPS 中现有的权限控制配置结果，作为本系统菜单可见范围、组织人员可见范围和数据访问范围的统一来源，本系统不重复建设独立的数据权限配置功能。
- 子项：
  1. IPS 权限结果引用
  2. 权限边界说明

**3.2.3.2.2.1 功能概述**
- 本系统集成在 IPS 中运行，菜单权限、角色权限、组织可见范围、人员可见范围和数据访问范围均直接复用 IPS 中现有的权限控制配置结果。
- 本系统不单独提供“角色权限配置”“角色权限定义”“数据权限范围”三类本地配置页面，仅按 IPS 返回的权限结果控制页面可见性、查询范围和可操作范围。

**3.2.3.2.2.2 角色范围**

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 否 | 否 | 否 | 否 | 权限在 IPS 中配置，本系统不提供独立权限配置页面 |
| 考勤员 | 否 | 否 | 否 | 否 | 在 IPS 授权范围内执行后台功能 |
| 部门负责人 | 否 | 否 | 否 | 否 | 在 IPS 授权范围内使用本系统功能 |
| 普通员工 | 否 | 否 | 否 | 否 | 在 IPS 授权范围内使用员工自助功能 |

**3.2.3.2.2.3 功能范围**
1. 使用 IPS 中现有菜单权限控制本系统功能入口的可见性。
2. 使用 IPS 中现有组织和人员权限控制本系统数据查看范围。
3. 使用 IPS 中现有角色权限控制本系统可执行操作范围。
4. 将 IPS 权限结果应用到员工自助、管理后台、审批中心、报表等功能。
5. 本系统不提供独立的数据权限配置、角色权限定义和数据范围维护功能。

**3.2.3.2.2.4 页面模块说明**
1. 暂无独立页面：数据权限由 IPS 统一配置，本系统不提供单独的数据权限设置页面。
2. 业务页面权限控制：各业务页面在加载时按 IPS 返回的权限结果控制入口展示、查询范围和操作权限。

**3.2.3.2.2.5 交互逻辑**
1. 用户通过 IPS 入口进入本系统。
2. 系统读取当前登录用户在 IPS 中的角色、组织和人员权限结果。
3. 系统按权限结果控制菜单是否展示、页面是否可进入、数据是否可见、操作是否可执行。
4. 当用户在 IPS 中的权限配置发生变化后，本系统在后续访问时按最新权限结果生效。

**3.2.3.2.2.6 数据规则**
1. 本系统的数据权限来源为 IPS，非本系统本地维护。
2. 菜单可见范围、组织范围、人员范围和操作范围均以 IPS 返回结果为准。
3. 本系统中所有引用 `3.2.3.2.2 数据权限设置` 的地方，均表示“使用 IPS 中现有权限控制结果”。
4. 本系统不额外保存一套与 IPS 平行的数据权限主数据。

**3.2.3.2.2.7 业务规则**
1. 本系统必须复用 IPS 权限控制能力，不重复建设本地权限配置功能。
2. 用户若无 IPS 授权，不应看到对应菜单、页面或数据。
3. 用户若仅具备查看权限，不应执行新增、编辑、审批、导出、计算、配置等操作。
4. 本系统权限边界应与 IPS 中的组织、人员、角色配置保持一致。

**3.2.3.2.2.8 验收标准**
1. 用户通过 IPS 进入本系统后，系统可按 IPS 权限结果控制菜单和页面可见性。
2. 考勤员、部门负责人、普通员工在本系统中看到的数据范围与 IPS 权限范围一致。
3. 本系统内不存在独立的数据权限配置页面。
4. 当 IPS 权限发生变化后，本系统后续访问可按最新权限结果生效。

**3.2.3.2.2.9 待确认问题**
1. IPS 向本系统提供权限结果的方式当前未展开，后续需在接口设计阶段补充。
2. 权限结果是登录时一次性加载，还是按接口实时校验，后续需结合集成方案确认。

###### 3.2.3.2.3 业务字段配置映射
- 范围：作为文档说明章节，汇总业务字段的数据来源与引用关系，便于需求、设计和开发阶段统一字段口径；本节不作为系统功能建设范围。
- 子项：
  1. 字段来源说明
  2. 适用模块映射
  3. 维护约定

**3.2.3.2.3.1 章节说明**
- 本节仅用于说明业务字段的数据来源、影响模块和引用关系，不提供页面、按钮、配置流程或独立维护能力。
- 各业务章节中引用 `3.2.3.2.3 业务字段配置映射` 时，表示“参照本说明章节确认字段来源”，不表示系统中存在独立的配置功能。

**3.2.3.2.3.2 使用原则**
1. 业务页面中的同义字段应尽量复用同一数据来源。
2. 简单类型字段应优先引用统一基础数据项。
3. 本节不替代复杂业务规则、复杂计算规则和复杂联动规则配置。

**表1：考勤基础数据**

| 类型 | 作用 | 典型内容 | 影响模块 | 备注 |
| --- | --- | --- | --- | --- |
| 考勤基础数据 | 维护系统共用的基础数据项、字典项、规则项 | 加班原因、加班类型、加班补偿方式、出差类型、补签卡原因、班次类型、日历模板、法定假日、工作日历、考勤周期、考勤状态、考勤设备 | 我的加班、我要出差、我要补签、我的考勤、打卡记录、排班管理、考勤制度、请假相关模块 | 主要解决“可选什么值、按什么基础规则算”的问题 |
| 加班原因 | 定义加班申请可选原因 | 工作任务、项目上线等 | 我的加班、加班单 | 已明确属于基础数据 |
| 加班类型 | 定义加班业务分类 | 工作日加班、公休日加班、法定节假日加班等 | 我的加班、加班单、调休规则 | 由系统在加班单审批通过后依据加班日期所属日历类型自动判定，影响补偿计算 |
| 加班补偿方式 | 定义加班后补偿口径 | 调休、加班费 | 我的加班、加班单 | 与调休规则关联 |
| 出差类型 | 定义出差单可选分类 | 市内、外地、长期等 | 我要出差、出差单 | 当前员工自助里是跳 IPS，但后台仍受影响 |
| 假期类型库 | 定义请假可选类型 | 年假、病假、事假等 | 我要请假、请假单、假期制度 | 实际更偏假期管理基础数据 |
| 补签卡原因 | 定义补签时可选原因 | 忘记打卡、外出公干、参加公司团建等 | 我要补签、补签卡管理、打卡异常处理 | 已明确带“是否允许提前补签”规则 |
| 考勤设备 | 定义打卡设备来源 | 云之家、考勤机等 | 打卡记录、考勤设备对接 | 影响数据采集来源 |
| 班次类型 | 定义班次分类 | 固定班次、弹性班次等 | 班次设置、排班管理、我的考勤 | 影响排班和考勤计算 |
| 日历模板 | 定义工作日/休息日模板 | 标准工作周等 | 工作日历、考勤制度、我的考勤 | 影响月历展示和考勤计算 |
| 法定假日 | 定义法定节假日日期 | 春节、国庆等 | 工作日历、考勤制度、请假/加班计算 | 影响假期和加班规则 |
| 工作日历 | 定义实际工作日安排 | 某制度使用的工作日历 | 考勤制度、请假制度、我的考勤 | 既是基础数据，也会被查询配置引用 |
| 考勤周期 | 定义考勤统计周期 | 月度周期、起止日期 | 我的考勤、考勤计算、报表 | 影响统计口径 |
| 考勤状态 | 定义状态字典 | 正常、迟到、早退、缺卡等 | 我的考勤、打卡记录、出勤记录、异常报表 | 影响前端展示和统计汇总 |
| 云之家签到接口配置 / 金蝶考勤机 | 定义外部设备接入参数 | 第三方接入配置 | 打卡记录、考勤采集 | 更偏系统接入项 |

**3.2.3.2.3.3 维护约定**
1. 本节为文档说明章节，不计入系统功能建设范围。
2. 本节内容用于统一需求、设计、开发和测试阶段的字段来源理解。
3. 若某字段来源发生调整，应同步更新本节说明内容和各业务章节中的字段数据来源引用。

##### 3.2.3.3 审批工作流
- 范围：用于说明请假、出差、加班、补签等单据在审批中心的固定审批流转规则。审批工作流由后端写死，不支持用户在后台进行创建、编辑、发布、停用、复制版本或条件分支配置。
- 子项：
  1. 固定流程结构说明
  2. 审核节点说明
  3. 审核人规则说明

**3.2.3.3.1 功能概述**
- 用于说明审批中心所使用的固定审批流规则，供 `3.2.2.10 审批中心` 进行任务流转。
- 审批工作流不提供前台配置能力，流程结构、节点顺序、节点动作和审核人解析逻辑均由后端固定实现。
- 当前默认审批流程为一级审核：`呈批 -> 部门负责人审核（组长审核） -> 完成`。
- 系统需预留二级审核能力：`呈批 -> 部门负责人审核（组长审核） -> 领导审核 -> 完成`。
- 一级审核节点和二级审核节点均支持多个审核人，处理方式固定为`或签`。

**3.2.3.3.2 角色范围**

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看固定审批工作流说明，不执行后台维护 |
| 考勤员 | 是 | 是 | 否 | 否 | 可查看固定审批工作流说明，不提供人工配置能力 |
| 部门负责人 | 否 | 否 | 否 | 否 | 不进入审批工作流说明页 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入审批工作流说明页 |

**3.2.3.3.3 功能范围**
1. 支持查看请假、出差、加班、补签四类单据共用的固定审批流程结构。
2. 支持查看当前默认一级审核流程：`呈批 -> 部门负责人审核（组长审核） -> 完成`。
3. 支持查看后端预留的二级审核流程：`呈批 -> 部门负责人审核（组长审核） -> 领导审核 -> 完成`。
4. 支持查看一级审核节点和二级审核节点的审核人规则。
5. 支持查看各审核节点多审核人处理方式固定为`或签`。
6. 支持查看审批工作流与审批中心的关联说明。

**3.2.3.3.4 页面模块说明**
1. 流程类型展示区：用于展示请假、出差、加班、补签对应的固定审批流说明。
2. 固定流程展示区：用于展示当前默认一级审核流程和后端预留二级审核流程。
3. 节点说明区：用于展示`呈批`、`部门负责人审核（组长审核）`、`领导审核`、`完成`等节点含义。
4. 审核人规则区：用于展示各审核节点支持多个审核人且按`或签`处理。
5. 关联说明区：用于说明审批工作流与 `3.2.2.10 审批中心` 的流转关系。

**3.2.3.3.5 列表字段定义**

| 字段名称 | 说明 |
| --- | --- |
| 流程类型 | 请假/出差/加班/补签 |
| 当前启用流程 | 当前后端生效的固定流程结构 |
| 预留流程 | 后端预留但当前未必启用的扩展流程结构 |
| 一级审核节点 | 部门负责人审核（组长审核）节点说明 |
| 二级审核节点 | 领导审核节点说明 |
| 审核方式 | 多审核人处理方式，固定为或签 |

**3.2.3.3.6 展示字段定义**
1. 流程结构：`呈批 -> 部门负责人审核（组长审核） -> 完成`，以及预留的 `呈批 -> 部门负责人审核（组长审核） -> 领导审核 -> 完成`。
2. 节点属性：节点名称、节点顺序、是否当前启用、是否后端预留。
3. 审核人属性：审核节点支持多个审核人，处理方式固定为`或签`。
4. 关联说明：审批任务进入审批中心后按后端固定流程流转，前台不提供流程配置入口。

**3.2.3.3.7 交互逻辑**
1. 用户进入“审批工作流”说明页后，系统展示各流程类型对应的固定审批流说明。
2. 系统默认展示当前启用的一级审核流程：`呈批 -> 部门负责人审核（组长审核） -> 完成`。
3. 系统同时展示后端预留的二级审核流程：`呈批 -> 部门负责人审核（组长审核） -> 领导审核 -> 完成`。
4. 用户可查看一级审核节点和二级审核节点的审核人规则说明。
5. 当后端启用二级审核时，新提交单据按二级审核流程进入审批中心流转；未启用时仍按一级审核流程流转。
6. 审批处理动作仍统一在 `3.2.2.10 审批中心` 执行，本节仅承载固定流程说明。

**3.2.3.3.8 数据规则**
1. 审批工作流由后端固定实现，不提供前台创建、编辑、发布、停用、复制版本、条件分支配置等能力。
2. 当前默认流程固定为：`呈批 -> 部门负责人审核（组长审核） -> 完成`。
3. 后端需预留二级审核流程：`呈批 -> 部门负责人审核（组长审核） -> 领导审核 -> 完成`。
4. 二级审核相较一级审核，仅新增一个`领导审核`节点，其余流程结构保持一致。
5. `部门负责人审核（组长审核）`节点和`领导审核`节点均支持多个审核人。
6. 多个审核人处理方式固定为`或签`，任一审核人处理完成即可流转到下一节点。
7. 审批工作流规则变更后，应影响审批中心新生成任务的流转路径；流程中的历史任务按提交时的后端生效规则执行。

**3.2.3.3.9 业务规则**

1. 审批工作流不支持业务人员在后台进行人工配置。
2. 当前业务默认使用一级审核流程：`呈批 -> 部门负责人审核（组长审核） -> 完成`。
3. 系统需预留二级审核能力，以便后端扩展为：`呈批 -> 部门负责人审核（组长审核） -> 领导审核 -> 完成`。
4. 一级审核节点和二级审核节点均支持多个审核人，审批方式固定为`或签`。
5. 二级审核是在一级审核基础上新增一个审核节点，不单独引入条件分支配置能力。
6. 审批工作流页面不提供审批处理动作，审批处理统一在 `3.2.2.10 审批中心` 执行。

**3.2.3.3.10 验收标准**
1. 文档中明确说明审批工作流由后端写死，不支持用户在后台进行配置。
2. 文档中明确展示当前默认一级审核流程：`呈批 -> 部门负责人审核（组长审核） -> 完成`。
3. 文档中明确展示后端预留二级审核流程：`呈批 -> 部门负责人审核（组长审核） -> 领导审核 -> 完成`。
4. 文档中明确说明一级审核节点和二级审核节点均支持多个审核人。
5. 文档中明确说明多个审核人处理方式固定为`或签`。
6. 文档中明确说明审批工作流仅为固定规则说明，审批处理仍在 `3.2.2.10 审批中心` 执行。
### 3.3 报表设计
#### 3.3.1 考勤报表
##### 3.3.1.1 考勤结果明细
###### 3.3.1.1.1 功能概述
用于按日期查询员工已计算完成的考勤结果明细数据，并以列表形式展示逐日考勤结果。页面支持按组织、员工编码、姓名等条件查询结果，并支持导出当前查询结果。

###### 3.3.1.1.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看并导出权限范围内考勤结果明细 |
| 考勤员 | 是 | 是 | 否 | 否 | 主要使用角色，可查询并导出权限范围内考勤结果明细 |
| 部门负责人 | 是 | 是 | 否 | 否 | 可查看权限范围内员工考勤结果明细 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台考勤结果明细报表 |

###### 3.3.1.1.3 功能范围
1. 支持按组织、员工编码、姓名、考勤日期查询已计算完成的考勤结果明细。
2. 支持按日期和员工维度展示逐日考勤结果列表。
3. 支持展示员工基础信息字段和考勤结果字段。
4. 支持导出当前查询结果。
5. 支持考勤组修改入口。

###### 3.3.1.1.4 页面模块说明
1. 查询条件区：用于录入组织、员工编码、姓名、考勤日期等查询条件。
2. 工具栏区：用于执行查询、考勤组修改、导出操作。
3. 结果列表区：用于按日期展示员工考勤结果明细。

###### 3.3.1.1.5 列表字段定义
1. 基础信息字段：考勤日期、员工编码、姓名、组织名称、职位、班次名称、上下班时间、考勤组织、状态。
2. 考勤结果字段：第一段上班时间、第一段下班时间，以及后续按 `3.2.3.1.1 考勤项目` 固定字段清单定义的考勤结果字段。

###### 3.3.1.1.6 展示字段定义
1. 查询条件区：组织、员工编码、姓名、考勤日期。
2. 工具栏区：查询、考勤组修改、导出。
3. 结果列表区：左侧展示考勤日期和员工基础信息，右侧展示考勤结果字段。

###### 3.3.1.1.7 交互逻辑
1. 用户进入考勤结果明细后，先输入或选择组织、员工编码、姓名、考勤日期等查询条件。
2. 用户点击 `查询` 后，系统返回符合条件的已计算考勤结果明细列表。
3. 用户点击 `导出` 后，系统将当前查询结果导出。
4. 用户点击 `考勤组修改` 后，系统进入对应的考勤组调整处理流程。

###### 3.3.1.1.8 数据规则
1. 本报表仅展示已计算完成的考勤结果明细，不展示未计算或待计算数据。
2. 页面查询范围中的组织和人员应遵循当前用户数据权限范围。
3. 列表按 `考勤日期 + 员工` 维度展示结果，同一员工同一天一行。
4. 列表左侧基础信息字段用于标识日期和员工身份信息，右侧考勤结果字段主体统一来源于 `3.2.3.1.1 考勤项目` 固定字段清单。
5. 本报表展示的是考勤计算后的结果明细，不是原始打卡流水。
6. 本报表与 `3.2.2.6 考勤计算` 的 `已计算 -> 明细模式` 使用同类结果数据，当前作为独立报表页按日期查询和导出。

###### 3.3.1.1.8.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 考勤结果明细 | 组织、员工编码、姓名可选范围 | `3.2.3.2.2 数据权限设置`、`IPS 员工基础数据` |
| 考勤结果明细 | 考勤日期 | 查询条件、考勤计算结果 |
| 考勤结果明细 | 组织名称、职位、班次名称、上下班时间、考勤组织、状态 | 考勤计算结果、考勤档案、排班结果 |
| 考勤结果明细 | 考勤结果字段 | `3.2.3.1.1 考勤项目` |
| 考勤结果明细 | 导出结果 | 当前查询结果列表 |

###### 3.3.1.1.9 业务规则
1. 考勤结果明细仅展示已计算结果，不提供人工修改结果字段能力。
2. 列表左侧基础信息字段与右侧考勤结果字段共同组成完整明细行。
3. 考勤结果字段主体统一采用 `3.2.3.1.1 考勤项目` 固定字段清单，不单独维护另一套结果字段体系。
4. 导出结果应与当前查询条件和当前列表展示结果保持一致。
5. 本报表是 `3.2.2.6 考勤计算` 已计算明细结果的报表化展示入口，不替代考勤计算主页面的计算能力。

###### 3.3.1.1.10 验收标准
1. 用户可按组织、员工编码、姓名、考勤日期查询考勤结果明细。
2. 页面可按 `考勤日期 + 员工` 维度展示逐日考勤结果明细。
3. 列表可展示基础信息字段和考勤结果字段。
4. 考勤结果字段主体统一来源于 `3.2.3.1.1 考勤项目`。
5. 用户可导出当前查询结果。

##### 3.3.1.2 考勤结果汇总
###### 3.3.1.2.1 功能概述
用于按员工维度汇总指定时间范围内的已计算考勤结果，并展示各考勤项目的累计结果。与 `3.3.1.1 考勤结果明细` 按 `考勤日期 + 员工` 展示逐日结果不同，本报表按员工汇总展示查询范围内的累计值，并支持导出当前结果。

###### 3.3.1.2.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看并导出权限范围内考勤结果汇总 |
| 考勤员 | 是 | 是 | 否 | 否 | 主要使用角色，可查询并导出权限范围内考勤结果汇总 |
| 部门负责人 | 是 | 是 | 否 | 否 | 可查看权限范围内员工考勤结果汇总 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台考勤结果汇总报表 |

###### 3.3.1.2.3 功能范围
1. 支持按组织、员工编码、姓名、开始日期、结束日期查询已计算考勤结果汇总。
2. 支持按员工维度汇总展示查询时间范围内的考勤结果累计值。
3. 支持展示员工基础信息字段和考勤项目累计结果字段。
4. 支持导出当前查询结果。
5. 支持 `员工和行政组汇总总` 入口。

###### 3.3.1.2.4 页面模块说明
1. 查询条件区：用于录入组织、员工编码、姓名、开始日期、结束日期等查询条件。
2. 工具栏区：用于执行查询、员工和行政组汇总总、导出操作。
3. 结果列表区：用于按员工展示查询范围内的考勤结果累计值。

###### 3.3.1.2.5 列表字段定义
1. 基础信息字段：员工编码、姓名、组织名称、当前任职。
2. 考勤结果汇总字段：迟到次数、早退次数、补卡次数、出差时长(天)、调休OT1小时数、调休OT2小时数、病假时长(天)、年假时长(天)、事假时长(天)、调休假时长(小时)、婚假时长(天)、加班时长、旷工天数，以及后续按 `3.2.3.1.1 考勤项目` 固定字段清单补充的汇总字段。

###### 3.3.1.2.6 展示字段定义
1. 查询条件区：组织、员工编码、姓名、开始日期、结束日期。
2. 工具栏区：查询、员工和行政组汇总总、导出。
3. 结果列表区：左侧展示员工基础信息，右侧展示查询时间范围内的考勤项目累计结果。

###### 3.3.1.2.7 交互逻辑
1. 用户进入考勤结果汇总后，先输入或选择组织、员工编码、姓名、开始日期、结束日期等查询条件。
2. 用户点击 `查询` 后，系统返回符合条件的已计算考勤结果汇总列表。
3. 用户点击 `导出` 后，系统将当前查询结果导出。
4. 用户点击 `员工和行政组汇总总` 后，系统进入对应的汇总查看或处理流程；具体处理逻辑后续补充。

###### 3.3.1.2.8 数据规则
1. 本报表仅展示已计算完成的考勤结果汇总，不展示未计算或待计算数据。
2. 页面查询范围中的组织和人员应遵循当前用户数据权限范围。
3. 列表按 `员工` 维度汇总展示结果，同一员工在当前查询范围内汇总为一行。
4. 列表左侧基础信息字段用于标识员工身份信息，右侧考勤结果字段主体统一来源于 `3.2.3.1.1 考勤项目` 固定字段清单，但在本报表中展示的是查询时间范围内的累计结果。
5. 本报表展示的是考勤计算后的汇总结果，不是原始打卡流水，也不是逐日明细记录。
6. 本报表与 `3.2.2.6 考勤计算` 的 `已计算 -> 汇总模式` 使用同类结果数据，当前作为独立报表页按时间范围查询和导出。

###### 3.3.1.2.8.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 考勤结果汇总 | 组织、员工编码、姓名可选范围 | `3.2.3.2.2 数据权限设置`、`IPS 员工基础数据` |
| 考勤结果汇总 | 开始日期、结束日期 | 查询条件 |
| 考勤结果汇总 | 组织名称、当前任职 | `IPS 员工基础数据`、考勤档案结果数据 |
| 考勤结果汇总 | 考勤结果汇总字段 | `3.2.3.1.1 考勤项目`、考勤计算结果 |
| 考勤结果汇总 | 导出结果 | 当前查询结果列表 |

###### 3.3.1.2.9 业务规则
1. 考勤结果汇总仅展示已计算结果，不提供人工修改汇总结果能力。
2. 列表左侧基础信息字段与右侧汇总字段共同组成完整汇总行。
3. 汇总字段主体统一采用 `3.2.3.1.1 考勤项目` 固定字段清单，不单独维护另一套汇总字段体系。
4. `3.3.1.1 考勤结果明细` 按 `考勤日期 + 员工` 展示逐日结果，本报表按 `员工` 维度汇总查询范围内的累计结果。
5. 导出结果应与当前查询条件和当前列表展示结果保持一致。
6. 本报表是 `3.2.2.6 考勤计算` 已计算汇总结果的报表化展示入口，不替代考勤计算主页面的计算能力。

###### 3.3.1.2.10 验收标准
1. 用户可按组织、员工编码、姓名、开始日期、结束日期查询考勤结果汇总。
2. 页面可按员工维度展示查询时间范围内的考勤结果累计值。
3. 列表可展示员工基础信息字段和考勤结果汇总字段。
4. 考勤结果汇总字段主体统一来源于 `3.2.3.1.1 考勤项目`。
5. 用户可导出当前查询结果。

##### 3.3.1.3 考勤统计分析报表

#### 3.3.2 假期报表
##### 3.3.2.1 假期汇总表
###### 3.3.2.1.1 功能概述
用于按组织、员工、时间范围和假期制度查询员工各假期类型的已请时长汇总结果。页面支持按查询条件生成汇总列表，并支持导出当前结果。

###### 3.3.2.1.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看并导出权限范围内假期汇总结果 |
| 考勤员 | 是 | 是 | 否 | 否 | 主要使用角色，可查询并导出权限范围内假期汇总结果 |
| 部门负责人 | 是 | 是 | 否 | 否 | 可查看权限范围内员工假期汇总结果 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台假期汇总表 |

###### 3.3.2.1.3 功能范围
1. 支持按组织、姓名、开始日期、结束日期、假期制度查询假期汇总结果。
2. 支持按员工维度展示指定时间范围内各假期类型的已请时长汇总结果。
3. 支持按假期类型分列展示汇总值。
4. 支持导出当前查询结果。

###### 3.3.2.1.4 页面模块说明
1. 查询条件区：用于录入组织、姓名、开始日期、结束日期、假期制度等查询条件。
2. 工具栏区：用于执行查询、导出操作。
3. 结果列表区：用于按员工展示各假期类型的汇总结果。

###### 3.3.2.1.5 列表字段定义
1. 员工基础信息字段：员工编码、姓名、组织名称。
2. 假期汇总字段：法定年假(天)、司龄假(天)、事假(天)、病假(天)、调休假(小时)、婚假(天)、产假(天)、陪产假(天)、哺乳假(天)、丧假(天)、丧假路途假(天)、孕检假(天)、流产假(天)、护理假(天)、育儿假(天)、育儿假1(天)。

###### 3.3.2.1.6 展示字段定义
1. 查询条件区：组织、姓名、开始日期、结束日期、假期制度。
2. 工具栏区：查询、导出。
3. 结果列表区：员工编码、姓名、组织名称，以及各假期类型的已请时长汇总值。

###### 3.3.2.1.7 交互逻辑
1. 用户进入假期汇总表后，先选择或输入组织、姓名、开始日期、结束日期、假期制度等查询条件。
2. 用户点击 `查询` 后，系统按当前查询条件返回符合条件的员工假期汇总结果。
3. 用户点击 `导出` 后，系统将当前查询结果导出。

###### 3.3.2.1.8 数据规则
1. 本报表用于统计员工在指定时间范围内各假期类型的已请时长汇总结果，不用于展示剩余额度或在途额度。
2. 页面查询范围中的组织和人员应遵循当前用户数据权限范围。
3. 列表按员工维度汇总展示结果，同一员工一行。
4. 调休假列以 `小时` 为单位统计，其余假期列按 `天` 为单位统计。
5. 当前列表字段按假期类型汇总展示；法定年假、司龄假是否拆分去年结余与本年额度，待后续确认。
6. 假期汇总统计时间口径待确认：当前先按“指定时间范围内的已请时长汇总”整理，实际按单据申请日期统计还是按休假覆盖日期统计，后续补充确认。

###### 3.3.2.1.8.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 假期汇总表 | 组织、姓名可选范围 | `3.2.3.2.2 数据权限设置`、`IPS 员工基础数据` |
| 假期汇总表 | 假期制度 | `3.1.3.3 假期制度定义` |
| 假期汇总表 | 员工编码、姓名、组织名称 | `IPS 员工基础数据` |
| 假期汇总表 | 各假期类型汇总值 | 请假单业务数据、假期类型定义、假期制度定义 |

###### 3.3.2.1.9 业务规则
1. 假期汇总表固定按员工维度汇总展示，不提供人工调整汇总结果能力。
2. 假期汇总表统计的是已请时长，不展示剩余额度、在途额度或冻结额度。
3. 调休假在本报表中固定按 `小时` 展示，其他假期固定按 `天` 展示。
4. 导出结果应与当前查询条件和当前列表展示结果保持一致。
5. 病假、产假等无预置额度假期，若在统计范围内发生请假记录，仍应纳入本报表汇总结果。

###### 3.3.2.1.10 验收标准
1. 用户可按组织、姓名、开始日期、结束日期、假期制度查询假期汇总结果。
2. 页面可按员工维度展示各假期类型的已请时长汇总值。
3. 调休假列按小时显示，其余假期列按天显示。
4. 用户可导出当前查询结果。
5. 页面不展示剩余额度、在途额度等非汇总字段。

##### 3.3.2.2 调休明细表
###### 3.3.2.2.1 功能概述
用于查询员工由加班补偿产生的调休额度相关数据，展示调休总额度、已调休额度和剩余额度，并按 `OT1`、`OT2`、`OT3` 拆分展示对应结果。页面支持按查询条件查询当前结果，并支持按历史查询方式查看指定范围内的调休台账数据。

###### 3.3.2.2.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看并导出权限范围内调休明细结果 |
| 考勤员 | 是 | 是 | 否 | 否 | 主要使用角色，可查询权限范围内调休明细结果 |
| 部门负责人 | 是 | 是 | 否 | 否 | 可查看权限范围内员工调休明细结果 |
| 普通员工 | 否 | 否 | 否 | 否 | 不进入后台调休明细表 |

###### 3.3.2.2.3 功能范围
1. 支持按组织、姓名、假期制度、历史月份范围查询调休明细结果。
2. 支持按员工维度展示调休总额度、已调休额度和剩余额度。
3. 支持按 `OT1`、`OT2`、`OT3` 拆分展示对应额度、已调休额度和剩余额度。
4. 支持查看指定查询范围内的历史调休台账结果。

###### 3.3.2.2.4 页面模块说明
1. 查询条件区：用于录入组织、姓名、假期制度、历史月份范围等查询条件。
2. 工具栏区：用于执行查询和历史查询方式切换。
3. 结果列表区：用于按员工展示调休额度、已调休额度和剩余额度相关结果。

###### 3.3.2.2.5 列表字段定义
1. 员工基础信息字段：员工编码、姓名、组织。
2. 调休台账字段：周期、额度、`OT1`、`OT2`、`OT3`、已调休额度、已调休 `OT1`、已调休 `OT2`、已调休 `OT3`、剩余额度、剩余 `OT1`、剩余 `OT2`、剩余 `OT3`。

###### 3.3.2.2.6 展示字段定义
1. 查询条件区：组织、姓名、假期制度、`第 1 到 24 月` 历史范围条件。
2. 工具栏区：查询、历史查询方式。
3. 结果列表区：员工编码、姓名、组织、周期、额度、`OT1`、`OT2`、`OT3`、已调休额度、已调休 `OT1`、已调休 `OT2`、已调休 `OT3`、剩余额度、剩余 `OT1`、剩余 `OT2`、剩余 `OT3`。

###### 3.3.2.2.7 交互逻辑
1. 用户进入调休明细表后，先选择或输入组织、姓名、假期制度以及历史月份范围等查询条件。
2. 用户点击 `查询` 后，系统按当前条件返回符合条件的员工调休明细结果。
3. 用户点击 `历史查询方式` 后，系统按当前定义的历史范围口径返回对应台账数据。

###### 3.3.2.2.8 数据规则
1. 本报表仅统计由加班补偿产生的调休额度，不涉及其他来源的调休数据。
2. 调休额度来源于已生效加班单，按 `1:1` 转换生成调休时长。
3. `OT1`、`OT2`、`OT3` 分别表示系统在加班单审批通过后依据加班日期所属日历类型自动判定的工作日加班、公休日加班、法定节假日加班所产生的调休额度。
4. 列表按员工维度展示调休台账结果，同一员工在同一周期下展示一行结果。
5. `额度` 表示当期生成的调休总额度；`已调休额度` 表示已被调休假使用的额度；`剩余额度` 表示当前仍可使用的额度。
6. `OT1`、`OT2`、`OT3` 及其已调休、剩余列分别表示对应加班类型下的生成值、已使用值和剩余值。
7. 每次加班产生的调休额度自生成之日起 `3个月` 后失效，失效后的额度不应计入剩余额度。
8. `周期` 和 `第 1 到 24 月` 的业务口径当前按页面事实保留，具体是按自然月、历史账期还是调休生成周期统计，待后续确认。

###### 3.3.2.2.8.1 字段数据来源

| 模块 | 规则字段 | 字段数据来源 |
| --- | --- | --- |
| 调休明细表 | 组织、姓名可选范围 | `3.2.3.2.2 数据权限设置`、`IPS 员工基础数据` |
| 调休明细表 | 假期制度 | `3.1.3.3 假期制度定义` |
| 调休明细表 | 员工编码、姓名、组织 | `IPS 员工基础数据` |
| 调休明细表 | 额度、已调休额度、剩余额度 | 加班单业务数据、调休规则、调休假使用数据 |
| 调休明细表 | `OT1`、`OT2`、`OT3` 及对应已调休、剩余列 | 加班单业务数据、`OT1/OT2/OT3` 固定定义、调休规则 |
| 调休明细表 | 周期、历史月份范围 | 当前页面历史查询条件定义，具体业务口径待后续确认 |

###### 3.3.2.2.9 业务规则
1. 调休明细表仅统计由加班补偿产生的调休额度，不纳入其他来源的调休数据。
2. 加班与调休固定按 `1:1` 转换。
3. `OT1`、`OT2`、`OT3` 的固定定义分别为：工作日加班、公休日加班、法定节假日加班；三者均为加班单审批通过后，系统依据加班日期所属日历类型自动判定的结果字段。
4. 调休总额度、已调休额度、剩余额度均应支持按 `OT1`、`OT2`、`OT3` 拆分展示。
5. 每次加班产生的调休额度超过 `3个月` 后失效，失效额度不得继续计入剩余额度。
6. 调休明细表用于查看调休台账结果，不提供人工调整额度能力。

###### 3.3.2.2.10 验收标准
1. 用户可按组织、姓名、假期制度和历史月份范围查询调休明细结果。
2. 页面可按员工维度展示调休总额度、已调休额度和剩余额度。
3. 页面可按 `OT1`、`OT2`、`OT3` 拆分展示对应结果。
4. 页面仅统计由加班补偿产生的调休额度数据。
5. 超过 `3个月` 的失效调休额度不计入剩余额度。

---

## 4. 实时提醒

### 4.1 实时提醒功能
#### 4.1.1 功能概述
用于在审批流转关键节点向相关人员发送即时提醒，保障单据处理及时性。当前仅覆盖两类触发场景：创建单据、退回单据。提醒渠道以企信提醒为主；若该事项已加入小程序代办，则需同步增加对应公众号提醒。

#### 4.1.2 角色范围

| 角色 | 是否可见 | 是否只读 | 是否可写 | 是否可审批/配置 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 系统管理员 | 是 | 是 | 否 | 否 | 可查看提醒规则，不作为日常提醒处理人 |
| 考勤员 | 是 | 是 | 否 | 否 | 按审批流转结果接收提醒 |
| 部门负责人 | 是 | 是 | 否 | 否 | 作为审批人时接收创建提醒 |
| 普通员工 | 是 | 是 | 否 | 否 | 作为退回目标人员时接收退回提醒 |

#### 4.1.3 功能范围
1. 单据创建时，向当前审批人发送企信提醒。
2. 单据退回时，向目标人员发送企信提醒。
3. 若该事项已加入小程序代办，则在上述场景下同步增加公众号提醒。
4. 当前版本仅实现上述提醒场景，不扩展缺卡异常、审批超时等其他提醒。

#### 4.1.4 触发场景与接收对象

| 触发场景 | 接收对象 | 企信提醒 | 公众号提醒 | 说明 |
| --- | --- | --- | --- | --- |
| 创建单据 | 当前审批节点审批人 | 是 | 条件触发 | 单据成功创建并进入审批流后触发 |
| 退回单据 | 退回目标人员 | 是 | 条件触发 | 审批动作为退回且系统完成退回后触发 |

> 公众号提醒条件：该事项已加入小程序代办。

#### 4.1.5 提醒内容字段定义
1. 业务类型：请假单、出差单、加班单、补签卡等。
2. 单据编号：唯一单据号。
3. 发起人：单据创建人。
4. 当前节点/动作：创建待审批、审批退回。
5. 接收人：审批人或退回目标人员。
6. 触发时间：提醒生成时间。
7. 代办链接：小程序代办链接或业务详情入口（按渠道适配）。

#### 4.1.6 交互逻辑
1. 用户创建单据并提交后，系统识别当前审批节点审批人。
2. 系统向审批人发送企信提醒。
3. 系统判断该事项是否已加入小程序代办；若是，则同步发送公众号提醒。
4. 审批人执行退回后，系统识别退回目标人员。
5. 系统向目标人员发送企信提醒。
6. 系统再次判断该事项是否已加入小程序代办；若是，则同步发送公众号提醒。

#### 4.1.7 数据规则
1. 提醒发送记录应留痕，至少包含单据编号、触发场景、接收人、渠道、发送时间、发送结果。
2. 企信提醒与公众号提醒按同一业务事件生成，可分别记录发送结果。
3. 公众号提醒仅在“已加入小程序代办”条件满足时生成，不满足条件时不发送。
4. 接收对象按审批流实时节点计算，不使用静态人员配置。

#### 4.1.8 业务规则
1. 创建单据仅提醒当前审批人，不抄送其他角色。
2. 退回单据仅提醒退回目标人员，不额外提醒当前审批人。
3. 企信提醒为当前必发渠道；公众号提醒为条件追加渠道。
4. 当前版本不包含催办、超时、升级通知、批量汇总提醒等扩展能力。

#### 4.1.9 验收标准
1. 新建单据后，当前审批人可收到企信提醒。
2. 审批退回后，退回目标人员可收到企信提醒。
3. 当事项已加入小程序代办时，上述两类场景均可收到对应公众号提醒。
4. 当事项未加入小程序代办时，上述两类场景仅发送企信提醒，不发送公众号提醒。
5. 系统可查询提醒发送留痕（触发场景、接收人、渠道、发送结果）。

#### 4.1.10 待确认问题
1. 公众号提醒模板文案与模板 ID 是否按业务类型区分，待确认。
2. “已加入小程序代办”的判定口径（按单据类型配置、按单据实例状态、或按组织开关）待确认。

