# 交付检查清单

## 开发前

- 已确认业务模块和最近可复用页面。
- 已读相关路由、菜单、权限、API 和公共组件实现。
- 已确认接口字段、权限码、菜单结构是否来自后端或既有代码。
- 已界定本次改动范围，不做无关重构。

## 编码中

- 页面、路由、API、菜单命名保持一致。
- 请求统一经过 `@/utils/request`。
- 表格页复用 `grid-table`、搜索区、`TopListTool` 和分页。
- 复杂页面已拆分子组件。
- 新增文件顶部已有简短职责备注。
- 时间处理优先使用 `dayjs`。

## 自测

- 正常流程可用。
- 接口失败或超时有状态反馈。
- 空数据状态可读。
- 权限差异、菜单入口、直接访问路由符合预期。
- 刷新页面后状态和路由表现正常。
- `npm run lint` 或项目约定检查已执行；如未执行，需要说明原因。

## 交付说明

- 说明改动范围和影响模块。
- 列出关键文件和职责。
- 说明新增或调整的 API。
- 汇报已测场景、未覆盖项和风险。
